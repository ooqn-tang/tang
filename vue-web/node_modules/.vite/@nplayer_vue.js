import {
  EMPTY_OBJ,
  NO,
  NOOP,
  PatchFlagNames,
  camelize,
  capitalize,
  extend,
  generateCodeFrame,
  hyphenate,
  init_runtime_dom_esm_bundler,
  init_shared_esm_bundler,
  isArray,
  isHTMLTag,
  isObject,
  isOn,
  isReservedProp,
  isSVGTag,
  isString,
  isSymbol,
  isVoidTag,
  makeMap,
  parseStringStyle,
  runtime_dom_esm_bundler_exports,
  shared_esm_bundler_exports,
  slotFlagsText,
  toHandlerKey
} from "./chunk-ZQBMOTHB.js";
import {
  __commonJS,
  __esm,
  __export
} from "./chunk-OWZ4CSWD.js";

// node_modules/@vue/compiler-core/dist/compiler-core.esm-bundler.js
function defaultOnError(error) {
  throw error;
}
function defaultOnWarn(msg) {
  console.warn(`[Vue warn] ${msg.message}`);
}
function createCompilerError(code, loc, messages, additionalMessage) {
  const msg = true ? (messages || errorMessages)[code] + (additionalMessage || ``) : code;
  const error = new SyntaxError(String(msg));
  error.code = code;
  error.loc = loc;
  return error;
}
function registerRuntimeHelpers(helpers) {
  Object.getOwnPropertySymbols(helpers).forEach((s) => {
    helperNameMap[s] = helpers[s];
  });
}
function createRoot(children, loc = locStub) {
  return {
    type: 0,
    children,
    helpers: [],
    components: [],
    directives: [],
    hoists: [],
    imports: [],
    cached: 0,
    temps: 0,
    codegenNode: void 0,
    loc
  };
}
function createVNodeCall(context, tag, props, children, patchFlag, dynamicProps, directives, isBlock = false, disableTracking = false, isComponent2 = false, loc = locStub) {
  if (context) {
    if (isBlock) {
      context.helper(OPEN_BLOCK);
      context.helper(getVNodeBlockHelper(context.inSSR, isComponent2));
    } else {
      context.helper(getVNodeHelper(context.inSSR, isComponent2));
    }
    if (directives) {
      context.helper(WITH_DIRECTIVES);
    }
  }
  return {
    type: 13,
    tag,
    props,
    children,
    patchFlag,
    dynamicProps,
    directives,
    isBlock,
    disableTracking,
    isComponent: isComponent2,
    loc
  };
}
function createArrayExpression(elements, loc = locStub) {
  return {
    type: 17,
    loc,
    elements
  };
}
function createObjectExpression(properties, loc = locStub) {
  return {
    type: 15,
    loc,
    properties
  };
}
function createObjectProperty(key, value) {
  return {
    type: 16,
    loc: locStub,
    key: isString(key) ? createSimpleExpression(key, true) : key,
    value
  };
}
function createSimpleExpression(content, isStatic = false, loc = locStub, constType = 0) {
  return {
    type: 4,
    loc,
    content,
    isStatic,
    constType: isStatic ? 3 : constType
  };
}
function createInterpolation(content, loc) {
  return {
    type: 5,
    loc,
    content: isString(content) ? createSimpleExpression(content, false, loc) : content
  };
}
function createCompoundExpression(children, loc = locStub) {
  return {
    type: 8,
    loc,
    children
  };
}
function createCallExpression(callee, args = [], loc = locStub) {
  return {
    type: 14,
    loc,
    callee,
    arguments: args
  };
}
function createFunctionExpression(params, returns = void 0, newline = false, isSlot = false, loc = locStub) {
  return {
    type: 18,
    params,
    returns,
    newline,
    isSlot,
    loc
  };
}
function createConditionalExpression(test, consequent, alternate, newline = true) {
  return {
    type: 19,
    test,
    consequent,
    alternate,
    newline,
    loc: locStub
  };
}
function createCacheExpression(index, value, isVNode = false) {
  return {
    type: 20,
    index,
    value,
    isVNode,
    loc: locStub
  };
}
function createBlockStatement(body) {
  return {
    type: 21,
    body,
    loc: locStub
  };
}
function createTemplateLiteral(elements) {
  return {
    type: 22,
    elements,
    loc: locStub
  };
}
function createIfStatement(test, consequent, alternate) {
  return {
    type: 23,
    test,
    consequent,
    alternate,
    loc: locStub
  };
}
function createAssignmentExpression(left, right) {
  return {
    type: 24,
    left,
    right,
    loc: locStub
  };
}
function createSequenceExpression(expressions) {
  return {
    type: 25,
    expressions,
    loc: locStub
  };
}
function createReturnStatement(returns) {
  return {
    type: 26,
    returns,
    loc: locStub
  };
}
function isCoreComponent(tag) {
  if (isBuiltInType(tag, "Teleport")) {
    return TELEPORT;
  } else if (isBuiltInType(tag, "Suspense")) {
    return SUSPENSE;
  } else if (isBuiltInType(tag, "KeepAlive")) {
    return KEEP_ALIVE;
  } else if (isBuiltInType(tag, "BaseTransition")) {
    return BASE_TRANSITION;
  }
}
function getInnerRange(loc, offset, length) {
  const source = loc.source.slice(offset, offset + length);
  const newLoc = {
    source,
    start: advancePositionWithClone(loc.start, loc.source, offset),
    end: loc.end
  };
  if (length != null) {
    newLoc.end = advancePositionWithClone(loc.start, loc.source, offset + length);
  }
  return newLoc;
}
function advancePositionWithClone(pos, source, numberOfCharacters = source.length) {
  return advancePositionWithMutation(extend({}, pos), source, numberOfCharacters);
}
function advancePositionWithMutation(pos, source, numberOfCharacters = source.length) {
  let linesCount = 0;
  let lastNewLinePos = -1;
  for (let i = 0; i < numberOfCharacters; i++) {
    if (source.charCodeAt(i) === 10) {
      linesCount++;
      lastNewLinePos = i;
    }
  }
  pos.offset += numberOfCharacters;
  pos.line += linesCount;
  pos.column = lastNewLinePos === -1 ? pos.column + numberOfCharacters : numberOfCharacters - lastNewLinePos;
  return pos;
}
function assert(condition, msg) {
  if (!condition) {
    throw new Error(msg || `unexpected compiler condition`);
  }
}
function findDir(node, name, allowEmpty = false) {
  for (let i = 0; i < node.props.length; i++) {
    const p = node.props[i];
    if (p.type === 7 && (allowEmpty || p.exp) && (isString(name) ? p.name === name : name.test(p.name))) {
      return p;
    }
  }
}
function findProp(node, name, dynamicOnly = false, allowEmpty = false) {
  for (let i = 0; i < node.props.length; i++) {
    const p = node.props[i];
    if (p.type === 6) {
      if (dynamicOnly)
        continue;
      if (p.name === name && (p.value || allowEmpty)) {
        return p;
      }
    } else if (p.name === "bind" && (p.exp || allowEmpty) && isStaticArgOf(p.arg, name)) {
      return p;
    }
  }
}
function isStaticArgOf(arg, name) {
  return !!(arg && isStaticExp(arg) && arg.content === name);
}
function hasDynamicKeyVBind(node) {
  return node.props.some((p) => p.type === 7 && p.name === "bind" && (!p.arg || p.arg.type !== 4 || !p.arg.isStatic));
}
function isText(node) {
  return node.type === 5 || node.type === 2;
}
function isVSlot(p) {
  return p.type === 7 && p.name === "slot";
}
function isTemplateNode(node) {
  return node.type === 1 && node.tagType === 3;
}
function isSlotOutlet(node) {
  return node.type === 1 && node.tagType === 2;
}
function getVNodeHelper(ssr, isComponent2) {
  return ssr || isComponent2 ? CREATE_VNODE : CREATE_ELEMENT_VNODE;
}
function getVNodeBlockHelper(ssr, isComponent2) {
  return ssr || isComponent2 ? CREATE_BLOCK : CREATE_ELEMENT_BLOCK;
}
function getUnnormalizedProps(props, callPath = []) {
  if (props && !isString(props) && props.type === 14) {
    const callee = props.callee;
    if (!isString(callee) && propsHelperSet.has(callee)) {
      return getUnnormalizedProps(props.arguments[0], callPath.concat(props));
    }
  }
  return [props, callPath];
}
function injectProp(node, prop, context) {
  let propsWithInjection;
  let props = node.type === 13 ? node.props : node.arguments[2];
  let callPath = [];
  let parentCall;
  if (props && !isString(props) && props.type === 14) {
    const ret = getUnnormalizedProps(props);
    props = ret[0];
    callPath = ret[1];
    parentCall = callPath[callPath.length - 1];
  }
  if (props == null || isString(props)) {
    propsWithInjection = createObjectExpression([prop]);
  } else if (props.type === 14) {
    const first = props.arguments[0];
    if (!isString(first) && first.type === 15) {
      first.properties.unshift(prop);
    } else {
      if (props.callee === TO_HANDLERS) {
        propsWithInjection = createCallExpression(context.helper(MERGE_PROPS), [
          createObjectExpression([prop]),
          props
        ]);
      } else {
        props.arguments.unshift(createObjectExpression([prop]));
      }
    }
    !propsWithInjection && (propsWithInjection = props);
  } else if (props.type === 15) {
    let alreadyExists = false;
    if (prop.key.type === 4) {
      const propKeyName = prop.key.content;
      alreadyExists = props.properties.some((p) => p.key.type === 4 && p.key.content === propKeyName);
    }
    if (!alreadyExists) {
      props.properties.unshift(prop);
    }
    propsWithInjection = props;
  } else {
    propsWithInjection = createCallExpression(context.helper(MERGE_PROPS), [
      createObjectExpression([prop]),
      props
    ]);
    if (parentCall && parentCall.callee === GUARD_REACTIVE_PROPS) {
      parentCall = callPath[callPath.length - 2];
    }
  }
  if (node.type === 13) {
    if (parentCall) {
      parentCall.arguments[0] = propsWithInjection;
    } else {
      node.props = propsWithInjection;
    }
  } else {
    if (parentCall) {
      parentCall.arguments[0] = propsWithInjection;
    } else {
      node.arguments[2] = propsWithInjection;
    }
  }
}
function toValidAssetId(name, type) {
  return `_${type}_${name.replace(/[^\w]/g, (searchValue, replaceValue) => {
    return searchValue === "-" ? "_" : name.charCodeAt(replaceValue).toString();
  })}`;
}
function hasScopeRef(node, ids) {
  if (!node || Object.keys(ids).length === 0) {
    return false;
  }
  switch (node.type) {
    case 1:
      for (let i = 0; i < node.props.length; i++) {
        const p = node.props[i];
        if (p.type === 7 && (hasScopeRef(p.arg, ids) || hasScopeRef(p.exp, ids))) {
          return true;
        }
      }
      return node.children.some((c) => hasScopeRef(c, ids));
    case 11:
      if (hasScopeRef(node.source, ids)) {
        return true;
      }
      return node.children.some((c) => hasScopeRef(c, ids));
    case 9:
      return node.branches.some((b) => hasScopeRef(b, ids));
    case 10:
      if (hasScopeRef(node.condition, ids)) {
        return true;
      }
      return node.children.some((c) => hasScopeRef(c, ids));
    case 4:
      return !node.isStatic && isSimpleIdentifier(node.content) && !!ids[node.content];
    case 8:
      return node.children.some((c) => isObject(c) && hasScopeRef(c, ids));
    case 5:
    case 12:
      return hasScopeRef(node.content, ids);
    case 2:
    case 3:
      return false;
    default:
      if (true)
        ;
      return false;
  }
}
function getMemoedVNodeCall(node) {
  if (node.type === 14 && node.callee === WITH_MEMO) {
    return node.arguments[1].returns;
  } else {
    return node;
  }
}
function makeBlock(node, { helper, removeHelper, inSSR }) {
  if (!node.isBlock) {
    node.isBlock = true;
    removeHelper(getVNodeHelper(inSSR, node.isComponent));
    helper(OPEN_BLOCK);
    helper(getVNodeBlockHelper(inSSR, node.isComponent));
  }
}
function getCompatValue(key, context) {
  const config = context.options ? context.options.compatConfig : context.compatConfig;
  const value = config && config[key];
  if (key === "MODE") {
    return value || 3;
  } else {
    return value;
  }
}
function isCompatEnabled(key, context) {
  const mode = getCompatValue("MODE", context);
  const value = getCompatValue(key, context);
  return mode === 3 ? value === true : value !== false;
}
function checkCompatEnabled(key, context, loc, ...args) {
  const enabled = isCompatEnabled(key, context);
  if (enabled) {
    warnDeprecation(key, context, loc, ...args);
  }
  return enabled;
}
function warnDeprecation(key, context, loc, ...args) {
  const val = getCompatValue(key, context);
  if (val === "suppress-warning") {
    return;
  }
  const { message, link } = deprecationData[key];
  const msg = `(deprecation ${key}) ${typeof message === "function" ? message(...args) : message}${link ? `
  Details: ${link}` : ``}`;
  const err = new SyntaxError(msg);
  err.code = key;
  if (loc)
    err.loc = loc;
  context.onWarn(err);
}
function baseParse(content, options = {}) {
  const context = createParserContext(content, options);
  const start = getCursor(context);
  return createRoot(parseChildren(context, 0, []), getSelection(context, start));
}
function createParserContext(content, rawOptions) {
  const options = extend({}, defaultParserOptions);
  let key;
  for (key in rawOptions) {
    options[key] = rawOptions[key] === void 0 ? defaultParserOptions[key] : rawOptions[key];
  }
  return {
    options,
    column: 1,
    line: 1,
    offset: 0,
    originalSource: content,
    source: content,
    inPre: false,
    inVPre: false,
    onWarn: options.onWarn
  };
}
function parseChildren(context, mode, ancestors) {
  const parent = last(ancestors);
  const ns = parent ? parent.ns : 0;
  const nodes = [];
  while (!isEnd(context, mode, ancestors)) {
    const s = context.source;
    let node = void 0;
    if (mode === 0 || mode === 1) {
      if (!context.inVPre && startsWith(s, context.options.delimiters[0])) {
        node = parseInterpolation(context, mode);
      } else if (mode === 0 && s[0] === "<") {
        if (s.length === 1) {
          emitError(context, 5, 1);
        } else if (s[1] === "!") {
          if (startsWith(s, "<!--")) {
            node = parseComment(context);
          } else if (startsWith(s, "<!DOCTYPE")) {
            node = parseBogusComment(context);
          } else if (startsWith(s, "<![CDATA[")) {
            if (ns !== 0) {
              node = parseCDATA(context, ancestors);
            } else {
              emitError(context, 1);
              node = parseBogusComment(context);
            }
          } else {
            emitError(context, 11);
            node = parseBogusComment(context);
          }
        } else if (s[1] === "/") {
          if (s.length === 2) {
            emitError(context, 5, 2);
          } else if (s[2] === ">") {
            emitError(context, 14, 2);
            advanceBy(context, 3);
            continue;
          } else if (/[a-z]/i.test(s[2])) {
            emitError(context, 23);
            parseTag(context, 1, parent);
            continue;
          } else {
            emitError(context, 12, 2);
            node = parseBogusComment(context);
          }
        } else if (/[a-z]/i.test(s[1])) {
          node = parseElement(context, ancestors);
          if (isCompatEnabled("COMPILER_NATIVE_TEMPLATE", context) && node && node.tag === "template" && !node.props.some((p) => p.type === 7 && isSpecialTemplateDirective(p.name))) {
            warnDeprecation("COMPILER_NATIVE_TEMPLATE", context, node.loc);
            node = node.children;
          }
        } else if (s[1] === "?") {
          emitError(context, 21, 1);
          node = parseBogusComment(context);
        } else {
          emitError(context, 12, 1);
        }
      }
    }
    if (!node) {
      node = parseText(context, mode);
    }
    if (isArray(node)) {
      for (let i = 0; i < node.length; i++) {
        pushNode(nodes, node[i]);
      }
    } else {
      pushNode(nodes, node);
    }
  }
  let removedWhitespace = false;
  if (mode !== 2 && mode !== 1) {
    const shouldCondense = context.options.whitespace !== "preserve";
    for (let i = 0; i < nodes.length; i++) {
      const node = nodes[i];
      if (!context.inPre && node.type === 2) {
        if (!/[^\t\r\n\f ]/.test(node.content)) {
          const prev = nodes[i - 1];
          const next = nodes[i + 1];
          if (!prev || !next || shouldCondense && (prev.type === 3 || next.type === 3 || prev.type === 1 && next.type === 1 && /[\r\n]/.test(node.content))) {
            removedWhitespace = true;
            nodes[i] = null;
          } else {
            node.content = " ";
          }
        } else if (shouldCondense) {
          node.content = node.content.replace(/[\t\r\n\f ]+/g, " ");
        }
      } else if (node.type === 3 && !context.options.comments) {
        removedWhitespace = true;
        nodes[i] = null;
      }
    }
    if (context.inPre && parent && context.options.isPreTag(parent.tag)) {
      const first = nodes[0];
      if (first && first.type === 2) {
        first.content = first.content.replace(/^\r?\n/, "");
      }
    }
  }
  return removedWhitespace ? nodes.filter(Boolean) : nodes;
}
function pushNode(nodes, node) {
  if (node.type === 2) {
    const prev = last(nodes);
    if (prev && prev.type === 2 && prev.loc.end.offset === node.loc.start.offset) {
      prev.content += node.content;
      prev.loc.end = node.loc.end;
      prev.loc.source += node.loc.source;
      return;
    }
  }
  nodes.push(node);
}
function parseCDATA(context, ancestors) {
  advanceBy(context, 9);
  const nodes = parseChildren(context, 3, ancestors);
  if (context.source.length === 0) {
    emitError(context, 6);
  } else {
    advanceBy(context, 3);
  }
  return nodes;
}
function parseComment(context) {
  const start = getCursor(context);
  let content;
  const match = /--(\!)?>/.exec(context.source);
  if (!match) {
    content = context.source.slice(4);
    advanceBy(context, context.source.length);
    emitError(context, 7);
  } else {
    if (match.index <= 3) {
      emitError(context, 0);
    }
    if (match[1]) {
      emitError(context, 10);
    }
    content = context.source.slice(4, match.index);
    const s = context.source.slice(0, match.index);
    let prevIndex = 1, nestedIndex = 0;
    while ((nestedIndex = s.indexOf("<!--", prevIndex)) !== -1) {
      advanceBy(context, nestedIndex - prevIndex + 1);
      if (nestedIndex + 4 < s.length) {
        emitError(context, 16);
      }
      prevIndex = nestedIndex + 1;
    }
    advanceBy(context, match.index + match[0].length - prevIndex + 1);
  }
  return {
    type: 3,
    content,
    loc: getSelection(context, start)
  };
}
function parseBogusComment(context) {
  const start = getCursor(context);
  const contentStart = context.source[1] === "?" ? 1 : 2;
  let content;
  const closeIndex = context.source.indexOf(">");
  if (closeIndex === -1) {
    content = context.source.slice(contentStart);
    advanceBy(context, context.source.length);
  } else {
    content = context.source.slice(contentStart, closeIndex);
    advanceBy(context, closeIndex + 1);
  }
  return {
    type: 3,
    content,
    loc: getSelection(context, start)
  };
}
function parseElement(context, ancestors) {
  const wasInPre = context.inPre;
  const wasInVPre = context.inVPre;
  const parent = last(ancestors);
  const element = parseTag(context, 0, parent);
  const isPreBoundary = context.inPre && !wasInPre;
  const isVPreBoundary = context.inVPre && !wasInVPre;
  if (element.isSelfClosing || context.options.isVoidTag(element.tag)) {
    if (isPreBoundary) {
      context.inPre = false;
    }
    if (isVPreBoundary) {
      context.inVPre = false;
    }
    return element;
  }
  ancestors.push(element);
  const mode = context.options.getTextMode(element, parent);
  const children = parseChildren(context, mode, ancestors);
  ancestors.pop();
  {
    const inlineTemplateProp = element.props.find((p) => p.type === 6 && p.name === "inline-template");
    if (inlineTemplateProp && checkCompatEnabled("COMPILER_INLINE_TEMPLATE", context, inlineTemplateProp.loc)) {
      const loc = getSelection(context, element.loc.end);
      inlineTemplateProp.value = {
        type: 2,
        content: loc.source,
        loc
      };
    }
  }
  element.children = children;
  if (startsWithEndTagOpen(context.source, element.tag)) {
    parseTag(context, 1, parent);
  } else {
    emitError(context, 24, 0, element.loc.start);
    if (context.source.length === 0 && element.tag.toLowerCase() === "script") {
      const first = children[0];
      if (first && startsWith(first.loc.source, "<!--")) {
        emitError(context, 8);
      }
    }
  }
  element.loc = getSelection(context, element.loc.start);
  if (isPreBoundary) {
    context.inPre = false;
  }
  if (isVPreBoundary) {
    context.inVPre = false;
  }
  return element;
}
function parseTag(context, type, parent) {
  const start = getCursor(context);
  const match = /^<\/?([a-z][^\t\r\n\f />]*)/i.exec(context.source);
  const tag = match[1];
  const ns = context.options.getNamespace(tag, parent);
  advanceBy(context, match[0].length);
  advanceSpaces(context);
  const cursor = getCursor(context);
  const currentSource = context.source;
  if (context.options.isPreTag(tag)) {
    context.inPre = true;
  }
  let props = parseAttributes(context, type);
  if (type === 0 && !context.inVPre && props.some((p) => p.type === 7 && p.name === "pre")) {
    context.inVPre = true;
    extend(context, cursor);
    context.source = currentSource;
    props = parseAttributes(context, type).filter((p) => p.name !== "v-pre");
  }
  let isSelfClosing = false;
  if (context.source.length === 0) {
    emitError(context, 9);
  } else {
    isSelfClosing = startsWith(context.source, "/>");
    if (type === 1 && isSelfClosing) {
      emitError(context, 4);
    }
    advanceBy(context, isSelfClosing ? 2 : 1);
  }
  if (type === 1) {
    return;
  }
  if (isCompatEnabled("COMPILER_V_IF_V_FOR_PRECEDENCE", context)) {
    let hasIf = false;
    let hasFor = false;
    for (let i = 0; i < props.length; i++) {
      const p = props[i];
      if (p.type === 7) {
        if (p.name === "if") {
          hasIf = true;
        } else if (p.name === "for") {
          hasFor = true;
        }
      }
      if (hasIf && hasFor) {
        warnDeprecation("COMPILER_V_IF_V_FOR_PRECEDENCE", context, getSelection(context, start));
        break;
      }
    }
  }
  let tagType = 0;
  if (!context.inVPre) {
    if (tag === "slot") {
      tagType = 2;
    } else if (tag === "template") {
      if (props.some((p) => p.type === 7 && isSpecialTemplateDirective(p.name))) {
        tagType = 3;
      }
    } else if (isComponent(tag, props, context)) {
      tagType = 1;
    }
  }
  return {
    type: 1,
    ns,
    tag,
    tagType,
    props,
    isSelfClosing,
    children: [],
    loc: getSelection(context, start),
    codegenNode: void 0
  };
}
function isComponent(tag, props, context) {
  const options = context.options;
  if (options.isCustomElement(tag)) {
    return false;
  }
  if (tag === "component" || /^[A-Z]/.test(tag) || isCoreComponent(tag) || options.isBuiltInComponent && options.isBuiltInComponent(tag) || options.isNativeTag && !options.isNativeTag(tag)) {
    return true;
  }
  for (let i = 0; i < props.length; i++) {
    const p = props[i];
    if (p.type === 6) {
      if (p.name === "is" && p.value) {
        if (p.value.content.startsWith("vue:")) {
          return true;
        } else if (checkCompatEnabled("COMPILER_IS_ON_ELEMENT", context, p.loc)) {
          return true;
        }
      }
    } else {
      if (p.name === "is") {
        return true;
      } else if (p.name === "bind" && isStaticArgOf(p.arg, "is") && true && checkCompatEnabled("COMPILER_IS_ON_ELEMENT", context, p.loc)) {
        return true;
      }
    }
  }
}
function parseAttributes(context, type) {
  const props = [];
  const attributeNames = new Set();
  while (context.source.length > 0 && !startsWith(context.source, ">") && !startsWith(context.source, "/>")) {
    if (startsWith(context.source, "/")) {
      emitError(context, 22);
      advanceBy(context, 1);
      advanceSpaces(context);
      continue;
    }
    if (type === 1) {
      emitError(context, 3);
    }
    const attr = parseAttribute(context, attributeNames);
    if (attr.type === 6 && attr.value && attr.name === "class") {
      attr.value.content = attr.value.content.replace(/\s+/g, " ").trim();
    }
    if (type === 0) {
      props.push(attr);
    }
    if (/^[^\t\r\n\f />]/.test(context.source)) {
      emitError(context, 15);
    }
    advanceSpaces(context);
  }
  return props;
}
function parseAttribute(context, nameSet) {
  const start = getCursor(context);
  const match = /^[^\t\r\n\f />][^\t\r\n\f />=]*/.exec(context.source);
  const name = match[0];
  if (nameSet.has(name)) {
    emitError(context, 2);
  }
  nameSet.add(name);
  if (name[0] === "=") {
    emitError(context, 19);
  }
  {
    const pattern = /["'<]/g;
    let m;
    while (m = pattern.exec(name)) {
      emitError(context, 17, m.index);
    }
  }
  advanceBy(context, name.length);
  let value = void 0;
  if (/^[\t\r\n\f ]*=/.test(context.source)) {
    advanceSpaces(context);
    advanceBy(context, 1);
    advanceSpaces(context);
    value = parseAttributeValue(context);
    if (!value) {
      emitError(context, 13);
    }
  }
  const loc = getSelection(context, start);
  if (!context.inVPre && /^(v-[A-Za-z0-9-]|:|\.|@|#)/.test(name)) {
    const match2 = /(?:^v-([a-z0-9-]+))?(?:(?::|^\.|^@|^#)(\[[^\]]+\]|[^\.]+))?(.+)?$/i.exec(name);
    let isPropShorthand = startsWith(name, ".");
    let dirName = match2[1] || (isPropShorthand || startsWith(name, ":") ? "bind" : startsWith(name, "@") ? "on" : "slot");
    let arg;
    if (match2[2]) {
      const isSlot = dirName === "slot";
      const startOffset = name.lastIndexOf(match2[2]);
      const loc2 = getSelection(context, getNewPosition(context, start, startOffset), getNewPosition(context, start, startOffset + match2[2].length + (isSlot && match2[3] || "").length));
      let content = match2[2];
      let isStatic = true;
      if (content.startsWith("[")) {
        isStatic = false;
        if (!content.endsWith("]")) {
          emitError(context, 27);
          content = content.slice(1);
        } else {
          content = content.slice(1, content.length - 1);
        }
      } else if (isSlot) {
        content += match2[3] || "";
      }
      arg = {
        type: 4,
        content,
        isStatic,
        constType: isStatic ? 3 : 0,
        loc: loc2
      };
    }
    if (value && value.isQuoted) {
      const valueLoc = value.loc;
      valueLoc.start.offset++;
      valueLoc.start.column++;
      valueLoc.end = advancePositionWithClone(valueLoc.start, value.content);
      valueLoc.source = valueLoc.source.slice(1, -1);
    }
    const modifiers = match2[3] ? match2[3].slice(1).split(".") : [];
    if (isPropShorthand)
      modifiers.push("prop");
    if (dirName === "bind" && arg) {
      if (modifiers.includes("sync") && checkCompatEnabled("COMPILER_V_BIND_SYNC", context, loc, arg.loc.source)) {
        dirName = "model";
        modifiers.splice(modifiers.indexOf("sync"), 1);
      }
      if (modifiers.includes("prop")) {
        checkCompatEnabled("COMPILER_V_BIND_PROP", context, loc);
      }
    }
    return {
      type: 7,
      name: dirName,
      exp: value && {
        type: 4,
        content: value.content,
        isStatic: false,
        constType: 0,
        loc: value.loc
      },
      arg,
      modifiers,
      loc
    };
  }
  if (!context.inVPre && startsWith(name, "v-")) {
    emitError(context, 26);
  }
  return {
    type: 6,
    name,
    value: value && {
      type: 2,
      content: value.content,
      loc: value.loc
    },
    loc
  };
}
function parseAttributeValue(context) {
  const start = getCursor(context);
  let content;
  const quote = context.source[0];
  const isQuoted = quote === `"` || quote === `'`;
  if (isQuoted) {
    advanceBy(context, 1);
    const endIndex = context.source.indexOf(quote);
    if (endIndex === -1) {
      content = parseTextData(context, context.source.length, 4);
    } else {
      content = parseTextData(context, endIndex, 4);
      advanceBy(context, 1);
    }
  } else {
    const match = /^[^\t\r\n\f >]+/.exec(context.source);
    if (!match) {
      return void 0;
    }
    const unexpectedChars = /["'<=`]/g;
    let m;
    while (m = unexpectedChars.exec(match[0])) {
      emitError(context, 18, m.index);
    }
    content = parseTextData(context, match[0].length, 4);
  }
  return { content, isQuoted, loc: getSelection(context, start) };
}
function parseInterpolation(context, mode) {
  const [open, close] = context.options.delimiters;
  const closeIndex = context.source.indexOf(close, open.length);
  if (closeIndex === -1) {
    emitError(context, 25);
    return void 0;
  }
  const start = getCursor(context);
  advanceBy(context, open.length);
  const innerStart = getCursor(context);
  const innerEnd = getCursor(context);
  const rawContentLength = closeIndex - open.length;
  const rawContent = context.source.slice(0, rawContentLength);
  const preTrimContent = parseTextData(context, rawContentLength, mode);
  const content = preTrimContent.trim();
  const startOffset = preTrimContent.indexOf(content);
  if (startOffset > 0) {
    advancePositionWithMutation(innerStart, rawContent, startOffset);
  }
  const endOffset = rawContentLength - (preTrimContent.length - content.length - startOffset);
  advancePositionWithMutation(innerEnd, rawContent, endOffset);
  advanceBy(context, close.length);
  return {
    type: 5,
    content: {
      type: 4,
      isStatic: false,
      constType: 0,
      content,
      loc: getSelection(context, innerStart, innerEnd)
    },
    loc: getSelection(context, start)
  };
}
function parseText(context, mode) {
  const endTokens = mode === 3 ? ["]]>"] : ["<", context.options.delimiters[0]];
  let endIndex = context.source.length;
  for (let i = 0; i < endTokens.length; i++) {
    const index = context.source.indexOf(endTokens[i], 1);
    if (index !== -1 && endIndex > index) {
      endIndex = index;
    }
  }
  const start = getCursor(context);
  const content = parseTextData(context, endIndex, mode);
  return {
    type: 2,
    content,
    loc: getSelection(context, start)
  };
}
function parseTextData(context, length, mode) {
  const rawText = context.source.slice(0, length);
  advanceBy(context, length);
  if (mode === 2 || mode === 3 || rawText.indexOf("&") === -1) {
    return rawText;
  } else {
    return context.options.decodeEntities(rawText, mode === 4);
  }
}
function getCursor(context) {
  const { column, line, offset } = context;
  return { column, line, offset };
}
function getSelection(context, start, end) {
  end = end || getCursor(context);
  return {
    start,
    end,
    source: context.originalSource.slice(start.offset, end.offset)
  };
}
function last(xs) {
  return xs[xs.length - 1];
}
function startsWith(source, searchString) {
  return source.startsWith(searchString);
}
function advanceBy(context, numberOfCharacters) {
  const { source } = context;
  advancePositionWithMutation(context, source, numberOfCharacters);
  context.source = source.slice(numberOfCharacters);
}
function advanceSpaces(context) {
  const match = /^[\t\r\n\f ]+/.exec(context.source);
  if (match) {
    advanceBy(context, match[0].length);
  }
}
function getNewPosition(context, start, numberOfCharacters) {
  return advancePositionWithClone(start, context.originalSource.slice(start.offset, numberOfCharacters), numberOfCharacters);
}
function emitError(context, code, offset, loc = getCursor(context)) {
  if (offset) {
    loc.offset += offset;
    loc.column += offset;
  }
  context.options.onError(createCompilerError(code, {
    start: loc,
    end: loc,
    source: ""
  }));
}
function isEnd(context, mode, ancestors) {
  const s = context.source;
  switch (mode) {
    case 0:
      if (startsWith(s, "</")) {
        for (let i = ancestors.length - 1; i >= 0; --i) {
          if (startsWithEndTagOpen(s, ancestors[i].tag)) {
            return true;
          }
        }
      }
      break;
    case 1:
    case 2: {
      const parent = last(ancestors);
      if (parent && startsWithEndTagOpen(s, parent.tag)) {
        return true;
      }
      break;
    }
    case 3:
      if (startsWith(s, "]]>")) {
        return true;
      }
      break;
  }
  return !s;
}
function startsWithEndTagOpen(source, tag) {
  return startsWith(source, "</") && source.slice(2, 2 + tag.length).toLowerCase() === tag.toLowerCase() && /[\t\r\n\f />]/.test(source[2 + tag.length] || ">");
}
function hoistStatic(root, context) {
  walk(root, context, isSingleElementRoot(root, root.children[0]));
}
function isSingleElementRoot(root, child) {
  const { children } = root;
  return children.length === 1 && child.type === 1 && !isSlotOutlet(child);
}
function walk(node, context, doNotHoistNode = false) {
  const { children } = node;
  const originalCount = children.length;
  let hoistedCount = 0;
  for (let i = 0; i < children.length; i++) {
    const child = children[i];
    if (child.type === 1 && child.tagType === 0) {
      const constantType = doNotHoistNode ? 0 : getConstantType(child, context);
      if (constantType > 0) {
        if (constantType >= 2) {
          child.codegenNode.patchFlag = -1 + (true ? ` /* HOISTED */` : ``);
          child.codegenNode = context.hoist(child.codegenNode);
          hoistedCount++;
          continue;
        }
      } else {
        const codegenNode = child.codegenNode;
        if (codegenNode.type === 13) {
          const flag = getPatchFlag(codegenNode);
          if ((!flag || flag === 512 || flag === 1) && getGeneratedPropsConstantType(child, context) >= 2) {
            const props = getNodeProps(child);
            if (props) {
              codegenNode.props = context.hoist(props);
            }
          }
          if (codegenNode.dynamicProps) {
            codegenNode.dynamicProps = context.hoist(codegenNode.dynamicProps);
          }
        }
      }
    } else if (child.type === 12 && getConstantType(child.content, context) >= 2) {
      child.codegenNode = context.hoist(child.codegenNode);
      hoistedCount++;
    }
    if (child.type === 1) {
      const isComponent2 = child.tagType === 1;
      if (isComponent2) {
        context.scopes.vSlot++;
      }
      walk(child, context);
      if (isComponent2) {
        context.scopes.vSlot--;
      }
    } else if (child.type === 11) {
      walk(child, context, child.children.length === 1);
    } else if (child.type === 9) {
      for (let i2 = 0; i2 < child.branches.length; i2++) {
        walk(child.branches[i2], context, child.branches[i2].children.length === 1);
      }
    }
  }
  if (hoistedCount && context.transformHoist) {
    context.transformHoist(children, context, node);
  }
  if (hoistedCount && hoistedCount === originalCount && node.type === 1 && node.tagType === 0 && node.codegenNode && node.codegenNode.type === 13 && isArray(node.codegenNode.children)) {
    node.codegenNode.children = context.hoist(createArrayExpression(node.codegenNode.children));
  }
}
function getConstantType(node, context) {
  const { constantCache } = context;
  switch (node.type) {
    case 1:
      if (node.tagType !== 0) {
        return 0;
      }
      const cached = constantCache.get(node);
      if (cached !== void 0) {
        return cached;
      }
      const codegenNode = node.codegenNode;
      if (codegenNode.type !== 13) {
        return 0;
      }
      if (codegenNode.isBlock && node.tag !== "svg" && node.tag !== "foreignObject") {
        return 0;
      }
      const flag = getPatchFlag(codegenNode);
      if (!flag) {
        let returnType2 = 3;
        const generatedPropsType = getGeneratedPropsConstantType(node, context);
        if (generatedPropsType === 0) {
          constantCache.set(node, 0);
          return 0;
        }
        if (generatedPropsType < returnType2) {
          returnType2 = generatedPropsType;
        }
        for (let i = 0; i < node.children.length; i++) {
          const childType = getConstantType(node.children[i], context);
          if (childType === 0) {
            constantCache.set(node, 0);
            return 0;
          }
          if (childType < returnType2) {
            returnType2 = childType;
          }
        }
        if (returnType2 > 1) {
          for (let i = 0; i < node.props.length; i++) {
            const p = node.props[i];
            if (p.type === 7 && p.name === "bind" && p.exp) {
              const expType = getConstantType(p.exp, context);
              if (expType === 0) {
                constantCache.set(node, 0);
                return 0;
              }
              if (expType < returnType2) {
                returnType2 = expType;
              }
            }
          }
        }
        if (codegenNode.isBlock) {
          context.removeHelper(OPEN_BLOCK);
          context.removeHelper(getVNodeBlockHelper(context.inSSR, codegenNode.isComponent));
          codegenNode.isBlock = false;
          context.helper(getVNodeHelper(context.inSSR, codegenNode.isComponent));
        }
        constantCache.set(node, returnType2);
        return returnType2;
      } else {
        constantCache.set(node, 0);
        return 0;
      }
    case 2:
    case 3:
      return 3;
    case 9:
    case 11:
    case 10:
      return 0;
    case 5:
    case 12:
      return getConstantType(node.content, context);
    case 4:
      return node.constType;
    case 8:
      let returnType = 3;
      for (let i = 0; i < node.children.length; i++) {
        const child = node.children[i];
        if (isString(child) || isSymbol(child)) {
          continue;
        }
        const childType = getConstantType(child, context);
        if (childType === 0) {
          return 0;
        } else if (childType < returnType) {
          returnType = childType;
        }
      }
      return returnType;
    default:
      if (true)
        ;
      return 0;
  }
}
function getConstantTypeOfHelperCall(value, context) {
  if (value.type === 14 && !isString(value.callee) && allowHoistedHelperSet.has(value.callee)) {
    const arg = value.arguments[0];
    if (arg.type === 4) {
      return getConstantType(arg, context);
    } else if (arg.type === 14) {
      return getConstantTypeOfHelperCall(arg, context);
    }
  }
  return 0;
}
function getGeneratedPropsConstantType(node, context) {
  let returnType = 3;
  const props = getNodeProps(node);
  if (props && props.type === 15) {
    const { properties } = props;
    for (let i = 0; i < properties.length; i++) {
      const { key, value } = properties[i];
      const keyType = getConstantType(key, context);
      if (keyType === 0) {
        return keyType;
      }
      if (keyType < returnType) {
        returnType = keyType;
      }
      let valueType;
      if (value.type === 4) {
        valueType = getConstantType(value, context);
      } else if (value.type === 14) {
        valueType = getConstantTypeOfHelperCall(value, context);
      } else {
        valueType = 0;
      }
      if (valueType === 0) {
        return valueType;
      }
      if (valueType < returnType) {
        returnType = valueType;
      }
    }
  }
  return returnType;
}
function getNodeProps(node) {
  const codegenNode = node.codegenNode;
  if (codegenNode.type === 13) {
    return codegenNode.props;
  }
}
function getPatchFlag(node) {
  const flag = node.patchFlag;
  return flag ? parseInt(flag, 10) : void 0;
}
function createTransformContext(root, { filename = "", prefixIdentifiers = false, hoistStatic: hoistStatic2 = false, cacheHandlers = false, nodeTransforms = [], directiveTransforms = {}, transformHoist = null, isBuiltInComponent = NOOP, isCustomElement = NOOP, expressionPlugins = [], scopeId = null, slotted = true, ssr = false, inSSR = false, ssrCssVars = ``, bindingMetadata = EMPTY_OBJ, inline = false, isTS = false, onError = defaultOnError, onWarn = defaultOnWarn, compatConfig }) {
  const nameMatch = filename.replace(/\?.*$/, "").match(/([^/\\]+)\.\w+$/);
  const context = {
    selfName: nameMatch && capitalize(camelize(nameMatch[1])),
    prefixIdentifiers,
    hoistStatic: hoistStatic2,
    cacheHandlers,
    nodeTransforms,
    directiveTransforms,
    transformHoist,
    isBuiltInComponent,
    isCustomElement,
    expressionPlugins,
    scopeId,
    slotted,
    ssr,
    inSSR,
    ssrCssVars,
    bindingMetadata,
    inline,
    isTS,
    onError,
    onWarn,
    compatConfig,
    root,
    helpers: new Map(),
    components: new Set(),
    directives: new Set(),
    hoists: [],
    imports: [],
    constantCache: new Map(),
    temps: 0,
    cached: 0,
    identifiers: Object.create(null),
    scopes: {
      vFor: 0,
      vSlot: 0,
      vPre: 0,
      vOnce: 0
    },
    parent: null,
    currentNode: root,
    childIndex: 0,
    inVOnce: false,
    helper(name) {
      const count = context.helpers.get(name) || 0;
      context.helpers.set(name, count + 1);
      return name;
    },
    removeHelper(name) {
      const count = context.helpers.get(name);
      if (count) {
        const currentCount = count - 1;
        if (!currentCount) {
          context.helpers.delete(name);
        } else {
          context.helpers.set(name, currentCount);
        }
      }
    },
    helperString(name) {
      return `_${helperNameMap[context.helper(name)]}`;
    },
    replaceNode(node) {
      if (true) {
        if (!context.currentNode) {
          throw new Error(`Node being replaced is already removed.`);
        }
        if (!context.parent) {
          throw new Error(`Cannot replace root node.`);
        }
      }
      context.parent.children[context.childIndex] = context.currentNode = node;
    },
    removeNode(node) {
      if (!context.parent) {
        throw new Error(`Cannot remove root node.`);
      }
      const list = context.parent.children;
      const removalIndex = node ? list.indexOf(node) : context.currentNode ? context.childIndex : -1;
      if (removalIndex < 0) {
        throw new Error(`node being removed is not a child of current parent`);
      }
      if (!node || node === context.currentNode) {
        context.currentNode = null;
        context.onNodeRemoved();
      } else {
        if (context.childIndex > removalIndex) {
          context.childIndex--;
          context.onNodeRemoved();
        }
      }
      context.parent.children.splice(removalIndex, 1);
    },
    onNodeRemoved: () => {
    },
    addIdentifiers(exp) {
    },
    removeIdentifiers(exp) {
    },
    hoist(exp) {
      if (isString(exp))
        exp = createSimpleExpression(exp);
      context.hoists.push(exp);
      const identifier = createSimpleExpression(`_hoisted_${context.hoists.length}`, false, exp.loc, 2);
      identifier.hoisted = exp;
      return identifier;
    },
    cache(exp, isVNode = false) {
      return createCacheExpression(context.cached++, exp, isVNode);
    }
  };
  {
    context.filters = new Set();
  }
  return context;
}
function transform(root, options) {
  const context = createTransformContext(root, options);
  traverseNode(root, context);
  if (options.hoistStatic) {
    hoistStatic(root, context);
  }
  if (!options.ssr) {
    createRootCodegen(root, context);
  }
  root.helpers = [...context.helpers.keys()];
  root.components = [...context.components];
  root.directives = [...context.directives];
  root.imports = context.imports;
  root.hoists = context.hoists;
  root.temps = context.temps;
  root.cached = context.cached;
  {
    root.filters = [...context.filters];
  }
}
function createRootCodegen(root, context) {
  const { helper } = context;
  const { children } = root;
  if (children.length === 1) {
    const child = children[0];
    if (isSingleElementRoot(root, child) && child.codegenNode) {
      const codegenNode = child.codegenNode;
      if (codegenNode.type === 13) {
        makeBlock(codegenNode, context);
      }
      root.codegenNode = codegenNode;
    } else {
      root.codegenNode = child;
    }
  } else if (children.length > 1) {
    let patchFlag = 64;
    let patchFlagText = PatchFlagNames[64];
    if (children.filter((c) => c.type !== 3).length === 1) {
      patchFlag |= 2048;
      patchFlagText += `, ${PatchFlagNames[2048]}`;
    }
    root.codegenNode = createVNodeCall(context, helper(FRAGMENT), void 0, root.children, patchFlag + (true ? ` /* ${patchFlagText} */` : ``), void 0, void 0, true, void 0, false);
  } else
    ;
}
function traverseChildren(parent, context) {
  let i = 0;
  const nodeRemoved = () => {
    i--;
  };
  for (; i < parent.children.length; i++) {
    const child = parent.children[i];
    if (isString(child))
      continue;
    context.parent = parent;
    context.childIndex = i;
    context.onNodeRemoved = nodeRemoved;
    traverseNode(child, context);
  }
}
function traverseNode(node, context) {
  context.currentNode = node;
  const { nodeTransforms } = context;
  const exitFns = [];
  for (let i2 = 0; i2 < nodeTransforms.length; i2++) {
    const onExit = nodeTransforms[i2](node, context);
    if (onExit) {
      if (isArray(onExit)) {
        exitFns.push(...onExit);
      } else {
        exitFns.push(onExit);
      }
    }
    if (!context.currentNode) {
      return;
    } else {
      node = context.currentNode;
    }
  }
  switch (node.type) {
    case 3:
      if (!context.ssr) {
        context.helper(CREATE_COMMENT);
      }
      break;
    case 5:
      if (!context.ssr) {
        context.helper(TO_DISPLAY_STRING);
      }
      break;
    case 9:
      for (let i2 = 0; i2 < node.branches.length; i2++) {
        traverseNode(node.branches[i2], context);
      }
      break;
    case 10:
    case 11:
    case 1:
    case 0:
      traverseChildren(node, context);
      break;
  }
  context.currentNode = node;
  let i = exitFns.length;
  while (i--) {
    exitFns[i]();
  }
}
function createStructuralDirectiveTransform(name, fn) {
  const matches = isString(name) ? (n) => n === name : (n) => name.test(n);
  return (node, context) => {
    if (node.type === 1) {
      const { props } = node;
      if (node.tagType === 3 && props.some(isVSlot)) {
        return;
      }
      const exitFns = [];
      for (let i = 0; i < props.length; i++) {
        const prop = props[i];
        if (prop.type === 7 && matches(prop.name)) {
          props.splice(i, 1);
          i--;
          const onExit = fn(node, prop, context);
          if (onExit)
            exitFns.push(onExit);
        }
      }
      return exitFns;
    }
  };
}
function createCodegenContext(ast, { mode = "function", prefixIdentifiers = mode === "module", sourceMap = false, filename = `template.vue.html`, scopeId = null, optimizeImports = false, runtimeGlobalName = `Vue`, runtimeModuleName = `vue`, ssrRuntimeModuleName = "vue/server-renderer", ssr = false, isTS = false, inSSR = false }) {
  const context = {
    mode,
    prefixIdentifiers,
    sourceMap,
    filename,
    scopeId,
    optimizeImports,
    runtimeGlobalName,
    runtimeModuleName,
    ssrRuntimeModuleName,
    ssr,
    isTS,
    inSSR,
    source: ast.loc.source,
    code: ``,
    column: 1,
    line: 1,
    offset: 0,
    indentLevel: 0,
    pure: false,
    map: void 0,
    helper(key) {
      return `_${helperNameMap[key]}`;
    },
    push(code, node) {
      context.code += code;
    },
    indent() {
      newline(++context.indentLevel);
    },
    deindent(withoutNewLine = false) {
      if (withoutNewLine) {
        --context.indentLevel;
      } else {
        newline(--context.indentLevel);
      }
    },
    newline() {
      newline(context.indentLevel);
    }
  };
  function newline(n) {
    context.push("\n" + `  `.repeat(n));
  }
  return context;
}
function generate(ast, options = {}) {
  const context = createCodegenContext(ast, options);
  if (options.onContextCreated)
    options.onContextCreated(context);
  const { mode, push, prefixIdentifiers, indent, deindent, newline, scopeId, ssr } = context;
  const hasHelpers = ast.helpers.length > 0;
  const useWithBlock = !prefixIdentifiers && mode !== "module";
  const preambleContext = context;
  {
    genFunctionPreamble(ast, preambleContext);
  }
  const functionName = ssr ? `ssrRender` : `render`;
  const args = ssr ? ["_ctx", "_push", "_parent", "_attrs"] : ["_ctx", "_cache"];
  const signature = args.join(", ");
  {
    push(`function ${functionName}(${signature}) {`);
  }
  indent();
  if (useWithBlock) {
    push(`with (_ctx) {`);
    indent();
    if (hasHelpers) {
      push(`const { ${ast.helpers.map((s) => `${helperNameMap[s]}: _${helperNameMap[s]}`).join(", ")} } = _Vue`);
      push(`
`);
      newline();
    }
  }
  if (ast.components.length) {
    genAssets(ast.components, "component", context);
    if (ast.directives.length || ast.temps > 0) {
      newline();
    }
  }
  if (ast.directives.length) {
    genAssets(ast.directives, "directive", context);
    if (ast.temps > 0) {
      newline();
    }
  }
  if (ast.filters && ast.filters.length) {
    newline();
    genAssets(ast.filters, "filter", context);
    newline();
  }
  if (ast.temps > 0) {
    push(`let `);
    for (let i = 0; i < ast.temps; i++) {
      push(`${i > 0 ? `, ` : ``}_temp${i}`);
    }
  }
  if (ast.components.length || ast.directives.length || ast.temps) {
    push(`
`);
    newline();
  }
  if (!ssr) {
    push(`return `);
  }
  if (ast.codegenNode) {
    genNode(ast.codegenNode, context);
  } else {
    push(`null`);
  }
  if (useWithBlock) {
    deindent();
    push(`}`);
  }
  deindent();
  push(`}`);
  return {
    ast,
    code: context.code,
    preamble: ``,
    map: context.map ? context.map.toJSON() : void 0
  };
}
function genFunctionPreamble(ast, context) {
  const { ssr, prefixIdentifiers, push, newline, runtimeModuleName, runtimeGlobalName, ssrRuntimeModuleName } = context;
  const VueBinding = runtimeGlobalName;
  const aliasHelper = (s) => `${helperNameMap[s]}: _${helperNameMap[s]}`;
  if (ast.helpers.length > 0) {
    {
      push(`const _Vue = ${VueBinding}
`);
      if (ast.hoists.length) {
        const staticHelpers = [
          CREATE_VNODE,
          CREATE_ELEMENT_VNODE,
          CREATE_COMMENT,
          CREATE_TEXT,
          CREATE_STATIC
        ].filter((helper) => ast.helpers.includes(helper)).map(aliasHelper).join(", ");
        push(`const { ${staticHelpers} } = _Vue
`);
      }
    }
  }
  genHoists(ast.hoists, context);
  newline();
  push(`return `);
}
function genAssets(assets, type, { helper, push, newline, isTS }) {
  const resolver = helper(type === "filter" ? RESOLVE_FILTER : type === "component" ? RESOLVE_COMPONENT : RESOLVE_DIRECTIVE);
  for (let i = 0; i < assets.length; i++) {
    let id = assets[i];
    const maybeSelfReference = id.endsWith("__self");
    if (maybeSelfReference) {
      id = id.slice(0, -6);
    }
    push(`const ${toValidAssetId(id, type)} = ${resolver}(${JSON.stringify(id)}${maybeSelfReference ? `, true` : ``})${isTS ? `!` : ``}`);
    if (i < assets.length - 1) {
      newline();
    }
  }
}
function genHoists(hoists, context) {
  if (!hoists.length) {
    return;
  }
  context.pure = true;
  const { push, newline, helper, scopeId, mode } = context;
  newline();
  for (let i = 0; i < hoists.length; i++) {
    const exp = hoists[i];
    if (exp) {
      push(`const _hoisted_${i + 1} = ${``}`);
      genNode(exp, context);
      newline();
    }
  }
  context.pure = false;
}
function isText$1(n) {
  return isString(n) || n.type === 4 || n.type === 2 || n.type === 5 || n.type === 8;
}
function genNodeListAsArray(nodes, context) {
  const multilines = nodes.length > 3 || nodes.some((n) => isArray(n) || !isText$1(n));
  context.push(`[`);
  multilines && context.indent();
  genNodeList(nodes, context, multilines);
  multilines && context.deindent();
  context.push(`]`);
}
function genNodeList(nodes, context, multilines = false, comma = true) {
  const { push, newline } = context;
  for (let i = 0; i < nodes.length; i++) {
    const node = nodes[i];
    if (isString(node)) {
      push(node);
    } else if (isArray(node)) {
      genNodeListAsArray(node, context);
    } else {
      genNode(node, context);
    }
    if (i < nodes.length - 1) {
      if (multilines) {
        comma && push(",");
        newline();
      } else {
        comma && push(", ");
      }
    }
  }
}
function genNode(node, context) {
  if (isString(node)) {
    context.push(node);
    return;
  }
  if (isSymbol(node)) {
    context.push(context.helper(node));
    return;
  }
  switch (node.type) {
    case 1:
    case 9:
    case 11:
      assert(node.codegenNode != null, `Codegen node is missing for element/if/for node. Apply appropriate transforms first.`);
      genNode(node.codegenNode, context);
      break;
    case 2:
      genText(node, context);
      break;
    case 4:
      genExpression(node, context);
      break;
    case 5:
      genInterpolation(node, context);
      break;
    case 12:
      genNode(node.codegenNode, context);
      break;
    case 8:
      genCompoundExpression(node, context);
      break;
    case 3:
      genComment(node, context);
      break;
    case 13:
      genVNodeCall(node, context);
      break;
    case 14:
      genCallExpression(node, context);
      break;
    case 15:
      genObjectExpression(node, context);
      break;
    case 17:
      genArrayExpression(node, context);
      break;
    case 18:
      genFunctionExpression(node, context);
      break;
    case 19:
      genConditionalExpression(node, context);
      break;
    case 20:
      genCacheExpression(node, context);
      break;
    case 21:
      genNodeList(node.body, context, true, false);
      break;
    case 22:
      break;
    case 23:
      break;
    case 24:
      break;
    case 25:
      break;
    case 26:
      break;
    case 10:
      break;
    default:
      if (true) {
        assert(false, `unhandled codegen node type: ${node.type}`);
        const exhaustiveCheck = node;
        return exhaustiveCheck;
      }
  }
}
function genText(node, context) {
  context.push(JSON.stringify(node.content), node);
}
function genExpression(node, context) {
  const { content, isStatic } = node;
  context.push(isStatic ? JSON.stringify(content) : content, node);
}
function genInterpolation(node, context) {
  const { push, helper, pure } = context;
  if (pure)
    push(PURE_ANNOTATION);
  push(`${helper(TO_DISPLAY_STRING)}(`);
  genNode(node.content, context);
  push(`)`);
}
function genCompoundExpression(node, context) {
  for (let i = 0; i < node.children.length; i++) {
    const child = node.children[i];
    if (isString(child)) {
      context.push(child);
    } else {
      genNode(child, context);
    }
  }
}
function genExpressionAsPropertyKey(node, context) {
  const { push } = context;
  if (node.type === 8) {
    push(`[`);
    genCompoundExpression(node, context);
    push(`]`);
  } else if (node.isStatic) {
    const text = isSimpleIdentifier(node.content) ? node.content : JSON.stringify(node.content);
    push(text, node);
  } else {
    push(`[${node.content}]`, node);
  }
}
function genComment(node, context) {
  const { push, helper, pure } = context;
  if (pure) {
    push(PURE_ANNOTATION);
  }
  push(`${helper(CREATE_COMMENT)}(${JSON.stringify(node.content)})`, node);
}
function genVNodeCall(node, context) {
  const { push, helper, pure } = context;
  const { tag, props, children, patchFlag, dynamicProps, directives, isBlock, disableTracking, isComponent: isComponent2 } = node;
  if (directives) {
    push(helper(WITH_DIRECTIVES) + `(`);
  }
  if (isBlock) {
    push(`(${helper(OPEN_BLOCK)}(${disableTracking ? `true` : ``}), `);
  }
  if (pure) {
    push(PURE_ANNOTATION);
  }
  const callHelper = isBlock ? getVNodeBlockHelper(context.inSSR, isComponent2) : getVNodeHelper(context.inSSR, isComponent2);
  push(helper(callHelper) + `(`, node);
  genNodeList(genNullableArgs([tag, props, children, patchFlag, dynamicProps]), context);
  push(`)`);
  if (isBlock) {
    push(`)`);
  }
  if (directives) {
    push(`, `);
    genNode(directives, context);
    push(`)`);
  }
}
function genNullableArgs(args) {
  let i = args.length;
  while (i--) {
    if (args[i] != null)
      break;
  }
  return args.slice(0, i + 1).map((arg) => arg || `null`);
}
function genCallExpression(node, context) {
  const { push, helper, pure } = context;
  const callee = isString(node.callee) ? node.callee : helper(node.callee);
  if (pure) {
    push(PURE_ANNOTATION);
  }
  push(callee + `(`, node);
  genNodeList(node.arguments, context);
  push(`)`);
}
function genObjectExpression(node, context) {
  const { push, indent, deindent, newline } = context;
  const { properties } = node;
  if (!properties.length) {
    push(`{}`, node);
    return;
  }
  const multilines = properties.length > 1 || properties.some((p) => p.value.type !== 4);
  push(multilines ? `{` : `{ `);
  multilines && indent();
  for (let i = 0; i < properties.length; i++) {
    const { key, value } = properties[i];
    genExpressionAsPropertyKey(key, context);
    push(`: `);
    genNode(value, context);
    if (i < properties.length - 1) {
      push(`,`);
      newline();
    }
  }
  multilines && deindent();
  push(multilines ? `}` : ` }`);
}
function genArrayExpression(node, context) {
  genNodeListAsArray(node.elements, context);
}
function genFunctionExpression(node, context) {
  const { push, indent, deindent } = context;
  const { params, returns, body, newline, isSlot } = node;
  if (isSlot) {
    push(`_${helperNameMap[WITH_CTX]}(`);
  }
  push(`(`, node);
  if (isArray(params)) {
    genNodeList(params, context);
  } else if (params) {
    genNode(params, context);
  }
  push(`) => `);
  if (newline || body) {
    push(`{`);
    indent();
  }
  if (returns) {
    if (newline) {
      push(`return `);
    }
    if (isArray(returns)) {
      genNodeListAsArray(returns, context);
    } else {
      genNode(returns, context);
    }
  } else if (body) {
    genNode(body, context);
  }
  if (newline || body) {
    deindent();
    push(`}`);
  }
  if (isSlot) {
    if (node.isNonScopedSlot) {
      push(`, undefined, true`);
    }
    push(`)`);
  }
}
function genConditionalExpression(node, context) {
  const { test, consequent, alternate, newline: needNewline } = node;
  const { push, indent, deindent, newline } = context;
  if (test.type === 4) {
    const needsParens = !isSimpleIdentifier(test.content);
    needsParens && push(`(`);
    genExpression(test, context);
    needsParens && push(`)`);
  } else {
    push(`(`);
    genNode(test, context);
    push(`)`);
  }
  needNewline && indent();
  context.indentLevel++;
  needNewline || push(` `);
  push(`? `);
  genNode(consequent, context);
  context.indentLevel--;
  needNewline && newline();
  needNewline || push(` `);
  push(`: `);
  const isNested = alternate.type === 19;
  if (!isNested) {
    context.indentLevel++;
  }
  genNode(alternate, context);
  if (!isNested) {
    context.indentLevel--;
  }
  needNewline && deindent(true);
}
function genCacheExpression(node, context) {
  const { push, helper, indent, deindent, newline } = context;
  push(`_cache[${node.index}] || (`);
  if (node.isVNode) {
    indent();
    push(`${helper(SET_BLOCK_TRACKING)}(-1),`);
    newline();
  }
  push(`_cache[${node.index}] = `);
  genNode(node.value, context);
  if (node.isVNode) {
    push(`,`);
    newline();
    push(`${helper(SET_BLOCK_TRACKING)}(1),`);
    newline();
    push(`_cache[${node.index}]`);
    deindent();
  }
  push(`)`);
}
function walkIdentifiers(root, onIdentifier, includeAll = false, parentStack = [], knownIds = Object.create(null)) {
  {
    return;
  }
}
function isReferencedIdentifier(id, parent, parentStack) {
  {
    return false;
  }
}
function isInDestructureAssignment(parent, parentStack) {
  if (parent && (parent.type === "ObjectProperty" || parent.type === "ArrayPattern")) {
    let i = parentStack.length;
    while (i--) {
      const p = parentStack[i];
      if (p.type === "AssignmentExpression") {
        return true;
      } else if (p.type !== "ObjectProperty" && !p.type.endsWith("Pattern")) {
        break;
      }
    }
  }
  return false;
}
function walkFunctionParams(node, onIdent) {
  for (const p of node.params) {
    for (const id of extractIdentifiers(p)) {
      onIdent(id);
    }
  }
}
function walkBlockDeclarations(block, onIdent) {
  for (const stmt of block.body) {
    if (stmt.type === "VariableDeclaration") {
      if (stmt.declare)
        continue;
      for (const decl of stmt.declarations) {
        for (const id of extractIdentifiers(decl.id)) {
          onIdent(id);
        }
      }
    } else if (stmt.type === "FunctionDeclaration" || stmt.type === "ClassDeclaration") {
      if (stmt.declare || !stmt.id)
        continue;
      onIdent(stmt.id);
    }
  }
}
function extractIdentifiers(param, nodes = []) {
  switch (param.type) {
    case "Identifier":
      nodes.push(param);
      break;
    case "MemberExpression":
      let object = param;
      while (object.type === "MemberExpression") {
        object = object.object;
      }
      nodes.push(object);
      break;
    case "ObjectPattern":
      for (const prop of param.properties) {
        if (prop.type === "RestElement") {
          extractIdentifiers(prop.argument, nodes);
        } else {
          extractIdentifiers(prop.value, nodes);
        }
      }
      break;
    case "ArrayPattern":
      param.elements.forEach((element) => {
        if (element)
          extractIdentifiers(element, nodes);
      });
      break;
    case "RestElement":
      extractIdentifiers(param.argument, nodes);
      break;
    case "AssignmentPattern":
      extractIdentifiers(param.left, nodes);
      break;
  }
  return nodes;
}
function validateBrowserExpression(node, context, asParams = false, asRawStatements = false) {
  const exp = node.content;
  if (!exp.trim()) {
    return;
  }
  try {
    new Function(asRawStatements ? ` ${exp} ` : `return ${asParams ? `(${exp}) => {}` : `(${exp})`}`);
  } catch (e) {
    let message = e.message;
    const keywordMatch = exp.replace(stripStringRE, "").match(prohibitedKeywordRE);
    if (keywordMatch) {
      message = `avoid using JavaScript keyword as property name: "${keywordMatch[0]}"`;
    }
    context.onError(createCompilerError(44, node.loc, void 0, message));
  }
}
function processExpression(node, context, asParams = false, asRawStatements = false, localVars = Object.create(context.identifiers)) {
  {
    if (true) {
      validateBrowserExpression(node, context, asParams, asRawStatements);
    }
    return node;
  }
}
function processIf(node, dir, context, processCodegen) {
  if (dir.name !== "else" && (!dir.exp || !dir.exp.content.trim())) {
    const loc = dir.exp ? dir.exp.loc : node.loc;
    context.onError(createCompilerError(28, dir.loc));
    dir.exp = createSimpleExpression(`true`, false, loc);
  }
  if (dir.exp) {
    validateBrowserExpression(dir.exp, context);
  }
  if (dir.name === "if") {
    const branch = createIfBranch(node, dir);
    const ifNode = {
      type: 9,
      loc: node.loc,
      branches: [branch]
    };
    context.replaceNode(ifNode);
    if (processCodegen) {
      return processCodegen(ifNode, branch, true);
    }
  } else {
    const siblings = context.parent.children;
    const comments = [];
    let i = siblings.indexOf(node);
    while (i-- >= -1) {
      const sibling = siblings[i];
      if (sibling && sibling.type === 3) {
        context.removeNode(sibling);
        comments.unshift(sibling);
        continue;
      }
      if (sibling && sibling.type === 2 && !sibling.content.trim().length) {
        context.removeNode(sibling);
        continue;
      }
      if (sibling && sibling.type === 9) {
        if (dir.name === "else-if" && sibling.branches[sibling.branches.length - 1].condition === void 0) {
          context.onError(createCompilerError(30, node.loc));
        }
        context.removeNode();
        const branch = createIfBranch(node, dir);
        if (comments.length && !(context.parent && context.parent.type === 1 && isBuiltInType(context.parent.tag, "transition"))) {
          branch.children = [...comments, ...branch.children];
        }
        if (true) {
          const key = branch.userKey;
          if (key) {
            sibling.branches.forEach(({ userKey }) => {
              if (isSameKey(userKey, key)) {
                context.onError(createCompilerError(29, branch.userKey.loc));
              }
            });
          }
        }
        sibling.branches.push(branch);
        const onExit = processCodegen && processCodegen(sibling, branch, false);
        traverseNode(branch, context);
        if (onExit)
          onExit();
        context.currentNode = null;
      } else {
        context.onError(createCompilerError(30, node.loc));
      }
      break;
    }
  }
}
function createIfBranch(node, dir) {
  return {
    type: 10,
    loc: node.loc,
    condition: dir.name === "else" ? void 0 : dir.exp,
    children: node.tagType === 3 && !findDir(node, "for") ? node.children : [node],
    userKey: findProp(node, `key`)
  };
}
function createCodegenNodeForBranch(branch, keyIndex, context) {
  if (branch.condition) {
    return createConditionalExpression(branch.condition, createChildrenCodegenNode(branch, keyIndex, context), createCallExpression(context.helper(CREATE_COMMENT), [
      true ? '"v-if"' : '""',
      "true"
    ]));
  } else {
    return createChildrenCodegenNode(branch, keyIndex, context);
  }
}
function createChildrenCodegenNode(branch, keyIndex, context) {
  const { helper } = context;
  const keyProperty = createObjectProperty(`key`, createSimpleExpression(`${keyIndex}`, false, locStub, 2));
  const { children } = branch;
  const firstChild = children[0];
  const needFragmentWrapper = children.length !== 1 || firstChild.type !== 1;
  if (needFragmentWrapper) {
    if (children.length === 1 && firstChild.type === 11) {
      const vnodeCall = firstChild.codegenNode;
      injectProp(vnodeCall, keyProperty, context);
      return vnodeCall;
    } else {
      let patchFlag = 64;
      let patchFlagText = PatchFlagNames[64];
      if (children.filter((c) => c.type !== 3).length === 1) {
        patchFlag |= 2048;
        patchFlagText += `, ${PatchFlagNames[2048]}`;
      }
      return createVNodeCall(context, helper(FRAGMENT), createObjectExpression([keyProperty]), children, patchFlag + (true ? ` /* ${patchFlagText} */` : ``), void 0, void 0, true, false, false, branch.loc);
    }
  } else {
    const ret = firstChild.codegenNode;
    const vnodeCall = getMemoedVNodeCall(ret);
    if (vnodeCall.type === 13) {
      makeBlock(vnodeCall, context);
    }
    injectProp(vnodeCall, keyProperty, context);
    return ret;
  }
}
function isSameKey(a, b) {
  if (!a || a.type !== b.type) {
    return false;
  }
  if (a.type === 6) {
    if (a.value.content !== b.value.content) {
      return false;
    }
  } else {
    const exp = a.exp;
    const branchExp = b.exp;
    if (exp.type !== branchExp.type) {
      return false;
    }
    if (exp.type !== 4 || exp.isStatic !== branchExp.isStatic || exp.content !== branchExp.content) {
      return false;
    }
  }
  return true;
}
function getParentCondition(node) {
  while (true) {
    if (node.type === 19) {
      if (node.alternate.type === 19) {
        node = node.alternate;
      } else {
        return node;
      }
    } else if (node.type === 20) {
      node = node.value;
    }
  }
}
function processFor(node, dir, context, processCodegen) {
  if (!dir.exp) {
    context.onError(createCompilerError(31, dir.loc));
    return;
  }
  const parseResult = parseForExpression(dir.exp, context);
  if (!parseResult) {
    context.onError(createCompilerError(32, dir.loc));
    return;
  }
  const { addIdentifiers, removeIdentifiers, scopes } = context;
  const { source, value, key, index } = parseResult;
  const forNode = {
    type: 11,
    loc: dir.loc,
    source,
    valueAlias: value,
    keyAlias: key,
    objectIndexAlias: index,
    parseResult,
    children: isTemplateNode(node) ? node.children : [node]
  };
  context.replaceNode(forNode);
  scopes.vFor++;
  const onExit = processCodegen && processCodegen(forNode);
  return () => {
    scopes.vFor--;
    if (onExit)
      onExit();
  };
}
function parseForExpression(input, context) {
  const loc = input.loc;
  const exp = input.content;
  const inMatch = exp.match(forAliasRE);
  if (!inMatch)
    return;
  const [, LHS, RHS] = inMatch;
  const result = {
    source: createAliasExpression(loc, RHS.trim(), exp.indexOf(RHS, LHS.length)),
    value: void 0,
    key: void 0,
    index: void 0
  };
  if (true) {
    validateBrowserExpression(result.source, context);
  }
  let valueContent = LHS.trim().replace(stripParensRE, "").trim();
  const trimmedOffset = LHS.indexOf(valueContent);
  const iteratorMatch = valueContent.match(forIteratorRE);
  if (iteratorMatch) {
    valueContent = valueContent.replace(forIteratorRE, "").trim();
    const keyContent = iteratorMatch[1].trim();
    let keyOffset;
    if (keyContent) {
      keyOffset = exp.indexOf(keyContent, trimmedOffset + valueContent.length);
      result.key = createAliasExpression(loc, keyContent, keyOffset);
      if (true) {
        validateBrowserExpression(result.key, context, true);
      }
    }
    if (iteratorMatch[2]) {
      const indexContent = iteratorMatch[2].trim();
      if (indexContent) {
        result.index = createAliasExpression(loc, indexContent, exp.indexOf(indexContent, result.key ? keyOffset + keyContent.length : trimmedOffset + valueContent.length));
        if (true) {
          validateBrowserExpression(result.index, context, true);
        }
      }
    }
  }
  if (valueContent) {
    result.value = createAliasExpression(loc, valueContent, trimmedOffset);
    if (true) {
      validateBrowserExpression(result.value, context, true);
    }
  }
  return result;
}
function createAliasExpression(range, content, offset) {
  return createSimpleExpression(content, false, getInnerRange(range, offset, content.length));
}
function createForLoopParams({ value, key, index }, memoArgs = []) {
  return createParamsList([value, key, index, ...memoArgs]);
}
function createParamsList(args) {
  let i = args.length;
  while (i--) {
    if (args[i])
      break;
  }
  return args.slice(0, i + 1).map((arg, i2) => arg || createSimpleExpression(`_`.repeat(i2 + 1), false));
}
function buildSlots(node, context, buildSlotFn = buildClientSlotFn) {
  context.helper(WITH_CTX);
  const { children, loc } = node;
  const slotsProperties = [];
  const dynamicSlots = [];
  let hasDynamicSlots = context.scopes.vSlot > 0 || context.scopes.vFor > 0;
  const onComponentSlot = findDir(node, "slot", true);
  if (onComponentSlot) {
    const { arg, exp } = onComponentSlot;
    if (arg && !isStaticExp(arg)) {
      hasDynamicSlots = true;
    }
    slotsProperties.push(createObjectProperty(arg || createSimpleExpression("default", true), buildSlotFn(exp, children, loc)));
  }
  let hasTemplateSlots = false;
  let hasNamedDefaultSlot = false;
  const implicitDefaultChildren = [];
  const seenSlotNames = new Set();
  for (let i = 0; i < children.length; i++) {
    const slotElement = children[i];
    let slotDir;
    if (!isTemplateNode(slotElement) || !(slotDir = findDir(slotElement, "slot", true))) {
      if (slotElement.type !== 3) {
        implicitDefaultChildren.push(slotElement);
      }
      continue;
    }
    if (onComponentSlot) {
      context.onError(createCompilerError(37, slotDir.loc));
      break;
    }
    hasTemplateSlots = true;
    const { children: slotChildren, loc: slotLoc } = slotElement;
    const { arg: slotName = createSimpleExpression(`default`, true), exp: slotProps, loc: dirLoc } = slotDir;
    let staticSlotName;
    if (isStaticExp(slotName)) {
      staticSlotName = slotName ? slotName.content : `default`;
    } else {
      hasDynamicSlots = true;
    }
    const slotFunction = buildSlotFn(slotProps, slotChildren, slotLoc);
    let vIf;
    let vElse;
    let vFor;
    if (vIf = findDir(slotElement, "if")) {
      hasDynamicSlots = true;
      dynamicSlots.push(createConditionalExpression(vIf.exp, buildDynamicSlot(slotName, slotFunction), defaultFallback));
    } else if (vElse = findDir(slotElement, /^else(-if)?$/, true)) {
      let j = i;
      let prev;
      while (j--) {
        prev = children[j];
        if (prev.type !== 3) {
          break;
        }
      }
      if (prev && isTemplateNode(prev) && findDir(prev, "if")) {
        children.splice(i, 1);
        i--;
        let conditional = dynamicSlots[dynamicSlots.length - 1];
        while (conditional.alternate.type === 19) {
          conditional = conditional.alternate;
        }
        conditional.alternate = vElse.exp ? createConditionalExpression(vElse.exp, buildDynamicSlot(slotName, slotFunction), defaultFallback) : buildDynamicSlot(slotName, slotFunction);
      } else {
        context.onError(createCompilerError(30, vElse.loc));
      }
    } else if (vFor = findDir(slotElement, "for")) {
      hasDynamicSlots = true;
      const parseResult = vFor.parseResult || parseForExpression(vFor.exp, context);
      if (parseResult) {
        dynamicSlots.push(createCallExpression(context.helper(RENDER_LIST), [
          parseResult.source,
          createFunctionExpression(createForLoopParams(parseResult), buildDynamicSlot(slotName, slotFunction), true)
        ]));
      } else {
        context.onError(createCompilerError(32, vFor.loc));
      }
    } else {
      if (staticSlotName) {
        if (seenSlotNames.has(staticSlotName)) {
          context.onError(createCompilerError(38, dirLoc));
          continue;
        }
        seenSlotNames.add(staticSlotName);
        if (staticSlotName === "default") {
          hasNamedDefaultSlot = true;
        }
      }
      slotsProperties.push(createObjectProperty(slotName, slotFunction));
    }
  }
  if (!onComponentSlot) {
    const buildDefaultSlotProperty = (props, children2) => {
      const fn = buildSlotFn(props, children2, loc);
      if (context.compatConfig) {
        fn.isNonScopedSlot = true;
      }
      return createObjectProperty(`default`, fn);
    };
    if (!hasTemplateSlots) {
      slotsProperties.push(buildDefaultSlotProperty(void 0, children));
    } else if (implicitDefaultChildren.length && implicitDefaultChildren.some((node2) => isNonWhitespaceContent(node2))) {
      if (hasNamedDefaultSlot) {
        context.onError(createCompilerError(39, implicitDefaultChildren[0].loc));
      } else {
        slotsProperties.push(buildDefaultSlotProperty(void 0, implicitDefaultChildren));
      }
    }
  }
  const slotFlag = hasDynamicSlots ? 2 : hasForwardedSlots(node.children) ? 3 : 1;
  let slots = createObjectExpression(slotsProperties.concat(createObjectProperty(`_`, createSimpleExpression(slotFlag + (true ? ` /* ${slotFlagsText[slotFlag]} */` : ``), false))), loc);
  if (dynamicSlots.length) {
    slots = createCallExpression(context.helper(CREATE_SLOTS), [
      slots,
      createArrayExpression(dynamicSlots)
    ]);
  }
  return {
    slots,
    hasDynamicSlots
  };
}
function buildDynamicSlot(name, fn) {
  return createObjectExpression([
    createObjectProperty(`name`, name),
    createObjectProperty(`fn`, fn)
  ]);
}
function hasForwardedSlots(children) {
  for (let i = 0; i < children.length; i++) {
    const child = children[i];
    switch (child.type) {
      case 1:
        if (child.tagType === 2 || hasForwardedSlots(child.children)) {
          return true;
        }
        break;
      case 9:
        if (hasForwardedSlots(child.branches))
          return true;
        break;
      case 10:
      case 11:
        if (hasForwardedSlots(child.children))
          return true;
        break;
    }
  }
  return false;
}
function isNonWhitespaceContent(node) {
  if (node.type !== 2 && node.type !== 12)
    return true;
  return node.type === 2 ? !!node.content.trim() : isNonWhitespaceContent(node.content);
}
function resolveComponentType(node, context, ssr = false) {
  let { tag } = node;
  const isExplicitDynamic = isComponentTag(tag);
  const isProp = findProp(node, "is");
  if (isProp) {
    if (isExplicitDynamic || isCompatEnabled("COMPILER_IS_ON_ELEMENT", context)) {
      const exp = isProp.type === 6 ? isProp.value && createSimpleExpression(isProp.value.content, true) : isProp.exp;
      if (exp) {
        return createCallExpression(context.helper(RESOLVE_DYNAMIC_COMPONENT), [
          exp
        ]);
      }
    } else if (isProp.type === 6 && isProp.value.content.startsWith("vue:")) {
      tag = isProp.value.content.slice(4);
    }
  }
  const isDir = !isExplicitDynamic && findDir(node, "is");
  if (isDir && isDir.exp) {
    return createCallExpression(context.helper(RESOLVE_DYNAMIC_COMPONENT), [
      isDir.exp
    ]);
  }
  const builtIn = isCoreComponent(tag) || context.isBuiltInComponent(tag);
  if (builtIn) {
    if (!ssr)
      context.helper(builtIn);
    return builtIn;
  }
  context.helper(RESOLVE_COMPONENT);
  context.components.add(tag);
  return toValidAssetId(tag, `component`);
}
function buildProps(node, context, props = node.props, ssr = false) {
  const { tag, loc: elementLoc, children } = node;
  const isComponent2 = node.tagType === 1;
  let properties = [];
  const mergeArgs = [];
  const runtimeDirectives = [];
  const hasChildren = children.length > 0;
  let shouldUseBlock = false;
  let patchFlag = 0;
  let hasRef = false;
  let hasClassBinding = false;
  let hasStyleBinding = false;
  let hasHydrationEventBinding = false;
  let hasDynamicKeys = false;
  let hasVnodeHook = false;
  const dynamicPropNames = [];
  const analyzePatchFlag = ({ key, value }) => {
    if (isStaticExp(key)) {
      const name = key.content;
      const isEventHandler = isOn(name);
      if (!isComponent2 && isEventHandler && name.toLowerCase() !== "onclick" && name !== "onUpdate:modelValue" && !isReservedProp(name)) {
        hasHydrationEventBinding = true;
      }
      if (isEventHandler && isReservedProp(name)) {
        hasVnodeHook = true;
      }
      if (value.type === 20 || (value.type === 4 || value.type === 8) && getConstantType(value, context) > 0) {
        return;
      }
      if (name === "ref") {
        hasRef = true;
      } else if (name === "class") {
        hasClassBinding = true;
      } else if (name === "style") {
        hasStyleBinding = true;
      } else if (name !== "key" && !dynamicPropNames.includes(name)) {
        dynamicPropNames.push(name);
      }
      if (isComponent2 && (name === "class" || name === "style") && !dynamicPropNames.includes(name)) {
        dynamicPropNames.push(name);
      }
    } else {
      hasDynamicKeys = true;
    }
  };
  for (let i = 0; i < props.length; i++) {
    const prop = props[i];
    if (prop.type === 6) {
      const { loc, name, value } = prop;
      let isStatic = true;
      if (name === "ref") {
        hasRef = true;
        if (context.scopes.vFor > 0) {
          properties.push(createObjectProperty(createSimpleExpression("ref_for", true), createSimpleExpression("true")));
        }
      }
      if (name === "is" && (isComponentTag(tag) || value && value.content.startsWith("vue:") || isCompatEnabled("COMPILER_IS_ON_ELEMENT", context))) {
        continue;
      }
      properties.push(createObjectProperty(createSimpleExpression(name, true, getInnerRange(loc, 0, name.length)), createSimpleExpression(value ? value.content : "", isStatic, value ? value.loc : loc)));
    } else {
      const { name, arg, exp, loc } = prop;
      const isVBind = name === "bind";
      const isVOn = name === "on";
      if (name === "slot") {
        if (!isComponent2) {
          context.onError(createCompilerError(40, loc));
        }
        continue;
      }
      if (name === "once" || name === "memo") {
        continue;
      }
      if (name === "is" || isVBind && isStaticArgOf(arg, "is") && (isComponentTag(tag) || isCompatEnabled("COMPILER_IS_ON_ELEMENT", context))) {
        continue;
      }
      if (isVOn && ssr) {
        continue;
      }
      if (isVBind && isStaticArgOf(arg, "key") || isVOn && hasChildren && isStaticArgOf(arg, "vue:before-update")) {
        shouldUseBlock = true;
      }
      if (isVBind && isStaticArgOf(arg, "ref") && context.scopes.vFor > 0) {
        properties.push(createObjectProperty(createSimpleExpression("ref_for", true), createSimpleExpression("true")));
      }
      if (!arg && (isVBind || isVOn)) {
        hasDynamicKeys = true;
        if (exp) {
          if (properties.length) {
            mergeArgs.push(createObjectExpression(dedupeProperties(properties), elementLoc));
            properties = [];
          }
          if (isVBind) {
            {
              if (true) {
                const hasOverridableKeys = mergeArgs.some((arg2) => {
                  if (arg2.type === 15) {
                    return arg2.properties.some(({ key }) => {
                      if (key.type !== 4 || !key.isStatic) {
                        return true;
                      }
                      return key.content !== "class" && key.content !== "style" && !isOn(key.content);
                    });
                  } else {
                    return true;
                  }
                });
                if (hasOverridableKeys) {
                  checkCompatEnabled("COMPILER_V_BIND_OBJECT_ORDER", context, loc);
                }
              }
              if (isCompatEnabled("COMPILER_V_BIND_OBJECT_ORDER", context)) {
                mergeArgs.unshift(exp);
                continue;
              }
            }
            mergeArgs.push(exp);
          } else {
            mergeArgs.push({
              type: 14,
              loc,
              callee: context.helper(TO_HANDLERS),
              arguments: [exp]
            });
          }
        } else {
          context.onError(createCompilerError(isVBind ? 34 : 35, loc));
        }
        continue;
      }
      const directiveTransform = context.directiveTransforms[name];
      if (directiveTransform) {
        const { props: props2, needRuntime } = directiveTransform(prop, node, context);
        !ssr && props2.forEach(analyzePatchFlag);
        properties.push(...props2);
        if (needRuntime) {
          runtimeDirectives.push(prop);
          if (isSymbol(needRuntime)) {
            directiveImportMap.set(prop, needRuntime);
          }
        }
      } else {
        runtimeDirectives.push(prop);
        if (hasChildren) {
          shouldUseBlock = true;
        }
      }
    }
  }
  let propsExpression = void 0;
  if (mergeArgs.length) {
    if (properties.length) {
      mergeArgs.push(createObjectExpression(dedupeProperties(properties), elementLoc));
    }
    if (mergeArgs.length > 1) {
      propsExpression = createCallExpression(context.helper(MERGE_PROPS), mergeArgs, elementLoc);
    } else {
      propsExpression = mergeArgs[0];
    }
  } else if (properties.length) {
    propsExpression = createObjectExpression(dedupeProperties(properties), elementLoc);
  }
  if (hasDynamicKeys) {
    patchFlag |= 16;
  } else {
    if (hasClassBinding && !isComponent2) {
      patchFlag |= 2;
    }
    if (hasStyleBinding && !isComponent2) {
      patchFlag |= 4;
    }
    if (dynamicPropNames.length) {
      patchFlag |= 8;
    }
    if (hasHydrationEventBinding) {
      patchFlag |= 32;
    }
  }
  if (!shouldUseBlock && (patchFlag === 0 || patchFlag === 32) && (hasRef || hasVnodeHook || runtimeDirectives.length > 0)) {
    patchFlag |= 512;
  }
  if (!context.inSSR && propsExpression) {
    switch (propsExpression.type) {
      case 15:
        let classKeyIndex = -1;
        let styleKeyIndex = -1;
        let hasDynamicKey = false;
        for (let i = 0; i < propsExpression.properties.length; i++) {
          const key = propsExpression.properties[i].key;
          if (isStaticExp(key)) {
            if (key.content === "class") {
              classKeyIndex = i;
            } else if (key.content === "style") {
              styleKeyIndex = i;
            }
          } else if (!key.isHandlerKey) {
            hasDynamicKey = true;
          }
        }
        const classProp = propsExpression.properties[classKeyIndex];
        const styleProp = propsExpression.properties[styleKeyIndex];
        if (!hasDynamicKey) {
          if (classProp && !isStaticExp(classProp.value)) {
            classProp.value = createCallExpression(context.helper(NORMALIZE_CLASS), [classProp.value]);
          }
          if (styleProp && !isStaticExp(styleProp.value) && (hasStyleBinding || styleProp.value.type === 17)) {
            styleProp.value = createCallExpression(context.helper(NORMALIZE_STYLE), [styleProp.value]);
          }
        } else {
          propsExpression = createCallExpression(context.helper(NORMALIZE_PROPS), [propsExpression]);
        }
        break;
      case 14:
        break;
      default:
        propsExpression = createCallExpression(context.helper(NORMALIZE_PROPS), [
          createCallExpression(context.helper(GUARD_REACTIVE_PROPS), [
            propsExpression
          ])
        ]);
        break;
    }
  }
  return {
    props: propsExpression,
    directives: runtimeDirectives,
    patchFlag,
    dynamicPropNames,
    shouldUseBlock
  };
}
function dedupeProperties(properties) {
  const knownProps = new Map();
  const deduped = [];
  for (let i = 0; i < properties.length; i++) {
    const prop = properties[i];
    if (prop.key.type === 8 || !prop.key.isStatic) {
      deduped.push(prop);
      continue;
    }
    const name = prop.key.content;
    const existing = knownProps.get(name);
    if (existing) {
      if (name === "style" || name === "class" || isOn(name)) {
        mergeAsArray(existing, prop);
      }
    } else {
      knownProps.set(name, prop);
      deduped.push(prop);
    }
  }
  return deduped;
}
function mergeAsArray(existing, incoming) {
  if (existing.value.type === 17) {
    existing.value.elements.push(incoming.value);
  } else {
    existing.value = createArrayExpression([existing.value, incoming.value], existing.loc);
  }
}
function buildDirectiveArgs(dir, context) {
  const dirArgs = [];
  const runtime = directiveImportMap.get(dir);
  if (runtime) {
    dirArgs.push(context.helperString(runtime));
  } else {
    {
      context.helper(RESOLVE_DIRECTIVE);
      context.directives.add(dir.name);
      dirArgs.push(toValidAssetId(dir.name, `directive`));
    }
  }
  const { loc } = dir;
  if (dir.exp)
    dirArgs.push(dir.exp);
  if (dir.arg) {
    if (!dir.exp) {
      dirArgs.push(`void 0`);
    }
    dirArgs.push(dir.arg);
  }
  if (Object.keys(dir.modifiers).length) {
    if (!dir.arg) {
      if (!dir.exp) {
        dirArgs.push(`void 0`);
      }
      dirArgs.push(`void 0`);
    }
    const trueExpression = createSimpleExpression(`true`, false, loc);
    dirArgs.push(createObjectExpression(dir.modifiers.map((modifier) => createObjectProperty(modifier, trueExpression)), loc));
  }
  return createArrayExpression(dirArgs, dir.loc);
}
function stringifyDynamicPropNames(props) {
  let propsNamesString = `[`;
  for (let i = 0, l = props.length; i < l; i++) {
    propsNamesString += JSON.stringify(props[i]);
    if (i < l - 1)
      propsNamesString += ", ";
  }
  return propsNamesString + `]`;
}
function isComponentTag(tag) {
  return tag === "component" || tag === "Component";
}
function processSlotOutlet(node, context) {
  let slotName = `"default"`;
  let slotProps = void 0;
  const nonNameProps = [];
  for (let i = 0; i < node.props.length; i++) {
    const p = node.props[i];
    if (p.type === 6) {
      if (p.value) {
        if (p.name === "name") {
          slotName = JSON.stringify(p.value.content);
        } else {
          p.name = camelize2(p.name);
          nonNameProps.push(p);
        }
      }
    } else {
      if (p.name === "bind" && isStaticArgOf(p.arg, "name")) {
        if (p.exp)
          slotName = p.exp;
      } else {
        if (p.name === "bind" && p.arg && isStaticExp(p.arg)) {
          p.arg.content = camelize2(p.arg.content);
        }
        nonNameProps.push(p);
      }
    }
  }
  if (nonNameProps.length > 0) {
    const { props, directives } = buildProps(node, context, nonNameProps);
    slotProps = props;
    if (directives.length) {
      context.onError(createCompilerError(36, directives[0].loc));
    }
  }
  return {
    slotName,
    slotProps
  };
}
function createTransformProps(props = []) {
  return { props };
}
function rewriteFilter(node, context) {
  if (node.type === 4) {
    parseFilter(node, context);
  } else {
    for (let i = 0; i < node.children.length; i++) {
      const child = node.children[i];
      if (typeof child !== "object")
        continue;
      if (child.type === 4) {
        parseFilter(child, context);
      } else if (child.type === 8) {
        rewriteFilter(node, context);
      } else if (child.type === 5) {
        rewriteFilter(child.content, context);
      }
    }
  }
}
function parseFilter(node, context) {
  const exp = node.content;
  let inSingle = false;
  let inDouble = false;
  let inTemplateString = false;
  let inRegex = false;
  let curly = 0;
  let square = 0;
  let paren = 0;
  let lastFilterIndex = 0;
  let c, prev, i, expression, filters = [];
  for (i = 0; i < exp.length; i++) {
    prev = c;
    c = exp.charCodeAt(i);
    if (inSingle) {
      if (c === 39 && prev !== 92)
        inSingle = false;
    } else if (inDouble) {
      if (c === 34 && prev !== 92)
        inDouble = false;
    } else if (inTemplateString) {
      if (c === 96 && prev !== 92)
        inTemplateString = false;
    } else if (inRegex) {
      if (c === 47 && prev !== 92)
        inRegex = false;
    } else if (c === 124 && exp.charCodeAt(i + 1) !== 124 && exp.charCodeAt(i - 1) !== 124 && !curly && !square && !paren) {
      if (expression === void 0) {
        lastFilterIndex = i + 1;
        expression = exp.slice(0, i).trim();
      } else {
        pushFilter();
      }
    } else {
      switch (c) {
        case 34:
          inDouble = true;
          break;
        case 39:
          inSingle = true;
          break;
        case 96:
          inTemplateString = true;
          break;
        case 40:
          paren++;
          break;
        case 41:
          paren--;
          break;
        case 91:
          square++;
          break;
        case 93:
          square--;
          break;
        case 123:
          curly++;
          break;
        case 125:
          curly--;
          break;
      }
      if (c === 47) {
        let j = i - 1;
        let p;
        for (; j >= 0; j--) {
          p = exp.charAt(j);
          if (p !== " ")
            break;
        }
        if (!p || !validDivisionCharRE.test(p)) {
          inRegex = true;
        }
      }
    }
  }
  if (expression === void 0) {
    expression = exp.slice(0, i).trim();
  } else if (lastFilterIndex !== 0) {
    pushFilter();
  }
  function pushFilter() {
    filters.push(exp.slice(lastFilterIndex, i).trim());
    lastFilterIndex = i + 1;
  }
  if (filters.length) {
    warnDeprecation("COMPILER_FILTER", context, node.loc);
    for (i = 0; i < filters.length; i++) {
      expression = wrapFilter(expression, filters[i], context);
    }
    node.content = expression;
  }
}
function wrapFilter(exp, filter, context) {
  context.helper(RESOLVE_FILTER);
  const i = filter.indexOf("(");
  if (i < 0) {
    context.filters.add(filter);
    return `${toValidAssetId(filter, "filter")}(${exp})`;
  } else {
    const name = filter.slice(0, i);
    const args = filter.slice(i + 1);
    context.filters.add(name);
    return `${toValidAssetId(name, "filter")}(${exp}${args !== ")" ? "," + args : args}`;
  }
}
function getBaseTransformPreset(prefixIdentifiers) {
  return [
    [
      transformOnce,
      transformIf,
      transformMemo,
      transformFor,
      ...[transformFilter],
      ...true ? [transformExpression] : [],
      transformSlotOutlet,
      transformElement,
      trackSlotScopes,
      transformText
    ],
    {
      on: transformOn,
      bind: transformBind,
      model: transformModel
    }
  ];
}
function baseCompile(template, options = {}) {
  const onError = options.onError || defaultOnError;
  const isModuleMode = options.mode === "module";
  {
    if (options.prefixIdentifiers === true) {
      onError(createCompilerError(46));
    } else if (isModuleMode) {
      onError(createCompilerError(47));
    }
  }
  const prefixIdentifiers = false;
  if (options.cacheHandlers) {
    onError(createCompilerError(48));
  }
  if (options.scopeId && !isModuleMode) {
    onError(createCompilerError(49));
  }
  const ast = isString(template) ? baseParse(template, options) : template;
  const [nodeTransforms, directiveTransforms] = getBaseTransformPreset();
  transform(ast, extend({}, options, {
    prefixIdentifiers,
    nodeTransforms: [
      ...nodeTransforms,
      ...options.nodeTransforms || []
    ],
    directiveTransforms: extend({}, directiveTransforms, options.directiveTransforms || {})
  }));
  return generate(ast, extend({}, options, {
    prefixIdentifiers
  }));
}
var errorMessages, FRAGMENT, TELEPORT, SUSPENSE, KEEP_ALIVE, BASE_TRANSITION, OPEN_BLOCK, CREATE_BLOCK, CREATE_ELEMENT_BLOCK, CREATE_VNODE, CREATE_ELEMENT_VNODE, CREATE_COMMENT, CREATE_TEXT, CREATE_STATIC, RESOLVE_COMPONENT, RESOLVE_DYNAMIC_COMPONENT, RESOLVE_DIRECTIVE, RESOLVE_FILTER, WITH_DIRECTIVES, RENDER_LIST, RENDER_SLOT, CREATE_SLOTS, TO_DISPLAY_STRING, MERGE_PROPS, NORMALIZE_CLASS, NORMALIZE_STYLE, NORMALIZE_PROPS, GUARD_REACTIVE_PROPS, TO_HANDLERS, CAMELIZE, CAPITALIZE, TO_HANDLER_KEY, SET_BLOCK_TRACKING, PUSH_SCOPE_ID, POP_SCOPE_ID, WITH_CTX, UNREF, IS_REF, WITH_MEMO, IS_MEMO_SAME, helperNameMap, locStub, isStaticExp, isBuiltInType, nonIdentifierRE, isSimpleIdentifier, validFirstIdentCharRE, validIdentCharRE, whitespaceRE, isMemberExpressionBrowser, isMemberExpressionNode, isMemberExpression, propsHelperSet, deprecationData, decodeRE, decodeMap, defaultParserOptions, isSpecialTemplateDirective, allowHoistedHelperSet, PURE_ANNOTATION, isFunctionType, isStaticProperty, isStaticPropertyKey, prohibitedKeywordRE, stripStringRE, transformExpression, transformIf, transformFor, forAliasRE, forIteratorRE, stripParensRE, defaultFallback, trackSlotScopes, trackVForSlotScopes, buildClientSlotFn, directiveImportMap, transformElement, cacheStringFunction, camelizeRE, camelize2, transformSlotOutlet, fnExpRE, transformOn, transformBind, injectPrefix, transformText, seen, transformOnce, transformModel, validDivisionCharRE, transformFilter, seen$1, transformMemo, noopDirectiveTransform;
var init_compiler_core_esm_bundler = __esm({
  "node_modules/@vue/compiler-core/dist/compiler-core.esm-bundler.js"() {
    init_shared_esm_bundler();
    init_shared_esm_bundler();
    errorMessages = {
      [0]: "Illegal comment.",
      [1]: "CDATA section is allowed only in XML context.",
      [2]: "Duplicate attribute.",
      [3]: "End tag cannot have attributes.",
      [4]: "Illegal '/' in tags.",
      [5]: "Unexpected EOF in tag.",
      [6]: "Unexpected EOF in CDATA section.",
      [7]: "Unexpected EOF in comment.",
      [8]: "Unexpected EOF in script.",
      [9]: "Unexpected EOF in tag.",
      [10]: "Incorrectly closed comment.",
      [11]: "Incorrectly opened comment.",
      [12]: "Illegal tag name. Use '&lt;' to print '<'.",
      [13]: "Attribute value was expected.",
      [14]: "End tag name was expected.",
      [15]: "Whitespace was expected.",
      [16]: "Unexpected '<!--' in comment.",
      [17]: `Attribute name cannot contain U+0022 ("), U+0027 ('), and U+003C (<).`,
      [18]: "Unquoted attribute value cannot contain U+0022 (\"), U+0027 ('), U+003C (<), U+003D (=), and U+0060 (`).",
      [19]: "Attribute name cannot start with '='.",
      [21]: "'<?' is allowed only in XML context.",
      [20]: `Unexpected null character.`,
      [22]: "Illegal '/' in tags.",
      [23]: "Invalid end tag.",
      [24]: "Element is missing end tag.",
      [25]: "Interpolation end sign was not found.",
      [27]: "End bracket for dynamic directive argument was not found. Note that dynamic directive argument cannot contain spaces.",
      [26]: "Legal directive name was expected.",
      [28]: `v-if/v-else-if is missing expression.`,
      [29]: `v-if/else branches must use unique keys.`,
      [30]: `v-else/v-else-if has no adjacent v-if or v-else-if.`,
      [31]: `v-for is missing expression.`,
      [32]: `v-for has invalid expression.`,
      [33]: `<template v-for> key should be placed on the <template> tag.`,
      [34]: `v-bind is missing expression.`,
      [35]: `v-on is missing expression.`,
      [36]: `Unexpected custom directive on <slot> outlet.`,
      [37]: `Mixed v-slot usage on both the component and nested <template>.When there are multiple named slots, all slots should use <template> syntax to avoid scope ambiguity.`,
      [38]: `Duplicate slot names found. `,
      [39]: `Extraneous children found when component already has explicitly named default slot. These children will be ignored.`,
      [40]: `v-slot can only be used on components or <template> tags.`,
      [41]: `v-model is missing expression.`,
      [42]: `v-model value must be a valid JavaScript member expression.`,
      [43]: `v-model cannot be used on v-for or v-slot scope variables because they are not writable.`,
      [44]: `Error parsing JavaScript expression: `,
      [45]: `<KeepAlive> expects exactly one child component.`,
      [46]: `"prefixIdentifiers" option is not supported in this build of compiler.`,
      [47]: `ES module mode is not supported in this build of compiler.`,
      [48]: `"cacheHandlers" option is only supported when the "prefixIdentifiers" option is enabled.`,
      [49]: `"scopeId" option is only supported in module mode.`,
      [50]: ``
    };
    FRAGMENT = Symbol(true ? `Fragment` : ``);
    TELEPORT = Symbol(true ? `Teleport` : ``);
    SUSPENSE = Symbol(true ? `Suspense` : ``);
    KEEP_ALIVE = Symbol(true ? `KeepAlive` : ``);
    BASE_TRANSITION = Symbol(true ? `BaseTransition` : ``);
    OPEN_BLOCK = Symbol(true ? `openBlock` : ``);
    CREATE_BLOCK = Symbol(true ? `createBlock` : ``);
    CREATE_ELEMENT_BLOCK = Symbol(true ? `createElementBlock` : ``);
    CREATE_VNODE = Symbol(true ? `createVNode` : ``);
    CREATE_ELEMENT_VNODE = Symbol(true ? `createElementVNode` : ``);
    CREATE_COMMENT = Symbol(true ? `createCommentVNode` : ``);
    CREATE_TEXT = Symbol(true ? `createTextVNode` : ``);
    CREATE_STATIC = Symbol(true ? `createStaticVNode` : ``);
    RESOLVE_COMPONENT = Symbol(true ? `resolveComponent` : ``);
    RESOLVE_DYNAMIC_COMPONENT = Symbol(true ? `resolveDynamicComponent` : ``);
    RESOLVE_DIRECTIVE = Symbol(true ? `resolveDirective` : ``);
    RESOLVE_FILTER = Symbol(true ? `resolveFilter` : ``);
    WITH_DIRECTIVES = Symbol(true ? `withDirectives` : ``);
    RENDER_LIST = Symbol(true ? `renderList` : ``);
    RENDER_SLOT = Symbol(true ? `renderSlot` : ``);
    CREATE_SLOTS = Symbol(true ? `createSlots` : ``);
    TO_DISPLAY_STRING = Symbol(true ? `toDisplayString` : ``);
    MERGE_PROPS = Symbol(true ? `mergeProps` : ``);
    NORMALIZE_CLASS = Symbol(true ? `normalizeClass` : ``);
    NORMALIZE_STYLE = Symbol(true ? `normalizeStyle` : ``);
    NORMALIZE_PROPS = Symbol(true ? `normalizeProps` : ``);
    GUARD_REACTIVE_PROPS = Symbol(true ? `guardReactiveProps` : ``);
    TO_HANDLERS = Symbol(true ? `toHandlers` : ``);
    CAMELIZE = Symbol(true ? `camelize` : ``);
    CAPITALIZE = Symbol(true ? `capitalize` : ``);
    TO_HANDLER_KEY = Symbol(true ? `toHandlerKey` : ``);
    SET_BLOCK_TRACKING = Symbol(true ? `setBlockTracking` : ``);
    PUSH_SCOPE_ID = Symbol(true ? `pushScopeId` : ``);
    POP_SCOPE_ID = Symbol(true ? `popScopeId` : ``);
    WITH_CTX = Symbol(true ? `withCtx` : ``);
    UNREF = Symbol(true ? `unref` : ``);
    IS_REF = Symbol(true ? `isRef` : ``);
    WITH_MEMO = Symbol(true ? `withMemo` : ``);
    IS_MEMO_SAME = Symbol(true ? `isMemoSame` : ``);
    helperNameMap = {
      [FRAGMENT]: `Fragment`,
      [TELEPORT]: `Teleport`,
      [SUSPENSE]: `Suspense`,
      [KEEP_ALIVE]: `KeepAlive`,
      [BASE_TRANSITION]: `BaseTransition`,
      [OPEN_BLOCK]: `openBlock`,
      [CREATE_BLOCK]: `createBlock`,
      [CREATE_ELEMENT_BLOCK]: `createElementBlock`,
      [CREATE_VNODE]: `createVNode`,
      [CREATE_ELEMENT_VNODE]: `createElementVNode`,
      [CREATE_COMMENT]: `createCommentVNode`,
      [CREATE_TEXT]: `createTextVNode`,
      [CREATE_STATIC]: `createStaticVNode`,
      [RESOLVE_COMPONENT]: `resolveComponent`,
      [RESOLVE_DYNAMIC_COMPONENT]: `resolveDynamicComponent`,
      [RESOLVE_DIRECTIVE]: `resolveDirective`,
      [RESOLVE_FILTER]: `resolveFilter`,
      [WITH_DIRECTIVES]: `withDirectives`,
      [RENDER_LIST]: `renderList`,
      [RENDER_SLOT]: `renderSlot`,
      [CREATE_SLOTS]: `createSlots`,
      [TO_DISPLAY_STRING]: `toDisplayString`,
      [MERGE_PROPS]: `mergeProps`,
      [NORMALIZE_CLASS]: `normalizeClass`,
      [NORMALIZE_STYLE]: `normalizeStyle`,
      [NORMALIZE_PROPS]: `normalizeProps`,
      [GUARD_REACTIVE_PROPS]: `guardReactiveProps`,
      [TO_HANDLERS]: `toHandlers`,
      [CAMELIZE]: `camelize`,
      [CAPITALIZE]: `capitalize`,
      [TO_HANDLER_KEY]: `toHandlerKey`,
      [SET_BLOCK_TRACKING]: `setBlockTracking`,
      [PUSH_SCOPE_ID]: `pushScopeId`,
      [POP_SCOPE_ID]: `popScopeId`,
      [WITH_CTX]: `withCtx`,
      [UNREF]: `unref`,
      [IS_REF]: `isRef`,
      [WITH_MEMO]: `withMemo`,
      [IS_MEMO_SAME]: `isMemoSame`
    };
    locStub = {
      source: "",
      start: { line: 1, column: 1, offset: 0 },
      end: { line: 1, column: 1, offset: 0 }
    };
    isStaticExp = (p) => p.type === 4 && p.isStatic;
    isBuiltInType = (tag, expected) => tag === expected || tag === hyphenate(expected);
    nonIdentifierRE = /^\d|[^\$\w]/;
    isSimpleIdentifier = (name) => !nonIdentifierRE.test(name);
    validFirstIdentCharRE = /[A-Za-z_$\xA0-\uFFFF]/;
    validIdentCharRE = /[\.\?\w$\xA0-\uFFFF]/;
    whitespaceRE = /\s+[.[]\s*|\s*[.[]\s+/g;
    isMemberExpressionBrowser = (path) => {
      path = path.trim().replace(whitespaceRE, (s) => s.trim());
      let state = 0;
      let stateStack = [];
      let currentOpenBracketCount = 0;
      let currentOpenParensCount = 0;
      let currentStringType = null;
      for (let i = 0; i < path.length; i++) {
        const char = path.charAt(i);
        switch (state) {
          case 0:
            if (char === "[") {
              stateStack.push(state);
              state = 1;
              currentOpenBracketCount++;
            } else if (char === "(") {
              stateStack.push(state);
              state = 2;
              currentOpenParensCount++;
            } else if (!(i === 0 ? validFirstIdentCharRE : validIdentCharRE).test(char)) {
              return false;
            }
            break;
          case 1:
            if (char === `'` || char === `"` || char === "`") {
              stateStack.push(state);
              state = 3;
              currentStringType = char;
            } else if (char === `[`) {
              currentOpenBracketCount++;
            } else if (char === `]`) {
              if (!--currentOpenBracketCount) {
                state = stateStack.pop();
              }
            }
            break;
          case 2:
            if (char === `'` || char === `"` || char === "`") {
              stateStack.push(state);
              state = 3;
              currentStringType = char;
            } else if (char === `(`) {
              currentOpenParensCount++;
            } else if (char === `)`) {
              if (i === path.length - 1) {
                return false;
              }
              if (!--currentOpenParensCount) {
                state = stateStack.pop();
              }
            }
            break;
          case 3:
            if (char === currentStringType) {
              state = stateStack.pop();
              currentStringType = null;
            }
            break;
        }
      }
      return !currentOpenBracketCount && !currentOpenParensCount;
    };
    isMemberExpressionNode = NOOP;
    isMemberExpression = isMemberExpressionBrowser;
    propsHelperSet = new Set([NORMALIZE_PROPS, GUARD_REACTIVE_PROPS]);
    deprecationData = {
      ["COMPILER_IS_ON_ELEMENT"]: {
        message: `Platform-native elements with "is" prop will no longer be treated as components in Vue 3 unless the "is" value is explicitly prefixed with "vue:".`,
        link: `https://v3.vuejs.org/guide/migration/custom-elements-interop.html`
      },
      ["COMPILER_V_BIND_SYNC"]: {
        message: (key) => `.sync modifier for v-bind has been removed. Use v-model with argument instead. \`v-bind:${key}.sync\` should be changed to \`v-model:${key}\`.`,
        link: `https://v3.vuejs.org/guide/migration/v-model.html`
      },
      ["COMPILER_V_BIND_PROP"]: {
        message: `.prop modifier for v-bind has been removed and no longer necessary. Vue 3 will automatically set a binding as DOM property when appropriate.`
      },
      ["COMPILER_V_BIND_OBJECT_ORDER"]: {
        message: `v-bind="obj" usage is now order sensitive and behaves like JavaScript object spread: it will now overwrite an existing non-mergeable attribute that appears before v-bind in the case of conflict. To retain 2.x behavior, move v-bind to make it the first attribute. You can also suppress this warning if the usage is intended.`,
        link: `https://v3.vuejs.org/guide/migration/v-bind.html`
      },
      ["COMPILER_V_ON_NATIVE"]: {
        message: `.native modifier for v-on has been removed as is no longer necessary.`,
        link: `https://v3.vuejs.org/guide/migration/v-on-native-modifier-removed.html`
      },
      ["COMPILER_V_IF_V_FOR_PRECEDENCE"]: {
        message: `v-if / v-for precedence when used on the same element has changed in Vue 3: v-if now takes higher precedence and will no longer have access to v-for scope variables. It is best to avoid the ambiguity with <template> tags or use a computed property that filters v-for data source.`,
        link: `https://v3.vuejs.org/guide/migration/v-if-v-for.html`
      },
      ["COMPILER_NATIVE_TEMPLATE"]: {
        message: `<template> with no special directives will render as a native template element instead of its inner content in Vue 3.`
      },
      ["COMPILER_INLINE_TEMPLATE"]: {
        message: `"inline-template" has been removed in Vue 3.`,
        link: `https://v3.vuejs.org/guide/migration/inline-template-attribute.html`
      },
      ["COMPILER_FILTER"]: {
        message: `filters have been removed in Vue 3. The "|" symbol will be treated as native JavaScript bitwise OR operator. Use method calls or computed properties instead.`,
        link: `https://v3.vuejs.org/guide/migration/filters.html`
      }
    };
    decodeRE = /&(gt|lt|amp|apos|quot);/g;
    decodeMap = {
      gt: ">",
      lt: "<",
      amp: "&",
      apos: "'",
      quot: '"'
    };
    defaultParserOptions = {
      delimiters: [`{{`, `}}`],
      getNamespace: () => 0,
      getTextMode: () => 0,
      isVoidTag: NO,
      isPreTag: NO,
      isCustomElement: NO,
      decodeEntities: (rawText) => rawText.replace(decodeRE, (_, p1) => decodeMap[p1]),
      onError: defaultOnError,
      onWarn: defaultOnWarn,
      comments: true
    };
    isSpecialTemplateDirective = makeMap(`if,else,else-if,for,slot`);
    allowHoistedHelperSet = new Set([
      NORMALIZE_CLASS,
      NORMALIZE_STYLE,
      NORMALIZE_PROPS,
      GUARD_REACTIVE_PROPS
    ]);
    PURE_ANNOTATION = `/*#__PURE__*/`;
    isFunctionType = (node) => {
      return /Function(?:Expression|Declaration)$|Method$/.test(node.type);
    };
    isStaticProperty = (node) => node && (node.type === "ObjectProperty" || node.type === "ObjectMethod") && !node.computed;
    isStaticPropertyKey = (node, parent) => isStaticProperty(parent) && parent.key === node;
    prohibitedKeywordRE = new RegExp("\\b" + "do,if,for,let,new,try,var,case,else,with,await,break,catch,class,const,super,throw,while,yield,delete,export,import,return,switch,default,extends,finally,continue,debugger,function,arguments,typeof,void".split(",").join("\\b|\\b") + "\\b");
    stripStringRE = /'(?:[^'\\]|\\.)*'|"(?:[^"\\]|\\.)*"|`(?:[^`\\]|\\.)*\$\{|\}(?:[^`\\]|\\.)*`|`(?:[^`\\]|\\.)*`/g;
    transformExpression = (node, context) => {
      if (node.type === 5) {
        node.content = processExpression(node.content, context);
      } else if (node.type === 1) {
        for (let i = 0; i < node.props.length; i++) {
          const dir = node.props[i];
          if (dir.type === 7 && dir.name !== "for") {
            const exp = dir.exp;
            const arg = dir.arg;
            if (exp && exp.type === 4 && !(dir.name === "on" && arg)) {
              dir.exp = processExpression(exp, context, dir.name === "slot");
            }
            if (arg && arg.type === 4 && !arg.isStatic) {
              dir.arg = processExpression(arg, context);
            }
          }
        }
      }
    };
    transformIf = createStructuralDirectiveTransform(/^(if|else|else-if)$/, (node, dir, context) => {
      return processIf(node, dir, context, (ifNode, branch, isRoot) => {
        const siblings = context.parent.children;
        let i = siblings.indexOf(ifNode);
        let key = 0;
        while (i-- >= 0) {
          const sibling = siblings[i];
          if (sibling && sibling.type === 9) {
            key += sibling.branches.length;
          }
        }
        return () => {
          if (isRoot) {
            ifNode.codegenNode = createCodegenNodeForBranch(branch, key, context);
          } else {
            const parentCondition = getParentCondition(ifNode.codegenNode);
            parentCondition.alternate = createCodegenNodeForBranch(branch, key + ifNode.branches.length - 1, context);
          }
        };
      });
    });
    transformFor = createStructuralDirectiveTransform("for", (node, dir, context) => {
      const { helper, removeHelper } = context;
      return processFor(node, dir, context, (forNode) => {
        const renderExp = createCallExpression(helper(RENDER_LIST), [
          forNode.source
        ]);
        const memo = findDir(node, "memo");
        const keyProp = findProp(node, `key`);
        const keyExp = keyProp && (keyProp.type === 6 ? createSimpleExpression(keyProp.value.content, true) : keyProp.exp);
        const keyProperty = keyProp ? createObjectProperty(`key`, keyExp) : null;
        const isStableFragment = forNode.source.type === 4 && forNode.source.constType > 0;
        const fragmentFlag = isStableFragment ? 64 : keyProp ? 128 : 256;
        forNode.codegenNode = createVNodeCall(context, helper(FRAGMENT), void 0, renderExp, fragmentFlag + (true ? ` /* ${PatchFlagNames[fragmentFlag]} */` : ``), void 0, void 0, true, !isStableFragment, false, node.loc);
        return () => {
          let childBlock;
          const isTemplate = isTemplateNode(node);
          const { children } = forNode;
          if (isTemplate) {
            node.children.some((c) => {
              if (c.type === 1) {
                const key = findProp(c, "key");
                if (key) {
                  context.onError(createCompilerError(33, key.loc));
                  return true;
                }
              }
            });
          }
          const needFragmentWrapper = children.length !== 1 || children[0].type !== 1;
          const slotOutlet = isSlotOutlet(node) ? node : isTemplate && node.children.length === 1 && isSlotOutlet(node.children[0]) ? node.children[0] : null;
          if (slotOutlet) {
            childBlock = slotOutlet.codegenNode;
            if (isTemplate && keyProperty) {
              injectProp(childBlock, keyProperty, context);
            }
          } else if (needFragmentWrapper) {
            childBlock = createVNodeCall(context, helper(FRAGMENT), keyProperty ? createObjectExpression([keyProperty]) : void 0, node.children, 64 + (true ? ` /* ${PatchFlagNames[64]} */` : ``), void 0, void 0, true, void 0, false);
          } else {
            childBlock = children[0].codegenNode;
            if (isTemplate && keyProperty) {
              injectProp(childBlock, keyProperty, context);
            }
            if (childBlock.isBlock !== !isStableFragment) {
              if (childBlock.isBlock) {
                removeHelper(OPEN_BLOCK);
                removeHelper(getVNodeBlockHelper(context.inSSR, childBlock.isComponent));
              } else {
                removeHelper(getVNodeHelper(context.inSSR, childBlock.isComponent));
              }
            }
            childBlock.isBlock = !isStableFragment;
            if (childBlock.isBlock) {
              helper(OPEN_BLOCK);
              helper(getVNodeBlockHelper(context.inSSR, childBlock.isComponent));
            } else {
              helper(getVNodeHelper(context.inSSR, childBlock.isComponent));
            }
          }
          if (memo) {
            const loop = createFunctionExpression(createForLoopParams(forNode.parseResult, [
              createSimpleExpression(`_cached`)
            ]));
            loop.body = createBlockStatement([
              createCompoundExpression([`const _memo = (`, memo.exp, `)`]),
              createCompoundExpression([
                `if (_cached`,
                ...keyExp ? [` && _cached.key === `, keyExp] : [],
                ` && ${context.helperString(IS_MEMO_SAME)}(_cached, _memo)) return _cached`
              ]),
              createCompoundExpression([`const _item = `, childBlock]),
              createSimpleExpression(`_item.memo = _memo`),
              createSimpleExpression(`return _item`)
            ]);
            renderExp.arguments.push(loop, createSimpleExpression(`_cache`), createSimpleExpression(String(context.cached++)));
          } else {
            renderExp.arguments.push(createFunctionExpression(createForLoopParams(forNode.parseResult), childBlock, true));
          }
        };
      });
    });
    forAliasRE = /([\s\S]*?)\s+(?:in|of)\s+([\s\S]*)/;
    forIteratorRE = /,([^,\}\]]*)(?:,([^,\}\]]*))?$/;
    stripParensRE = /^\(|\)$/g;
    defaultFallback = createSimpleExpression(`undefined`, false);
    trackSlotScopes = (node, context) => {
      if (node.type === 1 && (node.tagType === 1 || node.tagType === 3)) {
        const vSlot = findDir(node, "slot");
        if (vSlot) {
          vSlot.exp;
          context.scopes.vSlot++;
          return () => {
            context.scopes.vSlot--;
          };
        }
      }
    };
    trackVForSlotScopes = (node, context) => {
      let vFor;
      if (isTemplateNode(node) && node.props.some(isVSlot) && (vFor = findDir(node, "for"))) {
        const result = vFor.parseResult = parseForExpression(vFor.exp, context);
        if (result) {
          const { value, key, index } = result;
          const { addIdentifiers, removeIdentifiers } = context;
          value && addIdentifiers(value);
          key && addIdentifiers(key);
          index && addIdentifiers(index);
          return () => {
            value && removeIdentifiers(value);
            key && removeIdentifiers(key);
            index && removeIdentifiers(index);
          };
        }
      }
    };
    buildClientSlotFn = (props, children, loc) => createFunctionExpression(props, children, false, true, children.length ? children[0].loc : loc);
    directiveImportMap = new WeakMap();
    transformElement = (node, context) => {
      return function postTransformElement() {
        node = context.currentNode;
        if (!(node.type === 1 && (node.tagType === 0 || node.tagType === 1))) {
          return;
        }
        const { tag, props } = node;
        const isComponent2 = node.tagType === 1;
        let vnodeTag = isComponent2 ? resolveComponentType(node, context) : `"${tag}"`;
        const isDynamicComponent = isObject(vnodeTag) && vnodeTag.callee === RESOLVE_DYNAMIC_COMPONENT;
        let vnodeProps;
        let vnodeChildren;
        let vnodePatchFlag;
        let patchFlag = 0;
        let vnodeDynamicProps;
        let dynamicPropNames;
        let vnodeDirectives;
        let shouldUseBlock = isDynamicComponent || vnodeTag === TELEPORT || vnodeTag === SUSPENSE || !isComponent2 && (tag === "svg" || tag === "foreignObject");
        if (props.length > 0) {
          const propsBuildResult = buildProps(node, context);
          vnodeProps = propsBuildResult.props;
          patchFlag = propsBuildResult.patchFlag;
          dynamicPropNames = propsBuildResult.dynamicPropNames;
          const directives = propsBuildResult.directives;
          vnodeDirectives = directives && directives.length ? createArrayExpression(directives.map((dir) => buildDirectiveArgs(dir, context))) : void 0;
          if (propsBuildResult.shouldUseBlock) {
            shouldUseBlock = true;
          }
        }
        if (node.children.length > 0) {
          if (vnodeTag === KEEP_ALIVE) {
            shouldUseBlock = true;
            patchFlag |= 1024;
            if (node.children.length > 1) {
              context.onError(createCompilerError(45, {
                start: node.children[0].loc.start,
                end: node.children[node.children.length - 1].loc.end,
                source: ""
              }));
            }
          }
          const shouldBuildAsSlots = isComponent2 && vnodeTag !== TELEPORT && vnodeTag !== KEEP_ALIVE;
          if (shouldBuildAsSlots) {
            const { slots, hasDynamicSlots } = buildSlots(node, context);
            vnodeChildren = slots;
            if (hasDynamicSlots) {
              patchFlag |= 1024;
            }
          } else if (node.children.length === 1 && vnodeTag !== TELEPORT) {
            const child = node.children[0];
            const type = child.type;
            const hasDynamicTextChild = type === 5 || type === 8;
            if (hasDynamicTextChild && getConstantType(child, context) === 0) {
              patchFlag |= 1;
            }
            if (hasDynamicTextChild || type === 2) {
              vnodeChildren = child;
            } else {
              vnodeChildren = node.children;
            }
          } else {
            vnodeChildren = node.children;
          }
        }
        if (patchFlag !== 0) {
          if (true) {
            if (patchFlag < 0) {
              vnodePatchFlag = patchFlag + ` /* ${PatchFlagNames[patchFlag]} */`;
            } else {
              const flagNames = Object.keys(PatchFlagNames).map(Number).filter((n) => n > 0 && patchFlag & n).map((n) => PatchFlagNames[n]).join(`, `);
              vnodePatchFlag = patchFlag + ` /* ${flagNames} */`;
            }
          } else {
            vnodePatchFlag = String(patchFlag);
          }
          if (dynamicPropNames && dynamicPropNames.length) {
            vnodeDynamicProps = stringifyDynamicPropNames(dynamicPropNames);
          }
        }
        node.codegenNode = createVNodeCall(context, vnodeTag, vnodeProps, vnodeChildren, vnodePatchFlag, vnodeDynamicProps, vnodeDirectives, !!shouldUseBlock, false, isComponent2, node.loc);
      };
    };
    true ? Object.freeze({}) : {};
    true ? Object.freeze([]) : [];
    cacheStringFunction = (fn) => {
      const cache = Object.create(null);
      return (str) => {
        const hit = cache[str];
        return hit || (cache[str] = fn(str));
      };
    };
    camelizeRE = /-(\w)/g;
    camelize2 = cacheStringFunction((str) => {
      return str.replace(camelizeRE, (_, c) => c ? c.toUpperCase() : "");
    });
    transformSlotOutlet = (node, context) => {
      if (isSlotOutlet(node)) {
        const { children, loc } = node;
        const { slotName, slotProps } = processSlotOutlet(node, context);
        const slotArgs = [
          context.prefixIdentifiers ? `_ctx.$slots` : `$slots`,
          slotName,
          "{}",
          "undefined",
          "true"
        ];
        let expectedLen = 2;
        if (slotProps) {
          slotArgs[2] = slotProps;
          expectedLen = 3;
        }
        if (children.length) {
          slotArgs[3] = createFunctionExpression([], children, false, false, loc);
          expectedLen = 4;
        }
        if (context.scopeId && !context.slotted) {
          expectedLen = 5;
        }
        slotArgs.splice(expectedLen);
        node.codegenNode = createCallExpression(context.helper(RENDER_SLOT), slotArgs, loc);
      }
    };
    fnExpRE = /^\s*([\w$_]+|(async\s*)?\([^)]*?\))\s*=>|^\s*(async\s+)?function(?:\s+[\w$]+)?\s*\(/;
    transformOn = (dir, node, context, augmentor) => {
      const { loc, modifiers, arg } = dir;
      if (!dir.exp && !modifiers.length) {
        context.onError(createCompilerError(35, loc));
      }
      let eventName;
      if (arg.type === 4) {
        if (arg.isStatic) {
          let rawName = arg.content;
          if (rawName.startsWith("vue:")) {
            rawName = `vnode-${rawName.slice(4)}`;
          }
          eventName = createSimpleExpression(toHandlerKey(camelize(rawName)), true, arg.loc);
        } else {
          eventName = createCompoundExpression([
            `${context.helperString(TO_HANDLER_KEY)}(`,
            arg,
            `)`
          ]);
        }
      } else {
        eventName = arg;
        eventName.children.unshift(`${context.helperString(TO_HANDLER_KEY)}(`);
        eventName.children.push(`)`);
      }
      let exp = dir.exp;
      if (exp && !exp.content.trim()) {
        exp = void 0;
      }
      let shouldCache = context.cacheHandlers && !exp && !context.inVOnce;
      if (exp) {
        const isMemberExp = isMemberExpression(exp.content);
        const isInlineStatement = !(isMemberExp || fnExpRE.test(exp.content));
        const hasMultipleStatements = exp.content.includes(`;`);
        if (true) {
          validateBrowserExpression(exp, context, false, hasMultipleStatements);
        }
        if (isInlineStatement || shouldCache && isMemberExp) {
          exp = createCompoundExpression([
            `${isInlineStatement ? `$event` : `${``}(...args)`} => ${hasMultipleStatements ? `{` : `(`}`,
            exp,
            hasMultipleStatements ? `}` : `)`
          ]);
        }
      }
      let ret = {
        props: [
          createObjectProperty(eventName, exp || createSimpleExpression(`() => {}`, false, loc))
        ]
      };
      if (augmentor) {
        ret = augmentor(ret);
      }
      if (shouldCache) {
        ret.props[0].value = context.cache(ret.props[0].value);
      }
      ret.props.forEach((p) => p.key.isHandlerKey = true);
      return ret;
    };
    transformBind = (dir, _node, context) => {
      const { exp, modifiers, loc } = dir;
      const arg = dir.arg;
      if (arg.type !== 4) {
        arg.children.unshift(`(`);
        arg.children.push(`) || ""`);
      } else if (!arg.isStatic) {
        arg.content = `${arg.content} || ""`;
      }
      if (modifiers.includes("camel")) {
        if (arg.type === 4) {
          if (arg.isStatic) {
            arg.content = camelize(arg.content);
          } else {
            arg.content = `${context.helperString(CAMELIZE)}(${arg.content})`;
          }
        } else {
          arg.children.unshift(`${context.helperString(CAMELIZE)}(`);
          arg.children.push(`)`);
        }
      }
      if (!context.inSSR) {
        if (modifiers.includes("prop")) {
          injectPrefix(arg, ".");
        }
        if (modifiers.includes("attr")) {
          injectPrefix(arg, "^");
        }
      }
      if (!exp || exp.type === 4 && !exp.content.trim()) {
        context.onError(createCompilerError(34, loc));
        return {
          props: [createObjectProperty(arg, createSimpleExpression("", true, loc))]
        };
      }
      return {
        props: [createObjectProperty(arg, exp)]
      };
    };
    injectPrefix = (arg, prefix) => {
      if (arg.type === 4) {
        if (arg.isStatic) {
          arg.content = prefix + arg.content;
        } else {
          arg.content = `\`${prefix}\${${arg.content}}\``;
        }
      } else {
        arg.children.unshift(`'${prefix}' + (`);
        arg.children.push(`)`);
      }
    };
    transformText = (node, context) => {
      if (node.type === 0 || node.type === 1 || node.type === 11 || node.type === 10) {
        return () => {
          const children = node.children;
          let currentContainer = void 0;
          let hasText = false;
          for (let i = 0; i < children.length; i++) {
            const child = children[i];
            if (isText(child)) {
              hasText = true;
              for (let j = i + 1; j < children.length; j++) {
                const next = children[j];
                if (isText(next)) {
                  if (!currentContainer) {
                    currentContainer = children[i] = {
                      type: 8,
                      loc: child.loc,
                      children: [child]
                    };
                  }
                  currentContainer.children.push(` + `, next);
                  children.splice(j, 1);
                  j--;
                } else {
                  currentContainer = void 0;
                  break;
                }
              }
            }
          }
          if (!hasText || children.length === 1 && (node.type === 0 || node.type === 1 && node.tagType === 0 && !node.props.find((p) => p.type === 7 && !context.directiveTransforms[p.name]) && !(node.tag === "template"))) {
            return;
          }
          for (let i = 0; i < children.length; i++) {
            const child = children[i];
            if (isText(child) || child.type === 8) {
              const callArgs = [];
              if (child.type !== 2 || child.content !== " ") {
                callArgs.push(child);
              }
              if (!context.ssr && getConstantType(child, context) === 0) {
                callArgs.push(1 + (true ? ` /* ${PatchFlagNames[1]} */` : ``));
              }
              children[i] = {
                type: 12,
                content: child,
                loc: child.loc,
                codegenNode: createCallExpression(context.helper(CREATE_TEXT), callArgs)
              };
            }
          }
        };
      }
    };
    seen = new WeakSet();
    transformOnce = (node, context) => {
      if (node.type === 1 && findDir(node, "once", true)) {
        if (seen.has(node) || context.inVOnce) {
          return;
        }
        seen.add(node);
        context.inVOnce = true;
        context.helper(SET_BLOCK_TRACKING);
        return () => {
          context.inVOnce = false;
          const cur = context.currentNode;
          if (cur.codegenNode) {
            cur.codegenNode = context.cache(cur.codegenNode, true);
          }
        };
      }
    };
    transformModel = (dir, node, context) => {
      const { exp, arg } = dir;
      if (!exp) {
        context.onError(createCompilerError(41, dir.loc));
        return createTransformProps();
      }
      const rawExp = exp.loc.source;
      const expString = exp.type === 4 ? exp.content : rawExp;
      context.bindingMetadata[rawExp];
      const maybeRef = false;
      if (!expString.trim() || !isMemberExpression(expString) && !maybeRef) {
        context.onError(createCompilerError(42, exp.loc));
        return createTransformProps();
      }
      const propName = arg ? arg : createSimpleExpression("modelValue", true);
      const eventName = arg ? isStaticExp(arg) ? `onUpdate:${arg.content}` : createCompoundExpression(['"onUpdate:" + ', arg]) : `onUpdate:modelValue`;
      let assignmentExp;
      const eventArg = context.isTS ? `($event: any)` : `$event`;
      {
        assignmentExp = createCompoundExpression([
          `${eventArg} => ((`,
          exp,
          `) = $event)`
        ]);
      }
      const props = [
        createObjectProperty(propName, dir.exp),
        createObjectProperty(eventName, assignmentExp)
      ];
      if (dir.modifiers.length && node.tagType === 1) {
        const modifiers = dir.modifiers.map((m) => (isSimpleIdentifier(m) ? m : JSON.stringify(m)) + `: true`).join(`, `);
        const modifiersKey = arg ? isStaticExp(arg) ? `${arg.content}Modifiers` : createCompoundExpression([arg, ' + "Modifiers"']) : `modelModifiers`;
        props.push(createObjectProperty(modifiersKey, createSimpleExpression(`{ ${modifiers} }`, false, dir.loc, 2)));
      }
      return createTransformProps(props);
    };
    validDivisionCharRE = /[\w).+\-_$\]]/;
    transformFilter = (node, context) => {
      if (!isCompatEnabled("COMPILER_FILTER", context)) {
        return;
      }
      if (node.type === 5) {
        rewriteFilter(node.content, context);
      }
      if (node.type === 1) {
        node.props.forEach((prop) => {
          if (prop.type === 7 && prop.name !== "for" && prop.exp) {
            rewriteFilter(prop.exp, context);
          }
        });
      }
    };
    seen$1 = new WeakSet();
    transformMemo = (node, context) => {
      if (node.type === 1) {
        const dir = findDir(node, "memo");
        if (!dir || seen$1.has(node)) {
          return;
        }
        seen$1.add(node);
        return () => {
          const codegenNode = node.codegenNode || context.currentNode.codegenNode;
          if (codegenNode && codegenNode.type === 13) {
            if (node.tagType !== 1) {
              makeBlock(codegenNode, context);
            }
            node.codegenNode = createCallExpression(context.helper(WITH_MEMO), [
              dir.exp,
              createFunctionExpression(void 0, codegenNode),
              `_cache`,
              String(context.cached++)
            ]);
          }
        };
      }
    };
    noopDirectiveTransform = () => ({ props: [] });
  }
});

// node_modules/@vue/compiler-dom/dist/compiler-dom.esm-bundler.js
var compiler_dom_esm_bundler_exports = {};
__export(compiler_dom_esm_bundler_exports, {
  BASE_TRANSITION: () => BASE_TRANSITION,
  CAMELIZE: () => CAMELIZE,
  CAPITALIZE: () => CAPITALIZE,
  CREATE_BLOCK: () => CREATE_BLOCK,
  CREATE_COMMENT: () => CREATE_COMMENT,
  CREATE_ELEMENT_BLOCK: () => CREATE_ELEMENT_BLOCK,
  CREATE_ELEMENT_VNODE: () => CREATE_ELEMENT_VNODE,
  CREATE_SLOTS: () => CREATE_SLOTS,
  CREATE_STATIC: () => CREATE_STATIC,
  CREATE_TEXT: () => CREATE_TEXT,
  CREATE_VNODE: () => CREATE_VNODE,
  DOMDirectiveTransforms: () => DOMDirectiveTransforms,
  DOMNodeTransforms: () => DOMNodeTransforms,
  FRAGMENT: () => FRAGMENT,
  GUARD_REACTIVE_PROPS: () => GUARD_REACTIVE_PROPS,
  IS_MEMO_SAME: () => IS_MEMO_SAME,
  IS_REF: () => IS_REF,
  KEEP_ALIVE: () => KEEP_ALIVE,
  MERGE_PROPS: () => MERGE_PROPS,
  NORMALIZE_CLASS: () => NORMALIZE_CLASS,
  NORMALIZE_PROPS: () => NORMALIZE_PROPS,
  NORMALIZE_STYLE: () => NORMALIZE_STYLE,
  OPEN_BLOCK: () => OPEN_BLOCK,
  POP_SCOPE_ID: () => POP_SCOPE_ID,
  PUSH_SCOPE_ID: () => PUSH_SCOPE_ID,
  RENDER_LIST: () => RENDER_LIST,
  RENDER_SLOT: () => RENDER_SLOT,
  RESOLVE_COMPONENT: () => RESOLVE_COMPONENT,
  RESOLVE_DIRECTIVE: () => RESOLVE_DIRECTIVE,
  RESOLVE_DYNAMIC_COMPONENT: () => RESOLVE_DYNAMIC_COMPONENT,
  RESOLVE_FILTER: () => RESOLVE_FILTER,
  SET_BLOCK_TRACKING: () => SET_BLOCK_TRACKING,
  SUSPENSE: () => SUSPENSE,
  TELEPORT: () => TELEPORT,
  TO_DISPLAY_STRING: () => TO_DISPLAY_STRING,
  TO_HANDLERS: () => TO_HANDLERS,
  TO_HANDLER_KEY: () => TO_HANDLER_KEY,
  TRANSITION: () => TRANSITION,
  TRANSITION_GROUP: () => TRANSITION_GROUP,
  UNREF: () => UNREF,
  V_MODEL_CHECKBOX: () => V_MODEL_CHECKBOX,
  V_MODEL_DYNAMIC: () => V_MODEL_DYNAMIC,
  V_MODEL_RADIO: () => V_MODEL_RADIO,
  V_MODEL_SELECT: () => V_MODEL_SELECT,
  V_MODEL_TEXT: () => V_MODEL_TEXT,
  V_ON_WITH_KEYS: () => V_ON_WITH_KEYS,
  V_ON_WITH_MODIFIERS: () => V_ON_WITH_MODIFIERS,
  V_SHOW: () => V_SHOW,
  WITH_CTX: () => WITH_CTX,
  WITH_DIRECTIVES: () => WITH_DIRECTIVES,
  WITH_MEMO: () => WITH_MEMO,
  advancePositionWithClone: () => advancePositionWithClone,
  advancePositionWithMutation: () => advancePositionWithMutation,
  assert: () => assert,
  baseCompile: () => baseCompile,
  baseParse: () => baseParse,
  buildProps: () => buildProps,
  buildSlots: () => buildSlots,
  checkCompatEnabled: () => checkCompatEnabled,
  compile: () => compile,
  createArrayExpression: () => createArrayExpression,
  createAssignmentExpression: () => createAssignmentExpression,
  createBlockStatement: () => createBlockStatement,
  createCacheExpression: () => createCacheExpression,
  createCallExpression: () => createCallExpression,
  createCompilerError: () => createCompilerError,
  createCompoundExpression: () => createCompoundExpression,
  createConditionalExpression: () => createConditionalExpression,
  createDOMCompilerError: () => createDOMCompilerError,
  createForLoopParams: () => createForLoopParams,
  createFunctionExpression: () => createFunctionExpression,
  createIfStatement: () => createIfStatement,
  createInterpolation: () => createInterpolation,
  createObjectExpression: () => createObjectExpression,
  createObjectProperty: () => createObjectProperty,
  createReturnStatement: () => createReturnStatement,
  createRoot: () => createRoot,
  createSequenceExpression: () => createSequenceExpression,
  createSimpleExpression: () => createSimpleExpression,
  createStructuralDirectiveTransform: () => createStructuralDirectiveTransform,
  createTemplateLiteral: () => createTemplateLiteral,
  createTransformContext: () => createTransformContext,
  createVNodeCall: () => createVNodeCall,
  extractIdentifiers: () => extractIdentifiers,
  findDir: () => findDir,
  findProp: () => findProp,
  generate: () => generate,
  generateCodeFrame: () => generateCodeFrame,
  getBaseTransformPreset: () => getBaseTransformPreset,
  getInnerRange: () => getInnerRange,
  getMemoedVNodeCall: () => getMemoedVNodeCall,
  getVNodeBlockHelper: () => getVNodeBlockHelper,
  getVNodeHelper: () => getVNodeHelper,
  hasDynamicKeyVBind: () => hasDynamicKeyVBind,
  hasScopeRef: () => hasScopeRef,
  helperNameMap: () => helperNameMap,
  injectProp: () => injectProp,
  isBuiltInType: () => isBuiltInType,
  isCoreComponent: () => isCoreComponent,
  isFunctionType: () => isFunctionType,
  isInDestructureAssignment: () => isInDestructureAssignment,
  isMemberExpression: () => isMemberExpression,
  isMemberExpressionBrowser: () => isMemberExpressionBrowser,
  isMemberExpressionNode: () => isMemberExpressionNode,
  isReferencedIdentifier: () => isReferencedIdentifier,
  isSimpleIdentifier: () => isSimpleIdentifier,
  isSlotOutlet: () => isSlotOutlet,
  isStaticArgOf: () => isStaticArgOf,
  isStaticExp: () => isStaticExp,
  isStaticProperty: () => isStaticProperty,
  isStaticPropertyKey: () => isStaticPropertyKey,
  isTemplateNode: () => isTemplateNode,
  isText: () => isText,
  isVSlot: () => isVSlot,
  locStub: () => locStub,
  makeBlock: () => makeBlock,
  noopDirectiveTransform: () => noopDirectiveTransform,
  parse: () => parse,
  parserOptions: () => parserOptions,
  processExpression: () => processExpression,
  processFor: () => processFor,
  processIf: () => processIf,
  processSlotOutlet: () => processSlotOutlet,
  registerRuntimeHelpers: () => registerRuntimeHelpers,
  resolveComponentType: () => resolveComponentType,
  toValidAssetId: () => toValidAssetId,
  trackSlotScopes: () => trackSlotScopes,
  trackVForSlotScopes: () => trackVForSlotScopes,
  transform: () => transform,
  transformBind: () => transformBind,
  transformElement: () => transformElement,
  transformExpression: () => transformExpression,
  transformModel: () => transformModel,
  transformOn: () => transformOn,
  transformStyle: () => transformStyle,
  traverseNode: () => traverseNode,
  walkBlockDeclarations: () => walkBlockDeclarations,
  walkFunctionParams: () => walkFunctionParams,
  walkIdentifiers: () => walkIdentifiers,
  warnDeprecation: () => warnDeprecation
});
function decodeHtmlBrowser(raw, asAttr = false) {
  if (!decoder) {
    decoder = document.createElement("div");
  }
  if (asAttr) {
    decoder.innerHTML = `<div foo="${raw.replace(/"/g, "&quot;")}">`;
    return decoder.children[0].getAttribute("foo");
  } else {
    decoder.innerHTML = raw;
    return decoder.textContent;
  }
}
function createDOMCompilerError(code, loc) {
  return createCompilerError(code, loc, true ? DOMErrorMessages : void 0);
}
function hasMultipleChildren(node) {
  const children = node.children = node.children.filter((c) => c.type !== 3 && !(c.type === 2 && !c.content.trim()));
  const child = children[0];
  return children.length !== 1 || child.type === 11 || child.type === 9 && child.branches.some(hasMultipleChildren);
}
function compile(template, options = {}) {
  return baseCompile(template, extend({}, parserOptions, options, {
    nodeTransforms: [
      ignoreSideEffectTags,
      ...DOMNodeTransforms,
      ...options.nodeTransforms || []
    ],
    directiveTransforms: extend({}, DOMDirectiveTransforms, options.directiveTransforms || {}),
    transformHoist: null
  }));
}
function parse(template, options = {}) {
  return baseParse(template, extend({}, parserOptions, options));
}
var V_MODEL_RADIO, V_MODEL_CHECKBOX, V_MODEL_TEXT, V_MODEL_SELECT, V_MODEL_DYNAMIC, V_ON_WITH_MODIFIERS, V_ON_WITH_KEYS, V_SHOW, TRANSITION, TRANSITION_GROUP, decoder, isRawTextContainer, parserOptions, transformStyle, parseInlineCSS, DOMErrorMessages, transformVHtml, transformVText, transformModel2, isEventOptionModifier, isNonKeyModifier, maybeKeyModifier, isKeyboardEvent, resolveModifiers, transformClick, transformOn2, transformShow, warnTransitionChildren, ignoreSideEffectTags, DOMNodeTransforms, DOMDirectiveTransforms;
var init_compiler_dom_esm_bundler = __esm({
  "node_modules/@vue/compiler-dom/dist/compiler-dom.esm-bundler.js"() {
    init_compiler_core_esm_bundler();
    init_compiler_core_esm_bundler();
    init_shared_esm_bundler();
    V_MODEL_RADIO = Symbol(true ? `vModelRadio` : ``);
    V_MODEL_CHECKBOX = Symbol(true ? `vModelCheckbox` : ``);
    V_MODEL_TEXT = Symbol(true ? `vModelText` : ``);
    V_MODEL_SELECT = Symbol(true ? `vModelSelect` : ``);
    V_MODEL_DYNAMIC = Symbol(true ? `vModelDynamic` : ``);
    V_ON_WITH_MODIFIERS = Symbol(true ? `vOnModifiersGuard` : ``);
    V_ON_WITH_KEYS = Symbol(true ? `vOnKeysGuard` : ``);
    V_SHOW = Symbol(true ? `vShow` : ``);
    TRANSITION = Symbol(true ? `Transition` : ``);
    TRANSITION_GROUP = Symbol(true ? `TransitionGroup` : ``);
    registerRuntimeHelpers({
      [V_MODEL_RADIO]: `vModelRadio`,
      [V_MODEL_CHECKBOX]: `vModelCheckbox`,
      [V_MODEL_TEXT]: `vModelText`,
      [V_MODEL_SELECT]: `vModelSelect`,
      [V_MODEL_DYNAMIC]: `vModelDynamic`,
      [V_ON_WITH_MODIFIERS]: `withModifiers`,
      [V_ON_WITH_KEYS]: `withKeys`,
      [V_SHOW]: `vShow`,
      [TRANSITION]: `Transition`,
      [TRANSITION_GROUP]: `TransitionGroup`
    });
    isRawTextContainer = makeMap("style,iframe,script,noscript", true);
    parserOptions = {
      isVoidTag,
      isNativeTag: (tag) => isHTMLTag(tag) || isSVGTag(tag),
      isPreTag: (tag) => tag === "pre",
      decodeEntities: decodeHtmlBrowser,
      isBuiltInComponent: (tag) => {
        if (isBuiltInType(tag, `Transition`)) {
          return TRANSITION;
        } else if (isBuiltInType(tag, `TransitionGroup`)) {
          return TRANSITION_GROUP;
        }
      },
      getNamespace(tag, parent) {
        let ns = parent ? parent.ns : 0;
        if (parent && ns === 2) {
          if (parent.tag === "annotation-xml") {
            if (tag === "svg") {
              return 1;
            }
            if (parent.props.some((a) => a.type === 6 && a.name === "encoding" && a.value != null && (a.value.content === "text/html" || a.value.content === "application/xhtml+xml"))) {
              ns = 0;
            }
          } else if (/^m(?:[ions]|text)$/.test(parent.tag) && tag !== "mglyph" && tag !== "malignmark") {
            ns = 0;
          }
        } else if (parent && ns === 1) {
          if (parent.tag === "foreignObject" || parent.tag === "desc" || parent.tag === "title") {
            ns = 0;
          }
        }
        if (ns === 0) {
          if (tag === "svg") {
            return 1;
          }
          if (tag === "math") {
            return 2;
          }
        }
        return ns;
      },
      getTextMode({ tag, ns }) {
        if (ns === 0) {
          if (tag === "textarea" || tag === "title") {
            return 1;
          }
          if (isRawTextContainer(tag)) {
            return 2;
          }
        }
        return 0;
      }
    };
    transformStyle = (node) => {
      if (node.type === 1) {
        node.props.forEach((p, i) => {
          if (p.type === 6 && p.name === "style" && p.value) {
            node.props[i] = {
              type: 7,
              name: `bind`,
              arg: createSimpleExpression(`style`, true, p.loc),
              exp: parseInlineCSS(p.value.content, p.loc),
              modifiers: [],
              loc: p.loc
            };
          }
        });
      }
    };
    parseInlineCSS = (cssText, loc) => {
      const normalized = parseStringStyle(cssText);
      return createSimpleExpression(JSON.stringify(normalized), false, loc, 3);
    };
    DOMErrorMessages = {
      [50]: `v-html is missing expression.`,
      [51]: `v-html will override element children.`,
      [52]: `v-text is missing expression.`,
      [53]: `v-text will override element children.`,
      [54]: `v-model can only be used on <input>, <textarea> and <select> elements.`,
      [55]: `v-model argument is not supported on plain elements.`,
      [56]: `v-model cannot be used on file inputs since they are read-only. Use a v-on:change listener instead.`,
      [57]: `Unnecessary value binding used alongside v-model. It will interfere with v-model's behavior.`,
      [58]: `v-show is missing expression.`,
      [59]: `<Transition> expects exactly one child element or component.`,
      [60]: `Tags with side effect (<script> and <style>) are ignored in client component templates.`
    };
    transformVHtml = (dir, node, context) => {
      const { exp, loc } = dir;
      if (!exp) {
        context.onError(createDOMCompilerError(50, loc));
      }
      if (node.children.length) {
        context.onError(createDOMCompilerError(51, loc));
        node.children.length = 0;
      }
      return {
        props: [
          createObjectProperty(createSimpleExpression(`innerHTML`, true, loc), exp || createSimpleExpression("", true))
        ]
      };
    };
    transformVText = (dir, node, context) => {
      const { exp, loc } = dir;
      if (!exp) {
        context.onError(createDOMCompilerError(52, loc));
      }
      if (node.children.length) {
        context.onError(createDOMCompilerError(53, loc));
        node.children.length = 0;
      }
      return {
        props: [
          createObjectProperty(createSimpleExpression(`textContent`, true), exp ? createCallExpression(context.helperString(TO_DISPLAY_STRING), [exp], loc) : createSimpleExpression("", true))
        ]
      };
    };
    transformModel2 = (dir, node, context) => {
      const baseResult = transformModel(dir, node, context);
      if (!baseResult.props.length || node.tagType === 1) {
        return baseResult;
      }
      if (dir.arg) {
        context.onError(createDOMCompilerError(55, dir.arg.loc));
      }
      function checkDuplicatedValue() {
        const value = findProp(node, "value");
        if (value) {
          context.onError(createDOMCompilerError(57, value.loc));
        }
      }
      const { tag } = node;
      const isCustomElement = context.isCustomElement(tag);
      if (tag === "input" || tag === "textarea" || tag === "select" || isCustomElement) {
        let directiveToUse = V_MODEL_TEXT;
        let isInvalidType = false;
        if (tag === "input" || isCustomElement) {
          const type = findProp(node, `type`);
          if (type) {
            if (type.type === 7) {
              directiveToUse = V_MODEL_DYNAMIC;
            } else if (type.value) {
              switch (type.value.content) {
                case "radio":
                  directiveToUse = V_MODEL_RADIO;
                  break;
                case "checkbox":
                  directiveToUse = V_MODEL_CHECKBOX;
                  break;
                case "file":
                  isInvalidType = true;
                  context.onError(createDOMCompilerError(56, dir.loc));
                  break;
                default:
                  checkDuplicatedValue();
                  break;
              }
            }
          } else if (hasDynamicKeyVBind(node)) {
            directiveToUse = V_MODEL_DYNAMIC;
          } else {
            checkDuplicatedValue();
          }
        } else if (tag === "select") {
          directiveToUse = V_MODEL_SELECT;
        } else {
          checkDuplicatedValue();
        }
        if (!isInvalidType) {
          baseResult.needRuntime = context.helper(directiveToUse);
        }
      } else {
        context.onError(createDOMCompilerError(54, dir.loc));
      }
      baseResult.props = baseResult.props.filter((p) => !(p.key.type === 4 && p.key.content === "modelValue"));
      return baseResult;
    };
    isEventOptionModifier = makeMap(`passive,once,capture`);
    isNonKeyModifier = makeMap(`stop,prevent,self,ctrl,shift,alt,meta,exact,middle`);
    maybeKeyModifier = makeMap("left,right");
    isKeyboardEvent = makeMap(`onkeyup,onkeydown,onkeypress`, true);
    resolveModifiers = (key, modifiers, context, loc) => {
      const keyModifiers = [];
      const nonKeyModifiers = [];
      const eventOptionModifiers = [];
      for (let i = 0; i < modifiers.length; i++) {
        const modifier = modifiers[i];
        if (modifier === "native" && checkCompatEnabled("COMPILER_V_ON_NATIVE", context, loc)) {
          eventOptionModifiers.push(modifier);
        } else if (isEventOptionModifier(modifier)) {
          eventOptionModifiers.push(modifier);
        } else {
          if (maybeKeyModifier(modifier)) {
            if (isStaticExp(key)) {
              if (isKeyboardEvent(key.content)) {
                keyModifiers.push(modifier);
              } else {
                nonKeyModifiers.push(modifier);
              }
            } else {
              keyModifiers.push(modifier);
              nonKeyModifiers.push(modifier);
            }
          } else {
            if (isNonKeyModifier(modifier)) {
              nonKeyModifiers.push(modifier);
            } else {
              keyModifiers.push(modifier);
            }
          }
        }
      }
      return {
        keyModifiers,
        nonKeyModifiers,
        eventOptionModifiers
      };
    };
    transformClick = (key, event) => {
      const isStaticClick = isStaticExp(key) && key.content.toLowerCase() === "onclick";
      return isStaticClick ? createSimpleExpression(event, true) : key.type !== 4 ? createCompoundExpression([
        `(`,
        key,
        `) === "onClick" ? "${event}" : (`,
        key,
        `)`
      ]) : key;
    };
    transformOn2 = (dir, node, context) => {
      return transformOn(dir, node, context, (baseResult) => {
        const { modifiers } = dir;
        if (!modifiers.length)
          return baseResult;
        let { key, value: handlerExp } = baseResult.props[0];
        const { keyModifiers, nonKeyModifiers, eventOptionModifiers } = resolveModifiers(key, modifiers, context, dir.loc);
        if (nonKeyModifiers.includes("right")) {
          key = transformClick(key, `onContextmenu`);
        }
        if (nonKeyModifiers.includes("middle")) {
          key = transformClick(key, `onMouseup`);
        }
        if (nonKeyModifiers.length) {
          handlerExp = createCallExpression(context.helper(V_ON_WITH_MODIFIERS), [
            handlerExp,
            JSON.stringify(nonKeyModifiers)
          ]);
        }
        if (keyModifiers.length && (!isStaticExp(key) || isKeyboardEvent(key.content))) {
          handlerExp = createCallExpression(context.helper(V_ON_WITH_KEYS), [
            handlerExp,
            JSON.stringify(keyModifiers)
          ]);
        }
        if (eventOptionModifiers.length) {
          const modifierPostfix = eventOptionModifiers.map(capitalize).join("");
          key = isStaticExp(key) ? createSimpleExpression(`${key.content}${modifierPostfix}`, true) : createCompoundExpression([`(`, key, `) + "${modifierPostfix}"`]);
        }
        return {
          props: [createObjectProperty(key, handlerExp)]
        };
      });
    };
    transformShow = (dir, node, context) => {
      const { exp, loc } = dir;
      if (!exp) {
        context.onError(createDOMCompilerError(58, loc));
      }
      return {
        props: [],
        needRuntime: context.helper(V_SHOW)
      };
    };
    warnTransitionChildren = (node, context) => {
      if (node.type === 1 && node.tagType === 1) {
        const component = context.isBuiltInComponent(node.tag);
        if (component === TRANSITION) {
          return () => {
            if (node.children.length && hasMultipleChildren(node)) {
              context.onError(createDOMCompilerError(59, {
                start: node.children[0].loc.start,
                end: node.children[node.children.length - 1].loc.end,
                source: ""
              }));
            }
          };
        }
      }
    };
    ignoreSideEffectTags = (node, context) => {
      if (node.type === 1 && node.tagType === 0 && (node.tag === "script" || node.tag === "style")) {
        context.onError(createDOMCompilerError(60, node.loc));
        context.removeNode();
      }
    };
    DOMNodeTransforms = [
      transformStyle,
      ...true ? [warnTransitionChildren] : []
    ];
    DOMDirectiveTransforms = {
      cloak: noopDirectiveTransform,
      html: transformVHtml,
      text: transformVText,
      model: transformModel2,
      on: transformOn2,
      show: transformShow
    };
  }
});

// node_modules/vue/dist/vue.cjs.js
var require_vue_cjs = __commonJS({
  "node_modules/vue/dist/vue.cjs.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var compilerDom = (init_compiler_dom_esm_bundler(), compiler_dom_esm_bundler_exports);
    var runtimeDom = (init_runtime_dom_esm_bundler(), runtime_dom_esm_bundler_exports);
    var shared = (init_shared_esm_bundler(), shared_esm_bundler_exports);
    function _interopNamespace(e) {
      if (e && e.__esModule)
        return e;
      var n = Object.create(null);
      if (e) {
        Object.keys(e).forEach(function(k) {
          n[k] = e[k];
        });
      }
      n["default"] = e;
      return Object.freeze(n);
    }
    var runtimeDom__namespace = _interopNamespace(runtimeDom);
    var compileCache = Object.create(null);
    function compileToFunction(template, options) {
      if (!shared.isString(template)) {
        if (template.nodeType) {
          template = template.innerHTML;
        } else {
          runtimeDom.warn(`invalid template option: `, template);
          return shared.NOOP;
        }
      }
      const key = template;
      const cached = compileCache[key];
      if (cached) {
        return cached;
      }
      if (template[0] === "#") {
        const el = document.querySelector(template);
        if (!el) {
          runtimeDom.warn(`Template element not found or is empty: ${template}`);
        }
        template = el ? el.innerHTML : ``;
      }
      const { code } = compilerDom.compile(template, shared.extend({
        hoistStatic: true,
        onError,
        onWarn: (e) => onError(e, true)
      }, options));
      function onError(err, asWarning = false) {
        const message = asWarning ? err.message : `Template compilation error: ${err.message}`;
        const codeFrame = err.loc && shared.generateCodeFrame(template, err.loc.start.offset, err.loc.end.offset);
        runtimeDom.warn(codeFrame ? `${message}
${codeFrame}` : message);
      }
      const render = new Function("Vue", code)(runtimeDom__namespace);
      render._rc = true;
      return compileCache[key] = render;
    }
    runtimeDom.registerRuntimeCompiler(compileToFunction);
    Object.keys(runtimeDom).forEach(function(k) {
      if (k !== "default")
        exports[k] = runtimeDom[k];
    });
    exports.compile = compileToFunction;
  }
});

// node_modules/vue/index.js
var require_vue = __commonJS({
  "node_modules/vue/index.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_vue_cjs();
    }
  }
});

// node_modules/nplayer/dist/index.min.js
var require_index_min = __commonJS({
  "node_modules/nplayer/dist/index.min.js"(exports, module) {
    !function(t, e) {
      typeof exports == "object" && typeof module == "object" ? module.exports = e() : typeof define == "function" && define.amd ? define("NPlayer", [], e) : typeof exports == "object" ? exports.NPlayer = e() : t.NPlayer = e();
    }(exports, function() {
      return function() {
        var t = { 14: function(t2, e2, n2) {
          "use strict";
          n2.r(e2);
          var o2 = n2(645), r = n2.n(o2), i = n2(667), a = n2.n(i), l = new URL(n2(494), n2.b), c = r()(function(t3) {
            return t3[1];
          }), u = a()(l);
          c.push([t2.id, '.nplayer{--theme-color:#007aff;--poster-bg-color:transparent;--progress-bg:#007aff;--volume-progress-bg:#007aff;--safe-area-inset-bottom:$control_bottom;-webkit-font-smoothing:antialiased;-moz-osx-font-smoothing:grayscale;-webkit-tap-highlight-color:transparent;align-items:center;background:#000;color:#fff;display:flex;font-family:-apple-system,BlinkMacSystemFont,Microsoft YaHei;height:100%;justify-content:center;position:relative;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none;width:100%;z-index:auto}@supports(padding-bottom:constant(safe-area-inset-bottom)){.nplayer{--safe-area-inset-bottom:max(8px,constant(safe-area-inset-bottom))}}@supports(padding-bottom:env(safe-area-inset-bottom)){.nplayer{--safe-area-inset-bottom:max(8px,env(safe-area-inset-bottom))}}.nplayer *{box-sizing:content-box}.nplayer_control{z-index:10}.nplayer_control_bg{z-index:9}.nplayer_poster{z-index:20}.nplayer_loading{z-index:30}.nplayer_contextmenu{z-index:40}.nplayer_toast{z-index:50}.nplayer:focus{outline:none}.nplayer_flex{display:flex}.nplayer_align-center{align-items:center}.nplayer-web-full{bottom:0;left:0;position:fixed;right:0;top:0;z-index:99999}.nplayer-web-full .nplayer_control{padding-bottom:var(--safe-area-inset-bottom)}.nplayer-loading .nplayer_loading{display:block}.nplayer_icon{fill:#fff;height:28px;width:28px}.nplayer_video{max-height:100%;outline:none;touch-action:none;width:100%}.nplayer_spacer{flex:1}.nplayer_tooltip{cursor:pointer;padding:3px;position:relative;transition:background .2s ease}@media not all and (hover:none){.nplayer_tooltip:hover{background:var(--theme-color)}.nplayer_tooltip:hover .nplayer_tooltip_content{opacity:1;transform:translate(-50%)}.nplayer_tooltip:hover .nplayer_tooltip-left,.nplayer_tooltip:hover .nplayer_tooltip-right{transform:translate(0)}}.nplayer_tooltip_content{background:rgba(0,0,0,.7);bottom:100%;font-size:12px;left:50%;margin-bottom:10px;opacity:0;padding:5px 10px;pointer-events:none;position:absolute;transform:translate(-50%,10px) scale(.8);transform-origin:50% 100%;transition:transform .2s ease .1s,opacity .2s ease .1s;white-space:nowrap}.nplayer_tooltip-left{left:0;transform:translateY(10px) scale(.8);transform-origin:0 100%}.nplayer_tooltip-right{left:auto;right:0;transform:translateY(10px) scale(.8);transform-origin:100% 100%}.nplayer_tooltip-bottom{bottom:auto;margin-bottom:0;margin-top:10px;top:100%;transform:translate(-50%,-10px) scale(.8)}.nplayer_tooltip-bottom.nplayer_tooltip-left,.nplayer_tooltip-bottom.nplayer_tooltip-right{transform:translateY(-10px) scale(.8)}.nplayer_popover{pointer-events:none}.nplayer_popover-active{pointer-events:auto}.nplayer_popover-active .nplayer_popover_panel{opacity:1;transform:translateY(0)}.nplayer_popover_panel{background:rgba(0,0,0,.7);bottom:100%;box-shadow:0 1px 20px 1px rgba(0,0,0,.2);font-size:14px;margin-bottom:5px;opacity:0;overflow:hidden;padding:3px 0;position:absolute;right:0;transform:translateY(10px);transition:opacity .2s ease,transform .2s ease,height .2s cubic-bezier(.4,0,.2,1),width .2s cubic-bezier(.4,0,.2,1);white-space:nowrap;z-index:2}.nplayer_popover_panel-bottom{bottom:auto;margin-bottom:0;margin-top:5px;top:100%;transform:translateY(-10px)}.nplayer_popover_panel-left{left:0;right:auto}.nplayer_popover_mask{bottom:0;left:0;position:fixed;right:0;top:0;z-index:1}.nplayer_switch{background:rgba(255,255,255,.3);border-radius:10px;cursor:pointer;display:inline-block;padding:2px;transition:background-color .3s cubic-bezier(.4,0,.2,1);-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none;width:30px}.nplayer_switch:after{background:#fff;border-radius:50%;box-shadow:0 2px 1px -1px rgba(0,0,0,.2),0 1px 1px 0 rgba(0,0,0,.14),0 1px 3px 0 rgba(0,0,0,.12);content:"";display:block;height:15px;transition:transform .15s cubic-bezier(.4,0,.2,1);width:15px}.nplayer_switch-active{background:var(--theme-color)}.nplayer_switch-active:after{transform:translateX(16px)}.nplayer_loading{display:none;height:70px;left:50%;pointer-events:none;position:absolute;top:50%;transform:translate(-50%,-50%);width:70px}.nplayer_loading i{-webkit-animation:nplayer-loading 1s linear infinite;animation:nplayer-loading 1s linear infinite;background-color:#fff;border-radius:50%/20%;height:25%;left:44.5%;opacity:.25;position:absolute;top:37%;width:5px}.nplayer_loading i:first-child{-webkit-animation-delay:0s;animation-delay:0s;transform:rotate(-150deg) translateY(-150%)}.nplayer_loading i:nth-child(2){-webkit-animation-delay:.0833s;animation-delay:.0833s;transform:rotate(-120deg) translateY(-150%)}.nplayer_loading i:nth-child(3){-webkit-animation-delay:.1666s;animation-delay:.1666s;transform:rotate(-90deg) translateY(-150%)}.nplayer_loading i:nth-child(4){-webkit-animation-delay:.2499s;animation-delay:.2499s;transform:rotate(-60deg) translateY(-150%)}.nplayer_loading i:nth-child(5){-webkit-animation-delay:.3332s;animation-delay:.3332s;transform:rotate(-30deg) translateY(-150%)}.nplayer_loading i:nth-child(6){-webkit-animation-delay:.4165s;animation-delay:.4165s;transform:rotate(0deg) translateY(-150%)}.nplayer_loading i:nth-child(7){-webkit-animation-delay:.4998s;animation-delay:.4998s;transform:rotate(30deg) translateY(-150%)}.nplayer_loading i:nth-child(8){-webkit-animation-delay:.5831s;animation-delay:.5831s;transform:rotate(60deg) translateY(-150%)}.nplayer_loading i:nth-child(9){-webkit-animation-delay:.6664s;animation-delay:.6664s;transform:rotate(90deg) translateY(-150%)}.nplayer_loading i:nth-child(10){-webkit-animation-delay:.7497s;animation-delay:.7497s;transform:rotate(120deg) translateY(-150%)}.nplayer_loading i:nth-child(11){-webkit-animation-delay:.833s;animation-delay:.833s;transform:rotate(150deg) translateY(-150%)}.nplayer_loading i:nth-child(12){-webkit-animation-delay:.9163s;animation-delay:.9163s;transform:rotate(180deg) translateY(-150%)}.nplayer_toast{background:rgba(0,0,0,.7);font-size:16px;padding:3px 5px;position:absolute;white-space:nowrap}.nplayer_toast-center{left:50%;top:50%;transform:translate(-50%,-50%)}.nplayer_toast-left-top,.nplayer_toast-right-top{top:5px}.nplayer_toast-left-bottom,.nplayer_toast-left-top{left:5px}.nplayer_toast-right-bottom,.nplayer_toast-right-top{right:5px}.nplayer_toast-left-bottom,.nplayer_toast-right-bottom{bottom:60px}.nplayer_slider{cursor:pointer;display:inline-block;flex:1;margin:0 6px;padding:5px 0;position:relative;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none}.nplayer_slider_track{background:rgba(255,255,255,.3);height:4px;overflow:hidden;width:100%}.nplayer_slider_track_inner{background:var(--theme-color);height:100%;transform-origin:0 0}.nplayer_slider_stop{background:rgba(255,255,255,.7);font-size:12px;height:8px;position:absolute;top:3px;white-space:nowrap;width:3px}.nplayer_slider_stop span{left:2px;position:absolute;top:6px;transform:translateX(-50%)}.nplayer_slider_dot{background:var(--theme-color);border-radius:50%;height:13px;left:-6px;pointer-events:none;position:absolute;top:.5px;width:13px}.nplayer_checkbox{align-items:center;color:#fff;cursor:pointer;display:inline-flex;font-size:12px;position:relative;transition:color .2s ease;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none}.nplayer_checkbox:before{border:1px solid;border-radius:2px;content:"";display:block;height:1em;margin-right:5px;transition:background .2s ease;width:1em}.nplayer_checkbox:after{border:1px solid #fff;border-left:0;border-top:0;content:"";height:7px;left:.5em;opacity:0;position:absolute;top:50%;transform:translate(-30%,-70%) rotate(45deg);width:4px}.nplayer_checkbox:hover{color:var(--theme-color)}.nplayer_checkbox-active:before{background:var(--theme-color);border-color:var(--theme-color)}.nplayer_checkbox-active:after{opacity:1}.nplayer_poster{background-color:var(--poster-bg-color);background-position:50% 50%;background-repeat:no-repeat;background-size:cover;bottom:0;left:0;position:absolute;right:0;top:0}.nplayer_poster_play{cursor:pointer;height:50px;left:50%;position:absolute;top:50%;transform:translate(-50%,-50%);width:50px}.nplayer_control{bottom:0;left:0;padding:0 5px 8px;position:absolute;right:0;transition:opacity .2s ease;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none}.nplayer_control-hide{opacity:0;pointer-events:none}.nplayer_control:hover{cursor:default!important;opacity:1!important;pointer-events:auto!important}.nplayer_control:hover~.nplayer_control_bg{opacity:1!important}.nplayer_control_bg{background:url(' + u + ') repeat-x bottom;bottom:0;height:100px;left:0;pointer-events:none;position:absolute;right:0;transition:opacity .2s ease}.nplayer_control_bg-hide{opacity:0}.nplayer_control_bar{align-items:center;display:flex;font-size:0}.nplayer_control_bar-top{left:0;padding-top:5px;position:absolute;right:0;top:0;transition:opacity .2s ease;z-index:10}.nplayer_control_bar-hide{opacity:0;pointer-events:none}.nplayer_control_bar>div{margin:0 3px}.nplayer_control_time{font-size:13px;white-space:nowrap}.nplayer_control_time-live{align-items:center;display:flex}.nplayer_control_time-live:before{background:var(--theme-color);border-radius:50%;content:"";display:block;height:10px;margin-right:5px;margin-top:2px;width:10px}.nplayer_control_time :first-child:after{content:"/";display:inline-block;font-size:12px;margin:0 5px;transform:translateY(-.5px)}.nplayer_control_volume{position:relative;z-index:1}.nplayer_control_volume-ver .nplayer_control_volume_bars{bottom:100%;left:0;right:0}.nplayer_control_volume-hor .nplayer_control_volume_bars{bottom:0;left:100%;top:0}.nplayer_control_volume_bars{background:rgba(255,255,255,.3);font-size:0;opacity:0;overflow:hidden;pointer-events:none;position:absolute;transition:opacity .2s ease}.nplayer_control_volume:hover .nplayer_control_volume_bars{opacity:1;pointer-events:auto}.nplayer_control_volume_bar{background:var(--volume-progress-bg);height:100%;transform:scaleX(0) scaleY(0);transform-origin:0 100%;width:100%;will-change:transform}.nplayer_control_setting .nplayer_icon{transition:transform .2s ease}.nplayer_control_setting-active{background:var(--theme-color)}.nplayer_control_setting-active .nplayer_icon{transform:rotate(90deg)}.nplayer_control_setting_item,.nplayer_control_setting_option{align-items:center;display:flex;padding:5px 8px}.nplayer_control_setting_item:hover,.nplayer_control_setting_option:hover{background:rgba(255,255,255,.2)}.nplayer_control_setting_item{justify-content:space-between}.nplayer_control_setting_item>:first-child{margin-right:20px}.nplayer_control_setting_item-select:after{border:4px solid transparent;border-left-color:#fff;content:"";display:block;height:0;margin-left:10px;width:0}.nplayer_control_setting_option:before{border-bottom:1px solid #fff;border-right:1px solid #fff;content:"";display:inline-block;height:12px;margin-left:6px;margin-right:17px;opacity:0;transform:rotate(45deg);width:5px}.nplayer_control_setting_option-active:before{opacity:1}.nplayer_control_setting_back{border-bottom:1px solid rgba(255,255,255,.2);justify-content:flex-start;padding-right:20px}.nplayer_control_setting_back:before{border:4px solid transparent;border-right-color:#fff;content:"";display:block;height:0;margin-right:21px;width:0}.nplayer_contextmenu{background:rgba(0,0,0,.7);font-size:14px;left:0;padding:3px 0;position:absolute;top:0}.nplayer_contextmenu_item{align-items:center;cursor:pointer;display:flex;justify-content:space-between;padding:5px 8px;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none;white-space:nowrap}.nplayer_contextmenu_item:hover{background:rgba(255,255,255,.2)}.nplayer_contextmenu_item-disabled{color:rgba(255,255,255,.5)}.nplayer_contextmenu_item-disabled:hover{background:transparent}.nplayer_contextmenu_item-checked:after{border-bottom:1px solid #fff;border-right:1px solid #fff;content:"";display:inline-block;height:12px;margin:0 2px 5px;transform:rotate(45deg);width:5px}.nplayer_progress{cursor:pointer;flex:1;padding:5px 0;position:relative}.nplayer_progress_bars{background:rgba(255,255,255,.3);height:4px;overflow:hidden;position:relative;touch-action:none;transition:height .12s ease}.nplayer_progress_dot{left:0;pointer-events:none;position:absolute;top:50%;transform:translateY(-50%);transition:opacity .2s ease}.nplayer_progress_dot>:first-child{transform:translate(-50%)}.nplayer_progress_dot_inner{background:var(--theme-color);border-radius:50%;height:13px;width:13px}@media not all and (hover:none){.nplayer_progress_dot{opacity:0}.nplayer_progress:hover .nplayer_progress_dot{opacity:1}}.nplayer_progress_buf,.nplayer_progress_played{bottom:0;left:0;position:absolute;right:0;top:0;transform:scaleX(0);transform-origin:0 0}.nplayer_progress_played{background:var(--progress-bg);will-change:transform}.nplayer_progress_buf,.nplayer_progress_hover{background:rgba(255,255,255,.5)}.nplayer_progress_hover{opacity:0}.nplayer_progress:hover .nplayer_thumb{display:block}.nplayer_thumb{bottom:20px;display:none;font-size:12px;left:0;pointer-events:none;position:absolute;text-align:center;text-shadow:0 0 2px rgba(0,0,0,.3)}.nplayer_thumb_img{background-color:rgba(255,255,255,.5);background-repeat:no-repeat;border:1px solid #fff;box-shadow:0 0 10px rgba(0,0,0,.1)}.nplayer_thumb_time{background:rgba(0,0,0,.7);border-radius:3px;display:inline-block;margin-top:5px;padding:2px 5px 3px}@-webkit-keyframes nplayer-loading{0%{opacity:.85}50%{opacity:.25}to{opacity:.25}}@keyframes nplayer-loading{0%{opacity:.85}50%{opacity:.25}to{opacity:.25}}', ""]), e2.default = c;
        }, 645: function(t2) {
          "use strict";
          t2.exports = function(t3) {
            var e2 = [];
            return e2.toString = function() {
              return this.map(function(e3) {
                var n2 = t3(e3);
                return e3[2] ? "@media ".concat(e3[2], " {").concat(n2, "}") : n2;
              }).join("");
            }, e2.i = function(t4, n2, o2) {
              typeof t4 == "string" && (t4 = [[null, t4, ""]]);
              var r = {};
              if (o2)
                for (var i = 0; i < this.length; i++) {
                  var a = this[i][0];
                  a != null && (r[a] = true);
                }
              for (var l = 0; l < t4.length; l++) {
                var c = [].concat(t4[l]);
                o2 && r[c[0]] || (n2 && (c[2] ? c[2] = "".concat(n2, " and ").concat(c[2]) : c[2] = n2), e2.push(c));
              }
            }, e2;
          };
        }, 667: function(t2) {
          "use strict";
          t2.exports = function(t3, e2) {
            return e2 || (e2 = {}), t3 ? (t3 = String(t3.__esModule ? t3.default : t3), /^['"].*['"]$/.test(t3) && (t3 = t3.slice(1, -1)), e2.hash && (t3 += e2.hash), /["'() \t\n]|(%20)/.test(t3) || e2.needQuotes ? '"'.concat(t3.replace(/"/g, '\\"').replace(/\n/g, "\\n"), '"') : t3) : t3;
          };
        }, 4: function(t2, e2, n2) {
          var o2;
          typeof window != "undefined" && window.document && function() {
            var t3 = n2(14);
            t3.__esModule && (t3 = t3.default);
            var e3 = typeof t3 == "string" ? t3 : t3[0][1], r = false;
            o2 || ((o2 = document.createElement("style")).setAttribute("type", "text/css"), r = true), "textContent" in o2 ? o2.textContent = e3 : o2.styleSheet.cssText = e3, r && document.head.appendChild(o2);
          }();
        }, 494: function(t2) {
          "use strict";
          t2.exports = "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAADGCAYAAAAT+OqFAAAAdklEQVQoz42QQQ7AIAgEF/T/D+kbq/RWAlnQyyazA4aoAB4FsBSA/bFjuF1EOL7VbrIrBuusmrt4ZZORfb6ehbWdnRHEIiITaEUKa5EJqUakRSaEYBJSCY2dEstQY7AuxahwXFrvZmWl2rh4JZ07z9dLtesfNj5q0FU3A5ObbwAAAABJRU5ErkJggg==";
        } }, e = {};
        function n(o2) {
          var r = e[o2];
          if (r !== void 0)
            return r.exports;
          var i = e[o2] = { id: o2, exports: {} };
          return t[o2](i, i.exports, n), i.exports;
        }
        n.m = t, n.n = function(t2) {
          var e2 = t2 && t2.__esModule ? function() {
            return t2.default;
          } : function() {
            return t2;
          };
          return n.d(e2, { a: e2 }), e2;
        }, n.d = function(t2, e2) {
          for (var o2 in e2)
            n.o(e2, o2) && !n.o(t2, o2) && Object.defineProperty(t2, o2, { enumerable: true, get: e2[o2] });
        }, n.o = function(t2, e2) {
          return Object.prototype.hasOwnProperty.call(t2, e2);
        }, n.r = function(t2) {
          typeof Symbol != "undefined" && Symbol.toStringTag && Object.defineProperty(t2, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(t2, "__esModule", { value: true });
        }, n.b = document.baseURI || self.location.href;
        var o = {};
        return function() {
          "use strict";
          n.r(o), n.d(o, { AIRPLAY: function() {
            return cn;
          }, CLASS_PLAYER: function() {
            return l;
          }, CLASS_PREFIX: function() {
            return a;
          }, Checkbox: function() {
            return br;
          }, ContextMenu: function() {
            return re;
          }, Control: function() {
            return Ht;
          }, EVENT: function() {
            return c;
          }, EXIT_FULL_SCREEN: function() {
            return Ze;
          }, FULL_SCREEN: function() {
            return Ge;
          }, Fullscreen: function() {
            return ge;
          }, I18n: function() {
            return sn;
          }, Icon: function() {
            return pn;
          }, LIVE: function() {
            return un;
          }, LOOP: function() {
            return an;
          }, Loading: function() {
            return qt;
          }, MUTE: function() {
            return tn;
          }, NORMAL: function() {
            return nn;
          }, PAUSE: function() {
            return $e;
          }, PIP: function() {
            return rn;
          }, PLAY: function() {
            return Je;
          }, Player: function() {
            return Gr;
          }, Popover: function() {
            return Le;
          }, SETTINGS: function() {
            return Qe;
          }, SPEED: function() {
            return on;
          }, Shortcut: function() {
            return jn;
          }, Slider: function() {
            return kr;
          }, Switch: function() {
            return Ve;
          }, Toast: function() {
            return de;
          }, ToastItem: function() {
            return ye;
          }, Tooltip: function() {
            return bt;
          }, UNMUTE: function() {
            return en;
          }, VOLUME: function() {
            return ln;
          }, WEB_EXIT_FULL_SCREEN: function() {
            return Ke;
          }, WEB_FULL_SCREEN: function() {
            return qe;
          }, WebFullscreen: function() {
            return Ee;
          }, default: function() {
            return Gr;
          } });
          var t2 = {};
          n.r(t2), n.d(t2, { Checkbox: function() {
            return br;
          }, Popover: function() {
            return Le;
          }, Slider: function() {
            return kr;
          }, Switch: function() {
            return Ve;
          }, Tooltip: function() {
            return bt;
          } });
          n(4);
          function e2(t3, e3) {
            var n2 = Object.keys(t3);
            if (Object.getOwnPropertySymbols) {
              var o2 = Object.getOwnPropertySymbols(t3);
              e3 && (o2 = o2.filter(function(e4) {
                return Object.getOwnPropertyDescriptor(t3, e4).enumerable;
              })), n2.push.apply(n2, o2);
            }
            return n2;
          }
          function r(t3) {
            for (var n2 = 1; n2 < arguments.length; n2++) {
              var o2 = arguments[n2] != null ? arguments[n2] : {};
              n2 % 2 ? e2(Object(o2), true).forEach(function(e3) {
                i(t3, e3, o2[e3]);
              }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(t3, Object.getOwnPropertyDescriptors(o2)) : e2(Object(o2)).forEach(function(e3) {
                Object.defineProperty(t3, e3, Object.getOwnPropertyDescriptor(o2, e3));
              });
            }
            return t3;
          }
          function i(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var a = "nplayer_", l = "nplayer", c = { ENTER_FULLSCREEN: "EnterFullscreen", EXIT_FULLSCREEN: "ExitFullscreen", WEB_ENTER_FULLSCREEN: "WebEnterFullscreen", WEB_EXIT_FULLSCREEN: "WebExitFullscreen", DURATION_CHANGE: "DurationChange", RATE_CHANGE: "RateChange", AFTER_INIT: "AfterInit", PLAY: "Play", PAUSE: "Pause", ENDED: "Ended", WAITING: "Waiting", STALLED: "Stalled", CANPLAY: "Canplay", LOADED_METADATA: "LoadedMetadata", ERROR: "Error", SEEKED: "Seeked", TIME_UPDATE: "TimeUpdate", VOLUME_CHANGE: "VolumeChange", PROGRESS: "Progress", ENTER_PIP: "EnterPip", EXIT_PIP: "ExitPip", LOADING_SHOW: "LoadingShow", LOADING_HIDE: "LoadingHide", MOUNTED: "Mounted", UPDATE_SIZE: "UpdateSize", BEFORE_DISPOSE: "BeforeDispose", UPDATE_OPTIONS: "UpdateOptions", OPEN_EDGE: "OpenEdge", CONTROL_SHOW: "ControlShow", CONTROL_HIDE: "ControlHide", CONTROL_ITEM_UPDATE: "ControlItemUpdate", BP_CHANGE: "BpChange" }, u = typeof window != "undefined", s = u && /(iPad|iPhone|iPod)/gi.test(navigator.platform), f = u && /MSIE|Trident/.test(navigator.userAgent), p = u && f && navigator.userAgent.indexOf("Windows NT 10.0") > -1;
          function h(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          var y = /([\w-]+)?(?:#([\w-]+))?((?:\.(?:[\w-]+))*)/;
          function d(t3, e3, n2) {
            var o2 = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : a, r2 = [];
            t3 && (r2 = y.exec(t3) || []);
            var i2 = document.createElement(r2[1] || "div");
            return r2[2] && (i2.id = r2[3]), r2[3] && (i2.className = r2[3].replace(/\./g, " ".concat(o2)).trim()), e3 && Object.keys(e3).forEach(function(t4) {
              var n3 = e3[t4];
              n3 !== void 0 && (/^on\w+$/.test(t4) ? i2[t4] = n3 : t4 === "selected" ? n3 && i2.setAttribute(t4, "true") : i2.setAttribute(t4, n3));
            }), n2 && (N(n2) ? i2.innerHTML = n2 : n2.forEach(function(t4) {
              return i2.appendChild(t4);
            })), i2;
          }
          function b(t3) {
            return t3 ? N(t3) ? document.querySelector(t3) : t3 : null;
          }
          function v(t3) {
            t3 && (t3.remove ? t3.remove() : t3.parentNode && t3.parentNode.removeChild(t3));
          }
          function m(t3) {
            t3.style.display = "";
          }
          function g(t3) {
            t3.style.display = "none";
          }
          function _(t3) {
            var e3 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "", n2 = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : a;
            if (!(e3 = e3.trim()))
              return t3;
            if (t3.classList)
              e3.split(" ").forEach(function(e4) {
                return t3.classList.add(n2 + e4);
              });
            else {
              var o2 = t3.className && t3.className.baseVal || "";
              t3.setAttribute("class", (o2 ? "".concat(o2, " ") : "") + e3.split(" ").map(function(t4) {
                return n2 + t4;
              }).join(" "));
            }
            return t3;
          }
          function w(t3, e3) {
            var n2 = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : a;
            return t3.classList.remove(n2 + e3), t3;
          }
          function E(t3, e3) {
            var n2 = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : a;
            return t3.classList.contains(n2 + e3);
          }
          function O(t3, e3, n2) {
            var o2 = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : a;
            return e3 = o2 + e3, n2 ? (t3.classList.add(e3), true) : n2 === false ? (t3.classList.remove(e3), true) : t3.classList.toggle(e3, n2);
          }
          var k = "http://www.w3.org/2000/svg";
          function P(t3, e3) {
            var n2 = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "0 0 24 24", o2 = document.createElementNS(k, "svg");
            if (o2.setAttribute("viewBox", n2), t3 && _(o2, t3), e3) {
              var r2 = document.createElementNS(k, "path");
              r2.setAttributeNS(null, "d", e3), o2.appendChild(r2);
            }
            return o2;
          }
          function x(t3) {
            return t3.path || t3.composedPath();
          }
          var T = false;
          if (u)
            try {
              var S = Object.defineProperty({}, "once", { get: function() {
                T = true;
              } });
              window.addEventListener("test", null, S);
            } catch (t3) {
            }
          var j = function() {
            function t3(e4, n3, o3, r2) {
              var i2;
              !function(t4, e5) {
                if (!(t4 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, t3), this.node = e4, this.type = n3, this.handler = o3, this.options = r2, this.options = (i2 = r2) === true || i2 === false || B.call(i2) === "[object Boolean]" ? r2 : !!r2 && (T && r2), e4.addEventListener(n3, o3, this.options);
            }
            var e3, n2, o2;
            return e3 = t3, (n2 = [{ key: "dispose", value: function() {
              this.handler && (this.node.removeEventListener(this.type, this.handler, this.options), this.node = null, this.handler = null, this.options = null);
            } }]) && h(e3.prototype, n2), o2 && h(e3, o2), t3;
          }();
          function C(t3) {
            var e3 = t3.cloneNode(true);
            e3.style.position = "absolute", e3.style.opacity = "0", e3.removeAttribute("hidden");
            var n2 = t3.parentNode || document.body;
            n2.appendChild(e3);
            var o2 = e3.getBoundingClientRect();
            return n2.removeChild(e3), o2;
          }
          function R(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function L(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var I = function() {
            function t3(e4, n3, o3, r2) {
              var i2 = this;
              !function(t4, e5) {
                if (!(t4 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, t3), L(this, "pending", false), L(this, "downHandler", function(t4) {
                t4.preventDefault(), i2.el.setPointerCapture(t4.pointerId), i2.el.addEventListener("pointermove", i2.moveHandler, true), i2.start(t4);
              }), L(this, "moveHandler", function(t4) {
                t4.preventDefault(), i2.lastEv = t4, i2.pending || (i2.pending = true, requestAnimationFrame(i2.handlerMove));
              }), L(this, "handlerMove", function() {
                i2.move(i2.lastEv), i2.pending = false;
              }), L(this, "upHandler", function(t4) {
                t4.preventDefault(), i2.el.releasePointerCapture(t4.pointerId), i2.el.removeEventListener("pointermove", i2.moveHandler, true), i2.end && i2.end(t4);
              }), this.el = e4, this.start = n3, this.move = o3, this.end = r2, e4.addEventListener("pointerdown", this.downHandler, true), e4.addEventListener("pointerup", this.upHandler, true), e4.addEventListener("pointercancel", this.upHandler, true);
            }
            var e3, n2, o2;
            return e3 = t3, (n2 = [{ key: "dispose", value: function() {
              this.el && (this.el.removeEventListener("pointerdown", this.downHandler, true), this.el.removeEventListener("pointerup", this.upHandler, true), this.el.removeEventListener("pointercancel", this.upHandler, true), this.el.removeEventListener("pointermove", this.moveHandler, true), this.start = null, this.move = null, this.end = null, this.lastEv = null, this.el = null);
            } }]) && R(e3.prototype, n2), o2 && R(e3, o2), t3;
          }();
          function A(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          var D = function() {
            function t3(e4, n3) {
              var o3, r2, i2, a2 = this;
              !function(t4, e5) {
                if (!(t4 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, t3), i2 = function() {
                a2.rect = a2.el.getBoundingClientRect();
              }, (r2 = "update") in (o3 = this) ? Object.defineProperty(o3, r2, { value: i2, enumerable: true, configurable: true, writable: true }) : o3[r2] = i2, this.el = e4, this.player = n3, this.rect = {}, n3 && n3.on(c.UPDATE_SIZE, this.update);
            }
            var e3, n2, o2;
            return e3 = t3, (n2 = [{ key: "width", get: function() {
              return this.tryUpdate(this.rect.width), this.rect.width;
            } }, { key: "height", get: function() {
              return this.tryUpdate(this.rect.height), this.rect.height;
            } }, { key: "x", get: function() {
              return this.tryUpdate(this.rect.left), this.rect.left;
            } }, { key: "y", get: function() {
              return this.tryUpdate(this.rect.top), this.rect.top;
            } }, { key: "changed", get: function() {
              var t4 = this.el.getBoundingClientRect(), e4 = t4.top !== this.rect.top || t4.left !== this.rect.left || t4.width !== this.rect.width || t4.height !== this.rect.height;
              return e4 && (this.rect = t4), e4;
            } }, { key: "tryUpdate", value: function(t4) {
              t4 || this.update();
            } }, { key: "dispose", value: function() {
              this.player && this.player.off(c.UPDATE_SIZE, this.update), this.el = null, this.rect = null;
            } }]) && A(e3.prototype, n2), o2 && A(e3, o2), t3;
          }(), B = Object.prototype.toString;
          function N(t3) {
            return typeof t3 == "string" || t3 instanceof String;
          }
          function M(t3) {
            return typeof t3 == "function";
          }
          function H(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function U(t3, e3) {
            if (!(t3 instanceof e3))
              throw new TypeError("Cannot call a class as a function");
          }
          var F = function t3(e3, n2, o2) {
            U(this, t3), this.fn = e3, this.context = n2, this.once = o2, this.once = o2 || false;
          }, V = function() {
            function t3() {
              U(this, t3), this._events = Object.create(null), this._eventsCount = 0;
            }
            var e3, n2, o2;
            return e3 = t3, (n2 = [{ key: "emit", value: function(t4, e4, n3, o3, r2, i2) {
              if (!this._events[t4])
                return false;
              var a2, l2, c2 = this._events[t4], u2 = arguments.length;
              if (c2.fn) {
                switch (c2.once && this.off(t4, c2.fn, void 0, true), u2) {
                  case 1:
                    return c2.fn.call(c2.context), true;
                  case 2:
                    return c2.fn.call(c2.context, e4), true;
                  case 3:
                    return c2.fn.call(c2.context, e4, n3), true;
                  case 4:
                    return c2.fn.call(c2.context, e4, n3, o3), true;
                  case 5:
                    return c2.fn.call(c2.context, e4, n3, o3, r2), true;
                  case 6:
                    return c2.fn.call(c2.context, e4, n3, o3, r2, i2), true;
                }
                for (l2 = 1, a2 = new Array(u2 - 1); l2 < u2; l2++)
                  a2[l2 - 1] = arguments[l2];
                c2.fn.apply(c2.context, a2);
              } else {
                var s2, f2 = c2.length;
                for (l2 = 0; l2 < f2; l2++)
                  switch (c2[l2].once && this.off(t4, c2[l2].fn, void 0, true), u2) {
                    case 1:
                      c2[l2].fn.call(c2[l2].context);
                      break;
                    case 2:
                      c2[l2].fn.call(c2[l2].context, e4);
                      break;
                    case 3:
                      c2[l2].fn.call(c2[l2].context, e4, n3);
                      break;
                    case 4:
                      c2[l2].fn.call(c2[l2].context, e4, n3, o3);
                      break;
                    default:
                      if (!a2)
                        for (s2 = 1, a2 = new Array(u2 - 1); s2 < u2; s2++)
                          a2[s2 - 1] = arguments[s2];
                      c2[l2].fn.apply(c2[l2].context, a2);
                  }
              }
              return true;
            } }, { key: "on", value: function(t4, e4, n3) {
              var o3 = arguments.length > 3 && arguments[3] !== void 0 && arguments[3];
              if (typeof e4 != "function")
                throw new TypeError("The listener must be a function");
              var r2 = new F(e4, n3 || this, o3);
              return this._events[t4] ? this._events[t4].fn ? this._events[t4] = [this._events[t4], r2] : this._events[t4].push(r2) : (this._events[t4] = r2, this._eventsCount++), { dispose: this.off.bind(this, t4, e4) };
            } }, { key: "once", value: function(t4, e4, n3) {
              return this.on(t4, e4, n3, true);
            } }, { key: "off", value: function(t4, e4, n3, o3) {
              if (!this._events[t4])
                return this;
              if (!e4)
                return this.clearEvent(t4), this;
              var r2 = this._events[t4], i2 = [];
              if (r2.fn)
                r2.fn !== e4 || o3 && !r2.once || n3 && r2.context !== n3 || this.clearEvent(t4);
              else {
                for (var a2 = 0, l2 = r2.length; a2 < l2; a2++)
                  (r2[a2].fn !== e4 || o3 && !r2[a2].once || n3 && r2[a2].context !== n3) && i2.push(r2[a2]);
                i2.length ? this._events[t4] = i2.length === 1 ? i2[0] : i2 : this.clearEvent(t4);
              }
              return this;
            } }, { key: "removeAllListeners", value: function(t4) {
              return t4 ? this._events[t4] && this.clearEvent(t4) : (this._events = Object.create(null), this._eventsCount = 0), this;
            } }, { key: "clearEvent", value: function(t4) {
              --this._eventsCount == 0 ? this._events = Object.create(null) : delete this._events[t4];
            } }]) && H(e3.prototype, n2), o2 && H(e3, o2), t3;
          }();
          function z(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          var Y = function() {
            function t3(e4, n3, o3, r2, i2) {
              !function(t4, e5) {
                if (!(t4 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, t3), n3 && !N(n3) ? this.el = n3 : this.el = d(n3, o3, r2, i2), e4 && e4.appendChild(this.el);
            }
            var e3, n2, o2;
            return e3 = t3, (n2 = [{ key: "applyStyle", value: function(t4) {
              Object.assign(this.el.style, t4);
            } }, { key: "dispose", value: function() {
              v(this.el), et(this);
            } }]) && z(e3.prototype, n2), o2 && z(e3, o2), t3;
          }();
          function X(t3, e3) {
            return !(!t3 || !e3) && (t3 === e3 || t3.id === e3.id);
          }
          function W(t3, e3) {
            v(t3.el), e3 && e3(t3);
          }
          function G(t3, e3, n2, o2) {
            e3.insertBefore(t3.el, (n2 == null ? void 0 : n2.el) || null), o2 && o2(t3);
          }
          function Z(t3) {
            for (var e3, n2 = [], o2 = [t3[0]], r2 = 0, i2 = 0, a2 = 0, l2 = 0, c2 = 1, u2 = t3.length; c2 < u2; ++c2)
              if ((e3 = t3[c2]) !== -1)
                if (e3 > t3[o2[l2 = o2.length - 1]])
                  n2[c2] = o2[l2], o2.push(c2);
                else {
                  for (r2 = 0, i2 = l2; r2 < i2; )
                    e3 > t3[o2[a2 = (r2 + i2) / 2 | 0]] ? r2 = a2 + 1 : i2 = a2;
                  n2[c2] = o2[r2 - 1], o2[r2] = c2;
                }
            for (r2 = o2[(l2 = o2.length) - 1]; l2-- > 0; )
              o2[l2] = r2, r2 = n2[r2];
            return o2;
          }
          var q = { checked: true, muted: true, multiple: true, selected: true };
          function K(t3, e3, n2) {
            q[e3] ? t3[e3] = n2 : n2 == null ? t3.removeAttribute(e3) : t3.setAttribute(e3, n2);
          }
          function Q(t3, e3, n2) {
            var o2, r2;
            n2 !== void 0 && (Object.keys(n2).forEach(function(i2) {
              o2 = e3[i2], r2 = n2[i2], i2 === "style" ? function(t4, e4, n3) {
                n3 !== void 0 && (n3 ? (Object.keys(n3).forEach(function(e5) {
                  t4.style[e5] = n3[e5] || "";
                }), Object.keys(e4).forEach(function(e5) {
                  e5 in n3 || (t4.style[e5] = "");
                })) : t4.removeAttribute("style"));
              }(t3, o2, r2) : o2 !== r2 && K(t3, i2, r2);
            }), Object.keys(e3).forEach(function(e4) {
              e4 in n2 || K(t3, e4, void 0);
            }));
          }
          function J(t3) {
            var e3 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0, n2 = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 1;
            return Math.max(Math.min(t3, n2), e3);
          }
          var $ = new Map();
          function tt(t3, e3) {
            return $.has(t3) || $.set(t3, []), $.get(t3).push(e3), e3;
          }
          function et(t3) {
            $.has(t3) && ($.get(t3).forEach(function(t4) {
              return t4.dispose();
            }), $.delete(t3));
          }
          function nt(t3, e3, n2, o2, r2) {
            var i2 = new j(e3, n2, o2, r2);
            return t3 && tt(t3, i2), i2;
          }
          function ot(t3, e3) {
            var n2 = false, o2 = true, r2 = null;
            return function() {
              if (r2 = arguments, o2)
                return o2 = false, t3.apply(e3, r2);
              n2 || (n2 = true, requestAnimationFrame(function() {
                t3.apply(e3, r2), n2 = false;
              }));
            };
          }
          function rt(t3) {
            var e3 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 2, n2 = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "0";
            if ((t3 = String(t3)).length >= 2)
              return t3;
            for (var o2 = 0, r2 = e3 - t3.length; o2 < r2; o2++)
              t3 = n2 + t3;
            return t3;
          }
          function it(t3) {
            return isFinite(t3) ? t3 <= 0 ? "0:00" : (t3 = Math.round(t3)) < 60 ? "0:".concat(rt(t3)) : t3 < 3600 ? "".concat(Math.floor(t3 / 60), ":").concat(rt(t3 % 60)) : "".concat(Math.floor(t3 / 3600), ":").concat(rt(Math.floor(t3 % 3600 / 60)), ":").concat(rt(t3 % 60)) : "-";
          }
          var at = { $: d, clamp: J, addDisposableListener: nt, addDisposable: tt, createSvg: P, dispose: et, getEventPath: x, removeNode: v, addClass: _ };
          function lt(t3) {
            return (lt = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function ct(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function ut(t3, e3) {
            return (ut = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function st(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = pt(t3);
              if (e3) {
                var r2 = pt(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return ft(this, n2);
            };
          }
          function ft(t3, e3) {
            return !e3 || lt(e3) !== "object" && typeof e3 != "function" ? function(t4) {
              if (t4 === void 0)
                throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
              return t4;
            }(t3) : e3;
          }
          function pt(t3) {
            return (pt = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          var ht = "tooltip-left", yt = "tooltip-right", dt = "tooltip-bottom", bt = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && ut(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = st(i2);
            function i2(t4, e4) {
              var n3;
              return function(t5, e5) {
                if (!(t5 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, i2), n3 = r2.call(this, t4, ".tooltip_content"), _(t4, "tooltip"), e4 && (n3.html = e4), n3;
            }
            return e3 = i2, (n2 = [{ key: "html", get: function() {
              return this.el.innerHTML;
            }, set: function(t4) {
              this.el.innerHTML = t4;
            } }, { key: "resetPos", value: function() {
              w(this.el, ht), w(this.el, yt), w(this.el, dt);
            } }, { key: "setBottom", value: function() {
              _(this.el, dt);
            } }, { key: "setLeft", value: function() {
              _(this.el, ht);
            } }, { key: "setRight", value: function() {
              _(this.el, yt);
            } }, { key: "hide", value: function() {
              g(this.el);
            } }, { key: "show", value: function() {
              m(this.el);
            } }]) && ct(e3.prototype, n2), o2 && ct(e3, o2), i2;
          }(Y), vt = function() {
            return { id: "spacer", el: d(".spacer") };
          };
          function mt(t3) {
            return (mt = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function gt(t3, e3) {
            if (!(t3 instanceof e3))
              throw new TypeError("Cannot call a class as a function");
          }
          function _t(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function wt(t3, e3) {
            return (wt = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function Et(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = Pt(t3);
              if (e3) {
                var r2 = Pt(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return Ot(this, n2);
            };
          }
          function Ot(t3, e3) {
            return !e3 || mt(e3) !== "object" && typeof e3 != "function" ? kt(t3) : e3;
          }
          function kt(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function Pt(t3) {
            return (Pt = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          function xt(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var Tt = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && wt(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = Et(i2);
            function i2(t4, e4, n3) {
              var o3, a2 = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : 0;
              if (gt(this, i2), xt(kt(o3 = r2.call(this, t4, ".control_bar")), "prevItems", []), xt(kt(o3), "spacer", vt()), xt(kt(o3), "initControlItem", function(t5) {
                var e5;
                if (t5 = o3.getItem(t5))
                  return t5.el || (t5.el = d()), t5.mounted ? (t5.tooltip && (t5.tooltip.resetPos(), o3.position === 2 && t5.tooltip.setBottom()), void (t5.update && t5.update(o3.position))) : (t5.tip && (e5 = new bt(t5.el, t5.tip)), t5.init && (t5.init.length > 2 && !e5 && (e5 = new bt(t5.el)), t5.init(o3.player, o3.position, e5)), t5.dispose && tt(kt(o3), t5), e5 || (e5 = t5.tooltip), e5 && (e5.resetPos(), o3.position === 2 && e5.setBottom()), t5.mounted = true, t5);
              }), xt(kt(o3), "onHideControlItem", function(t5) {
                t5.hide && t5.hide();
              }), o3.player = e4, o3.position = a2, a2 === 2 && _(o3.el, "control_bar-top"), n3) {
                var l2 = document.createDocumentFragment();
                n3.forEach(function(t5) {
                  (t5 = o3.initControlItem(t5)) && (l2.appendChild(t5.el), o3.prevItems.push(t5));
                }), o3.updateTooltipPos(), o3.el.appendChild(l2);
              }
              return o3;
            }
            return e3 = i2, (n2 = [{ key: "getItem", value: function(t4) {
              return t4 === "spacer" ? this.spacer : (N(t4) && (t4 = this.player.getControlItem(t4)), !t4 || t4.isSupport && !t4.isSupport(this.player) ? void 0 : t4);
            } }, { key: "updateTooltipPos", value: function() {
              var t4 = this, e4 = this.prevItems.length - 1;
              this.prevItems.forEach(function(n3, o3) {
                n3.tooltip && (n3.tooltip.resetPos(), t4.position === 2 && n3.tooltip.setBottom(), o3 === 0 ? n3.tooltip.setLeft() : o3 === e4 && n3.tooltip.setRight());
              });
            } }, { key: "getItems", value: function() {
              return this.prevItems;
            } }, { key: "setItems", value: function(t4) {
              t4 && (this.prevItems = t4);
            } }, { key: "update", value: function(t4) {
              var e4 = this;
              if (t4) {
                var n3 = [];
                t4.forEach(function(t5) {
                  (t5 = e4.getItem(t5)) && n3.push(t5);
                }), function(t5, e5, n4) {
                  for (var o3 = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : {}, r3 = t5.length - 1, i3 = e5.length - 1, a2 = 0, l2 = t5[a2], c2 = e5[a2]; X(l2, c2); )
                    l2 = t5[++a2], c2 = e5[a2];
                  if (a2 < r3 && a2 < i3)
                    for (l2 = t5[r3], c2 = e5[i3]; X(l2, c2); )
                      l2 = t5[--r3], c2 = e5[--i3];
                  if (!(a2 > r3 && a2 > i3))
                    if (a2 > r3)
                      for (var u2 = a2; u2 <= i3; u2++)
                        G(e5[u2], n4, void 0, o3.mount);
                    else if (a2 > i3)
                      for (var s2 = a2; s2 <= r3; s2++)
                        W(t5[s2], o3.unmount);
                    else {
                      for (var f2 = [], p2 = new Map(), h2 = a2; h2 <= i3; h2++)
                        f2.push(-1), p2.set(e5[h2].id || e5[h2], h2);
                      for (var y2 = false, d2 = 0, b2 = 0, v2 = a2; v2 <= r3; v2++)
                        l2 = t5[v2], (b2 = p2.get(l2.id || l2)) == null ? W(l2, o3.unmount) : (f2[b2 - a2] = v2, d2 > b2 ? y2 = true : d2 = b2);
                      for (var m2, g2, _2 = y2 ? Z(f2) : [], w2 = _2.length - 1, E2 = f2.length - 1; E2 > -1; E2--)
                        m2 = f2[E2], g2 = e5[(b2 = a2 + E2) + 1], m2 === -1 ? G(e5[b2], n4, g2, o3.mount) : y2 && (E2 === _2[w2] ? w2-- : G(e5[b2], n4, g2, o3.update));
                    }
                }(this.prevItems, n3, this.el, { mount: this.initControlItem, unmount: this.onHideControlItem }), this.prevItems = n3, this.updateTooltipPos(), this.player.emit(c.CONTROL_ITEM_UPDATE);
              }
            } }]) && _t(e3.prototype, n2), o2 && _t(e3, o2), i2;
          }(Y);
          function St(t3) {
            return (St = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function jt(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function Ct(t3, e3) {
            return (Ct = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function Rt(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = At(t3);
              if (e3) {
                var r2 = At(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return Lt(this, n2);
            };
          }
          function Lt(t3, e3) {
            return !e3 || St(e3) !== "object" && typeof e3 != "function" ? It(t3) : e3;
          }
          function It(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function At(t3) {
            return (At = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          function Dt(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var Bt = "control-hide", Nt = "control_bg-hide", Mt = "control_bar-hide", Ht = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && Ct(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = Rt(i2);
            function i2(t4, e4) {
              var n3;
              !function(t5, e5) {
                if (!(t5 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, i2), Dt(It(n3 = r2.call(this, t4, ".control")), "delayHidTime", 3e3), Dt(It(n3), "latch", 0), Dt(It(n3), "controlBars", []), Dt(It(n3), "emitAndUpdateBp", function() {
                var t5 = n3.player.rect.width, e5 = n3.controls.find(function(e6) {
                  return t5 <= e6.bp;
                });
                if (n3.currentBp != (e5 == null ? void 0 : e5.bp)) {
                  n3.currentBp = e5 == null ? void 0 : e5.bp;
                  var o4 = (e5 == null ? void 0 : e5.controls) || n3.player.opts.controls;
                  n3.updateItems(o4[0], 0), n3.updateItems(o4[1], 1), n3.updateItems(o4[2], 2), n3.player.emit(c.BP_CHANGE, n3.currentBp);
                }
                return e5;
              }), Dt(It(n3), "show", function() {
                w(n3.el, Bt), w(n3.bgElement, Nt), w(n3.controlBars[2].el, Mt), n3.player.el.style.cursor = "", n3.player.emit(c.CONTROL_SHOW);
              }), Dt(It(n3), "hide", function() {
                _(n3.el, Bt), _(n3.bgElement, Nt), _(n3.controlBars[2].el, Mt), n3.player.el.style.cursor = "none", n3.player.emit(c.CONTROL_HIDE);
              }), Dt(It(n3), "showTransient", function() {
                n3.show(), clearTimeout(n3.showTimer), n3.showTimer = setTimeout(n3.tryHide, n3.delayHidTime);
              }), Dt(It(n3), "tryHide", function() {
                !n3.player.video.played.length || n3.player.paused || n3.latch || n3.hide();
              }), n3.player = e4, n3.bgElement = t4.appendChild(d(".control_bg")), n3.controlBars[1] = tt(It(n3), new Tt(n3.el, e4, e4.opts.controls[1], 1)), n3.controlBars[0] = tt(It(n3), new Tt(n3.el, e4, e4.opts.controls[0], 0)), n3.controlBars[2] = tt(It(n3), new Tt(t4, e4, e4.opts.controls[2], 2));
              var o3 = e4.opts.bpControls;
              return n3.controls = Object.keys(o3).map(function(t5) {
                return { bp: Number(t5), controls: o3[t5] };
              }).sort(function(t5, e5) {
                return e5.bp - t5.bp;
              }), n3.controls.length && (tt(It(n3), e4.on(c.UPDATE_SIZE, n3.emitAndUpdateBp)), tt(It(n3), e4.on(c.MOUNTED, n3.emitAndUpdateBp))), tt(It(n3), e4.on(c.PAUSE, function() {
                e4.opts.isTouch || n3.show();
              })), tt(It(n3), e4.on(c.PLAY, function() {
                e4.opts.isTouch || n3.showTransient();
              })), nt(It(n3), e4.el, "mousemove", n3.showTransient), nt(It(n3), e4.el, "mouseleave", n3.tryHide), n3.showTransient(), n3;
            }
            return e3 = i2, (n2 = [{ key: "isActive", get: function() {
              return !E(this.el, Bt);
            } }, { key: "filterItems", value: function(t4, e4) {
              if (t4.length && e4.length) {
                var n3 = new Map();
                return t4.forEach(function(t5) {
                  return n3.set(t5, true);
                }), e4.filter(function(t5) {
                  return !n3.get(t5);
                });
              }
            } }, { key: "updateItems", value: function(t4) {
              var e4 = this, n3 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0, o3 = this.controlBars[n3];
              if (o3) {
                o3.update(t4 || []);
                var r3 = o3.getItems();
                this.controlBars.forEach(function(t5, o4) {
                  o4 !== n3 && (t5.setItems(e4.filterItems(r3, t5.getItems())), t5.updateTooltipPos());
                });
              }
            } }, { key: "require", value: function() {
              this.latch++;
            } }, { key: "release", value: function() {
              this.latch && (this.latch--, this.tryHide());
            } }]) && jt(e3.prototype, n2), o2 && jt(e3, o2), i2;
          }(Y);
          function Ut(t3) {
            return (Ut = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function Ft(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function Vt(t3, e3) {
            return (Vt = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function zt(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = Wt(t3);
              if (e3) {
                var r2 = Wt(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return Yt(this, n2);
            };
          }
          function Yt(t3, e3) {
            return !e3 || Ut(e3) !== "object" && typeof e3 != "function" ? Xt(t3) : e3;
          }
          function Xt(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function Wt(t3) {
            return (Wt = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          function Gt(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var Zt = "-loading", qt = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && Vt(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = zt(i2);
            function i2(t4, e4) {
              var n3;
              return function(t5, e5) {
                if (!(t5 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, i2), Gt(Xt(n3 = r2.call(this, t4, e4.opts.loadingEl || ".loading", void 0, e4.opts.loadingEl ? void 0 : function(t5, e5) {
                for (var n4 = [], o3 = 0; o3 < e5; ++o3)
                  n4.push(t5);
                return n4.join("");
              }("<i></i>", 12))), "startWaitingTime", 0), Gt(Xt(n3), "_checkCanplay", function() {
                n3.startWaitingTime !== n3.player.currentTime && (n3.hide(), clearTimeout(n3.showTimer), n3.player.off(c.TIME_UPDATE, n3._checkCanplay));
              }), Gt(Xt(n3), "show", function() {
                _(n3.player.el, Zt, l), n3.player.emit(c.LOADING_SHOW);
              }), Gt(Xt(n3), "hide", function() {
                w(n3.player.el, Zt, l), n3.player.emit(c.LOADING_HIDE);
              }), n3.player = e4, tt(Xt(n3), e4.on(c.CANPLAY, n3.hide)), tt(Xt(n3), e4.on(c.WAITING, function() {
                n3.player.currentTime && n3.tryShow();
              })), tt(Xt(n3), e4.on(c.STALLED, function() {
                var t5 = n3.player.currentTime;
                if (t5) {
                  var e5 = true;
                  n3.player.eachBuffer(function(n4, o3) {
                    if (n4 <= t5 && o3 > t5)
                      return e5 = false, true;
                  }), e5 && n3.tryShow();
                }
              })), n3;
            }
            return e3 = i2, (n2 = [{ key: "isActive", get: function() {
              return E(this.player.el, Zt, l);
            } }, { key: "checkCanplay", value: function() {
              this.startWaitingTime = this.player.currentTime, this.player.off(c.TIME_UPDATE, this._checkCanplay), this.player.on(c.TIME_UPDATE, this._checkCanplay);
            } }, { key: "tryShow", value: function() {
              this.checkCanplay(), clearTimeout(this.showTimer), this.showTimer = setTimeout(this.show, 100);
            } }]) && Ft(e3.prototype, n2), o2 && Ft(e3, o2), i2;
          }(Y);
          function Kt(t3) {
            return (Kt = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function Qt(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function Jt(t3, e3) {
            return (Jt = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function $t(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = ne(t3);
              if (e3) {
                var r2 = ne(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return te(this, n2);
            };
          }
          function te(t3, e3) {
            return !e3 || Kt(e3) !== "object" && typeof e3 != "function" ? ee(t3) : e3;
          }
          function ee(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function ne(t3) {
            return (ne = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          function oe(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var re = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && Jt(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = $t(i2);
            function i2(t4, e4, n3) {
              var o3;
              return function(t5, e5) {
                if (!(t5 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, i2), oe(ee(o3 = r2.call(this, t4, ".contextmenu")), "showed", false), oe(ee(o3), "hide", function() {
                g(o3.el);
              }), o3.player = e4, o3.items = n3, g(o3.el), o3.rect = new D(o3.el, e4), o3.items.forEach(function(t5) {
                return t5.init && t5.init(e4, t5);
              }), nt(ee(o3), e4.el, "contextmenu", function(t5) {
                if (o3.hide(), (!e4.opts.contextMenuToggle || !o3.showed) && (t5.preventDefault(), t5.stopPropagation(), o3.renderItems())) {
                  m(o3.el), o3.rect.update(), o3.player.rect.update();
                  var n4 = o3.rect, r3 = n4.width, i3 = n4.height, a2 = o3.player.rect, l2 = a2.x, c2 = a2.y, u2 = window, s2 = u2.innerWidth, f2 = u2.innerHeight, p2 = t5.clientX, h2 = t5.clientY, y2 = p2 - l2, d2 = h2 - c2;
                  p2 + r3 > s2 && (y2 = s2 - r3), h2 + i3 > f2 && (d2 = f2 - i3), o3.applyStyle({ left: "".concat(y2, "px"), top: "".concat(d2, "px") });
                }
                o3.showed = !o3.showed;
              }), nt(ee(o3), document, "click", function() {
                o3.showed = false, o3.hide();
              }), o3;
            }
            return e3 = i2, (n2 = [{ key: "getDomNodes", value: function() {
              var t4 = this;
              return this.items.filter(function(t5) {
                return t5 && !t5.invisible;
              }).map(function(e4) {
                var n3 = d(".contextmenu_item");
                return e4.show && e4.show(t4.player, e4), e4.html && (n3.innerHTML = e4.html), e4.disabled && _(n3, "contextmenu_item-disabled"), e4.checked && _(n3, "contextmenu_item-checked"), e4.click && n3.addEventListener("click", function() {
                  return e4.click(t4.player, e4);
                }, false), n3;
              });
            } }, { key: "renderItems", value: function() {
              var t4 = this.getDomNodes();
              if (!t4.length)
                return false;
              this.el.textContent = "";
              var e4 = document.createDocumentFragment();
              return t4.forEach(function(t5) {
                return e4.appendChild(t5);
              }), this.el.appendChild(e4), true;
            } }, { key: "isActive", get: function() {
              return this.el.style.display !== "none";
            } }]) && Qt(e3.prototype, n2), o2 && Qt(e3, o2), i2;
          }(Y);
          function ie(t3) {
            return (ie = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function ae(t3, e3) {
            if (!(t3 instanceof e3))
              throw new TypeError("Cannot call a class as a function");
          }
          function le(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function ce(t3, e3, n2) {
            return e3 && le(t3.prototype, e3), n2 && le(t3, n2), t3;
          }
          function ue(t3, e3, n2) {
            return (ue = typeof Reflect != "undefined" && Reflect.get ? Reflect.get : function(t4, e4, n3) {
              var o2 = function(t5, e5) {
                for (; !Object.prototype.hasOwnProperty.call(t5, e5) && (t5 = he(t5)) !== null; )
                  ;
                return t5;
              }(t4, e4);
              if (o2) {
                var r2 = Object.getOwnPropertyDescriptor(o2, e4);
                return r2.get ? r2.get.call(n3) : r2.value;
              }
            })(t3, e3, n2 || t3);
          }
          function se(t3, e3) {
            return (se = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function fe(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = he(t3);
              if (e3) {
                var r2 = he(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return pe(this, n2);
            };
          }
          function pe(t3, e3) {
            return !e3 || ie(e3) !== "object" && typeof e3 != "function" ? function(t4) {
              if (t4 === void 0)
                throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
              return t4;
            }(t3) : e3;
          }
          function he(t3) {
            return (he = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          var ye = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && se(t4, e4);
            }(n2, t3);
            var e3 = fe(n2);
            function n2(t4, o2) {
              var r2, i2 = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "center";
              return ae(this, n2), (r2 = e3.call(this, t4, ".toast")).position = i2, _(r2.el, "toast-".concat(i2)), r2.el.innerHTML = o2, r2;
            }
            return ce(n2, [{ key: "dispose", value: function() {
              ue(he(n2.prototype), "dispose", this).call(this), clearTimeout(this.timer);
            } }]), n2;
          }(Y), de = function() {
            function t3(e3) {
              var n2, o2, r2;
              ae(this, t3), r2 = [], (o2 = "toasts") in (n2 = this) ? Object.defineProperty(n2, o2, { value: r2, enumerable: true, configurable: true, writable: true }) : n2[o2] = r2, this.container = e3;
            }
            return ce(t3, [{ key: "show", value: function(t4, e3) {
              var n2 = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 3e3, o2 = this.toasts.find(function(t5) {
                return t5.position === e3;
              });
              return o2 ? (clearTimeout(o2.timer), o2.el.innerHTML = t4) : (o2 = new ye(this.container, t4, e3), this.toasts.push(o2)), n2 > 0 && (o2.timer = setTimeout(this.close.bind(this, o2), n2)), o2;
            } }, { key: "close", value: function(t4) {
              if (t4)
                return this.toasts = this.toasts.filter(function(e3) {
                  return e3 !== t4;
                }), t4.dispose();
              this.toasts.forEach(function(t5) {
                return t5.dispose();
              }), this.toasts = [];
            } }, { key: "dispose", value: function() {
              this.toasts && (this.close(), this.toasts = null, this.container = null);
            } }]), t3;
          }();
          function be(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function ve(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var me = "-full", ge = function() {
            function t3(e4) {
              var n3 = this;
              !function(t4, e5) {
                if (!(t4 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, t3), ve(this, "prefix", this.getPrefix()), ve(this, "toggle", function() {
                n3.isActive ? n3.exit() : n3.enter();
              }), this.player = e4, nt(this, document, this.prefix === "ms" ? "MSFullscreenChange" : "".concat(this.prefix, "fullscreenchange"), function() {
                var t4 = "";
                n3.isActive ? (n3.addClass(), t4 = c.ENTER_FULLSCREEN) : (n3.removeClass(), t4 = c.EXIT_FULLSCREEN), n3.player.emit(t4), n3.player.emit(c.UPDATE_SIZE);
              }), tt(this, e4.on(c.UPDATE_OPTIONS, function() {
                n3.disableDblclick();
              })), this.setTarget(), this.isActive && this.addClass(), e4.opts.isTouch || this.enableDblclick();
            }
            var e3, n2, o2;
            return e3 = t3, (n2 = [{ key: "getPrefix", value: function() {
              if (M(document.exitFullscreen))
                return "";
              var t4 = "";
              return ["webkit", "moz", "ms"].forEach(function(e4) {
                (M(document["".concat(e4, "ExitFullscreen")]) || M(document["".concat(e4, "CancelFullScreen")])) && (t4 = e4);
              }), t4;
            } }, { key: "requestFullscreen", get: function() {
              return HTMLElement.prototype.requestFullscreen || HTMLElement.prototype.webkitRequestFullscreen || HTMLElement.prototype.mozRequestFullScreen || HTMLElement.prototype.msRequestFullscreen;
            } }, { key: "exitFullscreen", get: function() {
              return HTMLDocument.prototype.exitFullscreen || HTMLDocument.prototype.webkitExitFullscreen || HTMLDocument.prototype.cancelFullScreen || HTMLDocument.prototype.mozCancelFullScreen || HTMLDocument.prototype.msExitFullscreen;
            } }, { key: "fullscreenElement", get: function() {
              return document.fullscreenElement || document.mozFullScreenElement || document.msFullscreenElement || document.webkitFullscreenElement;
            } }, { key: "isActive", get: function() {
              return this.fullscreenElement === this.target;
            } }, { key: "addClass", value: function() {
              _(this.player.el, me, l);
            } }, { key: "removeClass", value: function() {
              w(this.player.el, me, l);
            } }, { key: "enableDblclick", value: function() {
              this.player.video.addEventListener("dblclick", this.toggle);
            } }, { key: "disableDblclick", value: function() {
              this.player.video.removeEventListener("dblclick", this.toggle);
            } }, { key: "setTarget", value: function(t4, e4) {
              this.target = (t4 && s ? e4 : t4) || (s ? this.player.video : this.player.el), this.isActive && this.enter();
            } }, { key: "enter", value: function() {
              s ? this.target.webkitEnterFullscreen() : (this.requestFullscreen.call(this.target, { navigationUI: "hide" }), this.player.emit(c.UPDATE_SIZE));
            } }, { key: "exit", value: function() {
              return !!this.isActive && (s ? this.target.webkitExitFullscreen() : (this.exitFullscreen.call(document), this.player.emit(c.UPDATE_SIZE)), true);
            } }, { key: "dispose", value: function() {
              this.player && (this.player.off(c.ENTER_FULLSCREEN), this.player.off(c.EXIT_FULLSCREEN), this.disableDblclick(), this.player = null, et(this));
            } }]) && be(e3.prototype, n2), o2 && be(e3, o2), t3;
          }();
          function _e(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          var we = "-web-full", Ee = function() {
            function t3(e4) {
              var n3, o3, r2, i2 = this;
              !function(t4, e5) {
                if (!(t4 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, t3), r2 = function() {
                i2.isActive ? i2.exit() : i2.enter();
              }, (o3 = "toggle") in (n3 = this) ? Object.defineProperty(n3, o3, { value: r2, enumerable: true, configurable: true, writable: true }) : n3[o3] = r2, this.player = e4;
            }
            var e3, n2, o2;
            return e3 = t3, (n2 = [{ key: "isActive", get: function() {
              return E(this.player.el, we, l);
            } }, { key: "enter", value: function() {
              _(this.player.el, we, l), this.player.emit(c.WEB_ENTER_FULLSCREEN), this.player.emit(c.UPDATE_SIZE);
            } }, { key: "exit", value: function() {
              return !!this.isActive && (w(this.player.el, we, l), this.player.emit(c.WEB_EXIT_FULLSCREEN), this.player.emit(c.UPDATE_SIZE), true);
            } }, { key: "dispose", value: function() {
              this.player && (this.player.off(c.WEB_ENTER_FULLSCREEN), this.player.off(c.WEB_EXIT_FULLSCREEN), this.player = null);
            } }]) && _e(e3.prototype, n2), o2 && _e(e3, o2), t3;
          }();
          function Oe(t3) {
            return (Oe = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function ke(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function Pe(t3, e3) {
            return (Pe = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function xe(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = je(t3);
              if (e3) {
                var r2 = je(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return Te(this, n2);
            };
          }
          function Te(t3, e3) {
            return !e3 || Oe(e3) !== "object" && typeof e3 != "function" ? Se(t3) : e3;
          }
          function Se(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function je(t3) {
            return (je = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          var Ce = "popover-active", Re = "popover_panel-bottom", Le = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && Pe(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = xe(i2);
            function i2(t4, e4, n3, o3) {
              var a2;
              return function(t5, e5) {
                if (!(t5 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, i2), (a2 = r2.call(this, t4, ".popover")).onHide = e4, a2.maskEl = a2.el.appendChild(d(".popover_mask")), a2.panelEl = a2.el.appendChild(d(".popover_panel")), n3 && a2.applyPanelStyle(n3), o3 && _(a2.panelEl, "popover_panel-left"), nt(Se(a2), a2.maskEl, "click", function(t5) {
                t5.stopPropagation(), x(t5).includes(a2.panelEl) || a2.hide(t5);
              }), a2;
            }
            return e3 = i2, (n2 = [{ key: "resetPos", value: function() {
              w(this.panelEl, Re);
            } }, { key: "setBottom", value: function() {
              _(this.panelEl, Re);
            } }, { key: "applyPanelStyle", value: function(t4) {
              Object.assign(this.panelEl.style, t4);
            } }, { key: "show", value: function() {
              _(this.el, Ce);
            } }, { key: "hide", value: function(t4) {
              w(this.el, Ce), this.onHide && this.onHide(t4);
            } }]) && ke(e3.prototype, n2), o2 && ke(e3, o2), i2;
          }(Y);
          function Ie(t3) {
            return (Ie = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function Ae(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function De(t3, e3) {
            return (De = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function Be(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = He(t3);
              if (e3) {
                var r2 = He(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return Ne(this, n2);
            };
          }
          function Ne(t3, e3) {
            return !e3 || Ie(e3) !== "object" && typeof e3 != "function" ? Me(t3) : e3;
          }
          function Me(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function He(t3) {
            return (He = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          var Ue, Fe = "switch-active", Ve = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && De(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = Be(i2);
            function i2(t4, e4, n3) {
              var o3;
              return function(t5, e5) {
                if (!(t5 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, i2), (o3 = r2.call(this, t4, ".switch")).toggle(e4 || false), n3 && nt(Me(o3), o3.el, "click", function() {
                o3.toggle(), n3(E(o3.el, Fe));
              }), o3;
            }
            return e3 = i2, (n2 = [{ key: "toggle", value: function(t4) {
              O(this.el, Fe, t4);
            } }]) && Ae(e3.prototype, n2), o2 && Ae(e3, o2), i2;
          }(Y);
          function ze(t3, e3) {
            var n2 = Object.keys(t3);
            if (Object.getOwnPropertySymbols) {
              var o2 = Object.getOwnPropertySymbols(t3);
              e3 && (o2 = o2.filter(function(e4) {
                return Object.getOwnPropertyDescriptor(t3, e4).enumerable;
              })), n2.push.apply(n2, o2);
            }
            return n2;
          }
          function Ye(t3) {
            for (var e3 = 1; e3 < arguments.length; e3++) {
              var n2 = arguments[e3] != null ? arguments[e3] : {};
              e3 % 2 ? ze(Object(n2), true).forEach(function(e4) {
                Xe(t3, e4, n2[e4]);
              }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(t3, Object.getOwnPropertyDescriptors(n2)) : ze(Object(n2)).forEach(function(e4) {
                Object.defineProperty(t3, e4, Object.getOwnPropertyDescriptor(n2, e4));
              });
            }
            return t3;
          }
          function Xe(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var We = Object.create(null), Ge = "Fullscreen", Ze = "Exit fullscreen", qe = "Web fullscreen", Ke = "Exit web fullscreen", Qe = "Settings", Je = "Play", $e = "Pause", tn = "Mute", en = "Unmute", nn = "Normal", on = "Speed", rn = "Picture in Picture", an = "Loop", ln = "Volume", cn = "Airplay", un = "Live", sn = { defaultLang: "", currentLang: "", t: function(t3, e3) {
            var n2;
            return ((n2 = We[e3 || sn.currentLang || sn.defaultLang]) === null || n2 === void 0 ? void 0 : n2[t3]) || t3;
          }, add: function(t3, e3) {
            We[t3.toLowerCase()] = Ye(Ye({}, We[t3.toLowerCase()]), e3), sn.fallback();
          }, fallback: function() {
            Object.keys(We).forEach(function(t3) {
              We[t3.split("-")[0]] = We[t3];
            });
          }, setCurrentLang: function(t3) {
            this.currentLang = t3 || navigator.language || navigator.userLanguage, this.currentLang && (this.currentLang = this.currentLang.toLowerCase());
          }, setDefaultLang: function(t3) {
            this.defaultLang = (t3 || "").toLowerCase();
          } };
          sn.add("zh-cn", (Xe(Ue = {}, Ge, "\u5168\u5C4F"), Xe(Ue, Ze, "\u53D6\u6D88\u5168\u5C4F"), Xe(Ue, qe, "\u7F51\u9875\u5168\u5C4F"), Xe(Ue, Ke, "\u9000\u51FA\u7F51\u9875\u5168\u5C4F"), Xe(Ue, Qe, "\u8BBE\u7F6E"), Xe(Ue, Je, "\u64AD\u653E"), Xe(Ue, $e, "\u6682\u505C"), Xe(Ue, tn, "\u9759\u97F3"), Xe(Ue, en, "\u53D6\u6D88\u9759\u97F3"), Xe(Ue, nn, "\u6B63\u5E38"), Xe(Ue, on, "\u64AD\u653E\u901F\u5EA6"), Xe(Ue, rn, "\u753B\u4E2D\u753B"), Xe(Ue, an, "\u5FAA\u73AF\u64AD\u653E"), Xe(Ue, ln, "\u97F3\u91CF"), Xe(Ue, cn, "\u9694\u7A7A\u64AD\u653E"), Xe(Ue, un, "\u76F4\u64AD"), Ue)), u && sn.setCurrentLang();
          function fn(t3) {
            return function(e3) {
              var n2 = P("icon", t3);
              return e3 && _(n2, e3), n2;
            };
          }
          var pn = Object.create(null);
          function hn(t3, e3) {
            pn[t3] = e3;
          }
          function yn(t3) {
            return (yn = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function dn(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function bn(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          function vn(t3, e3) {
            if (!(t3 instanceof e3))
              throw new TypeError("Cannot call a class as a function");
          }
          function mn(t3, e3) {
            if (typeof e3 != "function" && e3 !== null)
              throw new TypeError("Super expression must either be null or a function");
            t3.prototype = Object.create(e3 && e3.prototype, { constructor: { value: t3, writable: true, configurable: true } }), e3 && gn(t3, e3);
          }
          function gn(t3, e3) {
            return (gn = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function _n(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = On(t3);
              if (e3) {
                var r2 = On(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return wn(this, n2);
            };
          }
          function wn(t3, e3) {
            return !e3 || yn(e3) !== "object" && typeof e3 != "function" ? En(t3) : e3;
          }
          function En(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function On(t3) {
            return (On = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          Object.defineProperty(pn, "register", { value: hn }), hn("play", fn("M6 2.914v18.172L20.279 12 6 2.914z")), hn("pause", fn("M14.333 20.133H19V3.8h-4.667M5 20.133h4.667V3.8H5v16.333z")), hn("volume", fn("M14,3.23V5.29C16.89,6.15 19,8.83 19,12C19,15.17 16.89,17.84 14,18.7V20.77C18,19.86 21,16.28 21,12C21,7.72 18,4.14 14,3.23M16.5,12C16.5,10.23 15.5,8.71 14,7.97V16C15.5,15.29 16.5,13.76 16.5,12M3,9V15H7L12,20V4L7,9H3Z")), hn("muted", fn("M3,9H7L12,4V20L7,15H3V9M16.59,12L14,9.41L15.41,8L18,10.59L20.59,8L22,9.41L19.41,12L22,14.59L20.59,16L18,13.41L15.41,16L14,14.59L16.59,12Z")), hn("cog", fn("M12 15.5A3.5 3.5 0 0 1 8.5 12 3.5 3.5 0 0 1 12 8.5a3.5 3.5 0 0 1 3.5 3.5 3.5 3.5 0 0 1-3.5 3.5m7.43-2.53c.04-.32.07-.64.07-.97 0-.33-.03-.66-.07-1l2.11-1.63c.19-.15.24-.42.12-.64l-2-3.46c-.12-.22-.39-.31-.61-.22l-2.49 1c-.52-.39-1.06-.73-1.69-.98l-.37-2.65A.506.506 0 0 0 14 2h-4c-.25 0-.46.18-.5.42l-.37 2.65c-.63.25-1.17.59-1.69.98l-2.49-1c-.22-.09-.49 0-.61.22l-2 3.46c-.13.22-.07.49.12.64L4.57 11c-.04.34-.07.67-.07 1 0 .33.03.65.07.97l-2.11 1.66c-.19.15-.25.42-.12.64l2 3.46c.12.22.39.3.61.22l2.49-1.01c.52.4 1.06.74 1.69.99l.37 2.65c.04.24.25.42.5.42h4c.25 0 .46-.18.5-.42l.37-2.65c.63-.26 1.17-.59 1.69-.99l2.49 1.01c.22.08.49 0 .61-.22l2-3.46c.12-.22.07-.49-.12-.64l-2.11-1.66z")), hn("webEnterFullscreen", fn("M10,21V19H6.41L10.91,14.5L9.5,13.09L5,17.59V14H3V21H10M14.5,10.91L19,6.41V10H21V3H14V5H17.59L13.09,9.5L14.5,10.91Z")), hn("webExitFullscreen", fn("M19.5,3.09L15,7.59V4H13V11H20V9H16.41L20.91,4.5L19.5,3.09M4,13V15H7.59L3.09,19.5L4.5,20.91L9,16.41V20H11V13H4Z")), hn("enterFullscreen", fn("M3 3h6.429v2.571H5.57V9.43H3V3m11.571 0H21v6.429h-2.571V5.57H14.57V3m3.858 11.571H21V21h-6.429v-2.571h3.858V14.57m-9 3.858V21H3v-6.429h2.571v3.858H9.43z")), hn("exitFullscreen", fn("M14.571 14.571H21v2.572h-3.857V21H14.57v-6.429M3 14.571h6.429V21H6.857v-3.857H3V14.57M6.857 3H9.43v6.429H3V6.857h3.857V3M21 6.857V9.43h-6.429V3h2.572v3.857H21z")), hn("airplay", fn("M6 22h12l-6-6m9-13H3a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h4v-2H3V5h18v12h-4v2h4a2 2 0 0 0 2-2V5a2 2 0 0 0-2-2z"));
          var kn = "control_setting-active", Pn = "control_setting_option-active", xn = function(t3) {
            mn(i2, t3);
            var e3, n2, o2, r2 = _n(i2);
            function i2() {
              var t4;
              vn(this, i2);
              for (var e4 = arguments.length, n3 = new Array(e4), o3 = 0; o3 < e4; o3++)
                n3[o3] = arguments[o3];
              return bn(En(t4 = r2.call.apply(r2, [this].concat(n3))), "id", "settings"), bn(En(t4), "tip", sn.t(Qe)), bn(En(t4), "onItemClick", function(e5) {
                return function() {
                  e5.type === "switch" ? (e5.checked = !e5.checked, e5._switch.toggle(e5.checked), e5.change && e5.change(e5.checked, t4.player, e5)) : (t4.renderOptions(), t4.showOptionPage(e5._optionEl));
                };
              }), bn(En(t4), "onOptionClick", function(e5, n4) {
                return function() {
                  e5.value !== n4.value && (e5.value = n4.value, e5.change && e5.change(n4.value, t4.player, e5)), t4.renderHome(), t4.showHomePage(e5._optionEl);
                };
              }), bn(En(t4), "back", function(e5) {
                return function() {
                  t4.showHomePage(e5._optionEl);
                };
              }), bn(En(t4), "show", function(e5) {
                e5 && x(e5).includes(t4.popover.el) || (t4.tooltip.hide(), t4.renderHome(), t4.popover.show(), t4.homeEl.style.display = "", _(t4.el, kn));
              }), bn(En(t4), "hide", function(e5) {
                e5 && e5.stopPropagation(), t4.tooltip.show(), w(t4.el, kn), t4.currentOptionEl && setTimeout(function() {
                  return t4.showHomePage(t4.currentOptionEl);
                }, 200);
              }), t4;
            }
            return e3 = i2, (n2 = [{ key: "init", value: function(t4, e4, n3) {
              var o3 = this;
              this.player = t4, this.tooltip = n3, _(this.el, "control_setting"), this.items = t4.__settingItems, this.el.appendChild(pn.cog()), this.popover = new Le(this.el, this.hide, { willChange: "width, height" }), this.homeEl = this.popover.panelEl.appendChild(d()), this.setPos(e4), nt(this, this.el, "click", this.show), tt(this, t4.on(c.MOUNTED, function() {
                return o3.showHomePage();
              })), this.items.forEach(function(e5) {
                return e5.init && e5.init(t4, e5);
              }), this.renderHome();
            } }, { key: "update", value: function(t4) {
              this.setPos(t4);
            } }, { key: "setPos", value: function(t4) {
              this.popover.resetPos(), t4 === 2 && this.popover.setBottom();
            } }, { key: "renderHome", value: function() {
              var t4 = this;
              this.items.forEach(function(e4) {
                var n3 = e4._switch || e4._selectedEl || e4._optionEl ? null : d(".control_setting_item");
                if (n3 && (n3.appendChild(d(void 0, void 0, e4.html)), e4.type !== "switch" && n3.appendChild(d(".spacer"))), e4.type === "switch")
                  e4._switch || (e4._switch = new Ve(n3, e4.checked));
                else {
                  if (!e4.options || !e4.options.length)
                    return;
                  e4._selectedEl || (_(n3, "control_setting_item-select"), e4._selectedEl = n3.appendChild(d()));
                  var o3 = e4.options.find(function(t5) {
                    return t5.value === e4.value;
                  });
                  if (!o3)
                    return;
                  e4._selectedEl.innerHTML = o3.selectedHtml || o3.html || "";
                }
                e4._optionEl && (e4._optionEl.style.display = "none"), n3 && (n3.addEventListener("click", t4.onItemClick(e4)), t4.homeEl.appendChild(n3));
              });
            } }, { key: "renderOptions", value: function() {
              var t4 = this;
              this.items.forEach(function(e4) {
                if (e4.type !== "switch") {
                  if (!e4._optionEl) {
                    e4._optionEl = t4.popover.panelEl.appendChild(d());
                    var n3 = e4._optionEl.appendChild(d(".control_setting_item.control_setting_back"));
                    n3.innerHTML = e4.html || "", n3.addEventListener("click", t4.back(e4));
                  }
                  !e4._optionEls && e4.options && (e4._optionEls = e4.options.map(function(n4) {
                    var o3 = e4._optionEl.appendChild(d(".control_setting_option", void 0, n4.html));
                    return o3.addEventListener("click", t4.onOptionClick(e4, n4)), o3;
                  })), e4._optionEls && e4._optionEls.forEach(function(t5, n4) {
                    w(t5, Pn), e4.value === e4.options[n4].value && _(t5, Pn);
                  }), e4._optionEl.style.display = "none";
                }
              });
            } }, { key: "showOptionPage", value: function(t4) {
              this.homeEl.style.display = "none", t4.style.display = "";
              var e4 = C(t4), n3 = e4.width, o3 = e4.height;
              this.popover.applyPanelStyle({ width: "".concat(n3, "px"), height: "".concat(o3, "px") }), this.currentOptionEl = t4;
            } }, { key: "showHomePage", value: function(t4) {
              t4 && (t4.style.display = "none"), this.homeEl.style.display = "";
              var e4 = C(this.homeEl), n3 = e4.width, o3 = e4.height;
              this.popover.applyPanelStyle({ width: "".concat(n3, "px"), height: "".concat(o3, "px") });
            } }]) && dn(e3.prototype, n2), o2 && dn(e3, o2), i2;
          }(Y);
          function Tn(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          var Sn = ["input", "textarea", "select"], jn = function() {
            function t3(e4, n3) {
              var o3, r2, i2, a2 = this;
              !function(t4, e5) {
                if (!(t4 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, t3), i2 = function(t4) {
                if (!(t4.altKey || t4.ctrlKey || t4.metaKey || t4.shiftKey)) {
                  var e5 = document.activeElement;
                  if (e5) {
                    var n4 = e5.tagName.toLowerCase(), o4 = e5.getAttribute("contenteditable");
                    if (Sn.indexOf(n4) > -1 || o4 || o4 === "")
                      return;
                  }
                  var r3 = t4.keyCode ? t4.keyCode : t4.which;
                  if (typeof (i3 = r3) == "number" || B.call(i3) === "[object Number]") {
                    var i3, l3 = false;
                    a2.map[r3] && (a2.map[r3](a2.player), l3 = true, a2.player.control.showTransient()), l3 && (t4.preventDefault(), t4.stopPropagation());
                  }
                }
              }, (r2 = "onKeyDown") in (o3 = this) ? Object.defineProperty(o3, r2, { value: i2, enumerable: true, configurable: true, writable: true }) : o3[r2] = i2, this.player = e4, this.map = Object.create(null);
              var l2 = function(t4) {
                return t4.toast.show("".concat(sn.t(ln), " ").concat(Math.round(100 * t4.volume), "%"), "left-bottom", 500);
              };
              this.register(27, function(t4) {
                t4.fullscreen.exit() || t4.webFullscreen.exit();
              }), this.register(32, function(t4) {
                return t4.toggle();
              }), this.register(37, function(t4) {
                t4.rewind(), t4.toast.show("- ".concat(t4.opts.seekStep, "s"), "left-bottom", 500);
              }), this.register(38, function(t4) {
                t4.incVolume(), l2(t4);
              }), this.register(39, function(t4) {
                t4.forward(), t4.toast.show("+ ".concat(t4.opts.seekStep, "s"), "left-bottom", 500);
              }), this.register(40, function(t4) {
                t4.decVolume(), l2(t4);
              }), n3 && this.enable();
            }
            var e3, n2, o2;
            return e3 = t3, (n2 = [{ key: "register", value: function(t4, e4) {
              this.map[t4] = e4;
            } }, { key: "unregister", value: function(t4) {
              return delete this.map[t4];
            } }, { key: "dispose", value: function() {
              this.map && (et(this), this.map = null);
            } }, { key: "enable", value: function() {
              this.disable(), nt(this, this.player.el, "keydown", this.onKeyDown);
            } }, { key: "disable", value: function() {
              et(this);
            } }]) && Tn(e3.prototype, n2), o2 && Tn(e3, o2), t3;
          }();
          function Cn(t3) {
            return (Cn = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function Rn(t3, e3) {
            if (!(t3 instanceof e3))
              throw new TypeError("Cannot call a class as a function");
          }
          function Ln(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function In(t3, e3) {
            return (In = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function An(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = Nn(t3);
              if (e3) {
                var r2 = Nn(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return Dn(this, n2);
            };
          }
          function Dn(t3, e3) {
            return !e3 || Cn(e3) !== "object" && typeof e3 != "function" ? Bn(t3) : e3;
          }
          function Bn(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function Nn(t3) {
            return (Nn = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          function Mn(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var Hn = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && In(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = An(i2);
            function i2() {
              var t4;
              Rn(this, i2);
              for (var e4 = arguments.length, n3 = new Array(e4), o3 = 0; o3 < e4; o3++)
                n3[o3] = arguments[o3];
              return Mn(Bn(t4 = r2.call.apply(r2, [this].concat(n3))), "id", "play"), Mn(Bn(t4), "onPlay", function() {
                m(t4.pauseIcon), g(t4.playIcon), t4.tooltip.html = sn.t($e);
              }), Mn(Bn(t4), "onPause", function() {
                m(t4.playIcon), g(t4.pauseIcon), t4.tooltip.html = sn.t(Je);
              }), t4;
            }
            return e3 = i2, (n2 = [{ key: "init", value: function(t4, e4, n3) {
              this.tooltip = n3, this.playIcon = this.el.appendChild(pn.play()), this.pauseIcon = this.el.appendChild(pn.pause()), t4.paused ? this.onPause() : this.onPlay(), tt(this, t4.on(c.PLAY, this.onPlay)), tt(this, t4.on(c.PAUSE, this.onPause)), nt(this, this.el, "click", t4.toggle);
            } }]) && Ln(e3.prototype, n2), o2 && Ln(e3, o2), i2;
          }(Y);
          function Un(t3) {
            return (Un = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function Fn(t3, e3) {
            if (!(t3 instanceof e3))
              throw new TypeError("Cannot call a class as a function");
          }
          function Vn(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function zn(t3, e3) {
            return (zn = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function Yn(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = Gn(t3);
              if (e3) {
                var r2 = Gn(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return Xn(this, n2);
            };
          }
          function Xn(t3, e3) {
            return !e3 || Un(e3) !== "object" && typeof e3 != "function" ? Wn(t3) : e3;
          }
          function Wn(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function Gn(t3) {
            return (Gn = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          function Zn(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var qn = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && zn(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = Yn(i2);
            function i2() {
              var t4;
              Fn(this, i2);
              for (var e4 = arguments.length, n3 = new Array(e4), o3 = 0; o3 < e4; o3++)
                n3[o3] = arguments[o3];
              return Zn(Wn(t4 = r2.call.apply(r2, [this].concat(n3))), "id", "volume"), Zn(Wn(t4), "onDragStart", function(e5) {
                t4.onDragging(e5);
              }), Zn(Wn(t4), "onDragging", function(e5) {
                t4.rect.update(), t4.player.volume = J((t4.isVer ? t4.rect.height - e5.clientY + t4.rect.y : e5.clientX - t4.rect.x) / (t4.isVer ? t4.rect.height : t4.rect.width));
              }), Zn(Wn(t4), "onVolumeChange", function() {
                t4.player.muted ? t4.mute() : t4.unmute(), t4.bar.style.transform = "scale".concat(t4.isVer ? "Y" : "X", "(").concat(t4.player.volume, ")");
              }), t4;
            }
            return e3 = i2, (n2 = [{ key: "init", value: function(t4, e4, n3) {
              this.player = t4, this.tooltip = n3, this.isVer = t4.opts.volumeVertical, _(this.el, "control_volume"), _(this.el, "control_volume-".concat(this.isVer ? "ver" : "hor")), this.isVer && n3.hide(), this.volumeIcon = this.el.appendChild(pn.volume()), this.mutedIcon = this.el.appendChild(pn.muted());
              var o3 = this.el.appendChild(d(".control_volume_bars")), r3 = t4.opts.volumeBarLength;
              o3.style[this.isVer ? "height" : "width"] = N(r3) ? r3 : "".concat(r3, "px"), this.bar = o3.appendChild(d(".control_volume_bar")), this.rect = new D(o3, t4), tt(this, t4.on(c.VOLUME_CHANGE, this.onVolumeChange)), tt(this, new I(o3, this.onDragStart, this.onDragging)), nt(this, this.el, "click", function(e5) {
                x(e5).includes(o3) || t4.toggleVolume();
              }), t4.opts.isTouch && nt(this, o3, "touchstart", function(t5) {
                return t5.preventDefault();
              }), this.onVolumeChange();
            } }, { key: "mute", value: function() {
              m(this.mutedIcon), g(this.volumeIcon), this.tooltip.html = sn.t(en);
            } }, { key: "unmute", value: function() {
              m(this.volumeIcon), g(this.mutedIcon), this.tooltip.html = sn.t(tn);
            } }]) && Vn(e3.prototype, n2), o2 && Vn(e3, o2), i2;
          }(Y);
          function Kn(t3) {
            return (Kn = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function Qn(t3, e3) {
            if (!(t3 instanceof e3))
              throw new TypeError("Cannot call a class as a function");
          }
          function Jn(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function $n(t3, e3) {
            return ($n = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function to(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = oo(t3);
              if (e3) {
                var r2 = oo(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return eo(this, n2);
            };
          }
          function eo(t3, e3) {
            return !e3 || Kn(e3) !== "object" && typeof e3 != "function" ? no(t3) : e3;
          }
          function no(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function oo(t3) {
            return (oo = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          function ro(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var io = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && $n(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = to(i2);
            function i2() {
              var t4;
              Qn(this, i2);
              for (var e4 = arguments.length, n3 = new Array(e4), o3 = 0; o3 < e4; o3++)
                n3[o3] = arguments[o3];
              return ro(no(t4 = r2.call.apply(r2, [this].concat(n3))), "id", "time"), t4;
            }
            return e3 = i2, (n2 = [{ key: "init", value: function(t4) {
              var e4 = this;
              _(this.el, "control_time"), t4.opts.live ? (_(this.el, "control_time-live"), this.el.textContent = sn.t(un)) : (this.playedEl = this.el.appendChild(d("span")), this.totalEl = this.el.appendChild(d("span")), this.played = t4.currentTime, this.total = t4.duration, tt(this, t4.on(c.TIME_UPDATE, function() {
                e4.played = t4.currentTime;
              })), tt(this, t4.on(c.DURATION_CHANGE, function() {
                e4.total = t4.duration;
              })));
            } }, { key: "played", set: function(t4) {
              this.playedEl.textContent = it(t4);
            } }, { key: "total", set: function(t4) {
              this.totalEl.textContent = it(t4);
            } }]) && Jn(e3.prototype, n2), o2 && Jn(e3, o2), i2;
          }(Y);
          function ao(t3) {
            return (ao = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function lo(t3, e3) {
            if (!(t3 instanceof e3))
              throw new TypeError("Cannot call a class as a function");
          }
          function co(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function uo(t3, e3) {
            return (uo = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function so(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = ho(t3);
              if (e3) {
                var r2 = ho(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return fo(this, n2);
            };
          }
          function fo(t3, e3) {
            return !e3 || ao(e3) !== "object" && typeof e3 != "function" ? po(t3) : e3;
          }
          function po(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function ho(t3) {
            return (ho = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          function yo(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var bo = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && uo(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = so(i2);
            function i2() {
              var t4;
              lo(this, i2);
              for (var e4 = arguments.length, n3 = new Array(e4), o3 = 0; o3 < e4; o3++)
                n3[o3] = arguments[o3];
              return yo(po(t4 = r2.call.apply(r2, [this].concat(n3))), "id", "web-fullscreen"), yo(po(t4), "enter", function() {
                m(t4.exitIcon), g(t4.enterIcon), t4.tooltip.html = sn.t(Ke);
              }), yo(po(t4), "exit", function() {
                g(t4.exitIcon), m(t4.enterIcon), t4.tooltip.html = sn.t(qe);
              }), t4;
            }
            return e3 = i2, (n2 = [{ key: "init", value: function(t4, e4, n3) {
              this.tooltip = n3, this.enterIcon = this.el.appendChild(pn.webEnterFullscreen()), this.exitIcon = this.el.appendChild(pn.webExitFullscreen()), t4.webFullscreen.isActive ? this.enter() : this.exit(), tt(this, t4.on(c.WEB_ENTER_FULLSCREEN, this.enter)), tt(this, t4.on(c.WEB_EXIT_FULLSCREEN, this.exit)), nt(this, this.el, "click", t4.webFullscreen.toggle);
            } }]) && co(e3.prototype, n2), o2 && co(e3, o2), i2;
          }(Y);
          function vo(t3) {
            return (vo = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function mo(t3, e3) {
            if (!(t3 instanceof e3))
              throw new TypeError("Cannot call a class as a function");
          }
          function go(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function _o(t3, e3) {
            return (_o = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function wo(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = ko(t3);
              if (e3) {
                var r2 = ko(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return Eo(this, n2);
            };
          }
          function Eo(t3, e3) {
            return !e3 || vo(e3) !== "object" && typeof e3 != "function" ? Oo(t3) : e3;
          }
          function Oo(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function ko(t3) {
            return (ko = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          function Po(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var xo = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && _o(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = wo(i2);
            function i2() {
              var t4;
              mo(this, i2);
              for (var e4 = arguments.length, n3 = new Array(e4), o3 = 0; o3 < e4; o3++)
                n3[o3] = arguments[o3];
              return Po(Oo(t4 = r2.call.apply(r2, [this].concat(n3))), "id", "fullscreen"), Po(Oo(t4), "enter", function() {
                m(t4.exitIcon), g(t4.enterIcon), t4.tooltip.html = sn.t(Ze);
              }), Po(Oo(t4), "exit", function() {
                g(t4.exitIcon), m(t4.enterIcon), t4.tooltip.html = sn.t(Ge);
              }), t4;
            }
            return e3 = i2, (n2 = [{ key: "init", value: function(t4, e4, n3) {
              this.tooltip = n3, this.exitIcon = this.el.appendChild(pn.exitFullscreen()), this.enterIcon = this.el.appendChild(pn.enterFullscreen()), t4.fullscreen.isActive ? this.enter() : this.exit(), tt(this, t4.on(c.ENTER_FULLSCREEN, this.enter)), tt(this, t4.on(c.EXIT_FULLSCREEN, this.exit)), nt(this, this.el, "click", t4.fullscreen.toggle);
            } }]) && go(e3.prototype, n2), o2 && go(e3, o2), i2;
          }(Y);
          function To(t3) {
            return (To = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function So(t3, e3) {
            if (!(t3 instanceof e3))
              throw new TypeError("Cannot call a class as a function");
          }
          function jo(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function Co(t3, e3) {
            return (Co = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function Ro(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = Ao(t3);
              if (e3) {
                var r2 = Ao(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return Lo(this, n2);
            };
          }
          function Lo(t3, e3) {
            return !e3 || To(e3) !== "object" && typeof e3 != "function" ? Io(t3) : e3;
          }
          function Io(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function Ao(t3) {
            return (Ao = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          function Do(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var Bo = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && Co(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = Ro(i2);
            function i2() {
              var t4;
              So(this, i2);
              for (var e4 = arguments.length, n3 = new Array(e4), o3 = 0; o3 < e4; o3++)
                n3[o3] = arguments[o3];
              return Do(Io(t4 = r2.call.apply(r2, [this].concat(n3))), "id", "airplay"), Do(Io(t4), "tip", sn.t(cn)), t4;
            }
            return e3 = i2, (n2 = [{ key: "init", value: function(t4) {
              var e4 = this;
              this.isSupport() ? (this.el.appendChild(pn.airplay()), nt(this, t4.video, "webkitplaybacktargetavailabilitychanged", function(t5) {
                t5.availability === "available" ? m(e4.el) : g(e4.el);
              }), nt(this, this.el, "click", function() {
                var e5 = t4.video;
                e5 && e5.webkitShowPlaybackTargetPicker && e5.webkitShowPlaybackTargetPicker();
              })) : g(this.el);
            } }, { key: "isSupport", value: function() {
              return !!window.WebKitPlaybackTargetAvailabilityEvent;
            } }]) && jo(e3.prototype, n2), o2 && jo(e3, o2), i2;
          }(Y);
          function No(t3) {
            return (No = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function Mo(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function Ho(t3, e3) {
            return (Ho = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function Uo(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = zo(t3);
              if (e3) {
                var r2 = zo(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return Fo(this, n2);
            };
          }
          function Fo(t3, e3) {
            return !e3 || No(e3) !== "object" && typeof e3 != "function" ? Vo(t3) : e3;
          }
          function Vo(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function zo(t3) {
            return (zo = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          var Yo = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && Ho(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = Uo(i2);
            function i2(t4, e4) {
              var n3, o3, a2, l2;
              return function(t5, e5) {
                if (!(t5 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, i2), n3 = r2.call(this, t4, ".thumb"), o3 = Vo(n3), l2 = { startSecond: 0, gapSecond: 10, col: 5, row: 5, width: 160, height: 90, images: [] }, (a2 = "opts") in o3 ? Object.defineProperty(o3, a2, { value: l2, enumerable: true, configurable: true, writable: true }) : o3[a2] = l2, n3.updateOptions(e4), n3.timeEl = n3.el.appendChild(d(".thumb_time")), n3.timeEl.textContent = "0:00", n3.rect = tt(Vo(n3), new D(n3.el)), n3;
            }
            return e3 = i2, (n2 = [{ key: "getCurrentThumb", value: function(t4) {
              var e4 = 0 | Math.max(t4 / this.opts.gapSecond - this.ssGapRatio, 0), n3 = this.opts.images[e4 / this.thumbImgPrePic | 0];
              if (n3)
                return { url: n3, x: e4 % this.opts.col * this.opts.width, y: ~~(e4 % this.thumbImgPrePic / this.opts.row) * this.opts.height };
            } }, { key: "updateOptions", value: function(t4) {
              Object.assign(this.opts, t4), this.opts.images.length && (this.imgEl || (this.imgEl = this.el.appendChild(d(".thumb_img"))), this.imgEl.style.width = "".concat(this.opts.width, "px"), this.imgEl.style.height = "".concat(this.opts.height, "px"), this.thumbImgPrePic = this.opts.col * this.opts.row, this.ssGapRatio = this.opts.startSecond / this.opts.gapSecond);
            } }, { key: "update", value: function(t4, e4, n3) {
              if (this.timeEl.textContent = it(t4), this.imgEl) {
                var o3 = this.getCurrentThumb(t4);
                o3 && (this.imgEl.style.backgroundImage = "url(".concat(o3.url, ")"), this.imgEl.style.backgroundPosition = "-".concat(o3.x, "px -").concat(o3.y, "px"));
              } else
                this.rect.update();
              var r3 = this.rect.width / 2;
              this.applyStyle({ left: "".concat(J(e4 - r3, 0, Math.max(0, n3 - 2 * r3)), "px") });
            } }]) && Mo(e3.prototype, n2), o2 && Mo(e3, o2), i2;
          }(Y);
          function Xo(t3) {
            return (Xo = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function Wo(t3, e3) {
            if (!(t3 instanceof e3))
              throw new TypeError("Cannot call a class as a function");
          }
          function Go(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function Zo(t3, e3) {
            return (Zo = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function qo(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = Jo(t3);
              if (e3) {
                var r2 = Jo(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return Ko(this, n2);
            };
          }
          function Ko(t3, e3) {
            return !e3 || Xo(e3) !== "object" && typeof e3 != "function" ? Qo(t3) : e3;
          }
          function Qo(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function Jo(t3) {
            return (Jo = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          function $o(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var tr = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && Zo(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = qo(i2);
            function i2() {
              var t4;
              Wo(this, i2);
              for (var e4 = arguments.length, n3 = new Array(e4), o3 = 0; o3 < e4; o3++)
                n3[o3] = arguments[o3];
              return $o(Qo(t4 = r2.call.apply(r2, [this].concat(n3))), "id", "progress"), $o(Qo(t4), "dragging", false), $o(Qo(t4), "onDragStart", function(e5) {
                t4.dragging = true, t4.rect.update(), t4.onDragging(e5);
              }), $o(Qo(t4), "onDragging", function(e5) {
                var n4 = e5.clientX - t4.rect.x;
                t4.setPlayedBarLength(n4 / t4.rect.width), t4.updateThumbnail(e5.clientX);
              }), $o(Qo(t4), "onDragEnd", function(e5) {
                t4.dragging = false, t4.player.seek(t4.getCurrentTime(e5.clientX));
              }), $o(Qo(t4), "updateBufBar", function() {
                if (!t4.player.buffered.length)
                  return t4.setBufBarLength(0);
                var e5 = t4.player.currentTime, n4 = 0;
                t4.player.eachBuffer(function(o4, r3) {
                  if (o4 <= e5 && r3 >= e5)
                    return n4 = r3 / t4.player.duration, true;
                }), t4.setBufBarLength(n4);
              }), $o(Qo(t4), "updatePlayedBar", function() {
                t4.dragging || t4.setPlayedBarLength(t4.player.currentTime / t4.player.duration);
              }), t4;
            }
            return e3 = i2, (n2 = [{ key: "init", value: function(t4) {
              var e4 = this;
              this.player = t4, _(this.el, "progress"), this.bars = this.el.appendChild(d(".progress_bars")), this.bufBar = this.bars.appendChild(d(".progress_buf")), this.playedBar = this.bars.appendChild(d(".progress_played")), this.dot = this.el.appendChild(d(".progress_dot")), this.dot.appendChild(t4.opts.progressDot || d(".progress_dot_inner")), this.rect = tt(this, new D(this.bars, t4)), this.thumbnail = tt(this, new Yo(this.el, t4.opts.thumbnail)), tt(this, new I(this.el, this.onDragStart, this.onDragging, this.onDragEnd)), tt(this, t4.on(c.TIME_UPDATE, this.updatePlayedBar)), tt(this, t4.on(c.PROGRESS, this.updateBufBar)), tt(this, t4.on(c.UPDATE_OPTIONS, function() {
                return e4.thumbnail.updateOptions(t4.opts.thumbnail);
              })), tt(this, t4.on(c.UPDATE_SIZE, function() {
                t4.playing || e4.resetPlayedBar();
              })), tt(this, t4.on(c.CONTROL_ITEM_UPDATE, function() {
                e4.rect.update(), e4.resetPlayedBar();
              })), nt(this, this.el, "mousemove", ot(function(t5) {
                return e4.updateThumbnail(t5.clientX);
              }), true), t4.opts.isTouch && nt(this, this.el, "touchstart", function(t5) {
                return t5.preventDefault();
              });
            } }, { key: "resetPlayedBar", value: function() {
              this.setPlayedBarLength(this.player.currentTime / this.player.duration);
            } }, { key: "setPlayedBarLength", value: function(t4) {
              this.playedBar.style.transform = "scaleX(".concat(J(t4), ")"), this.dot.style.transform = "translate(".concat(J(t4 * this.rect.width, 0, this.rect.width), "px, -50%)");
            } }, { key: "setBufBarLength", value: function(t4) {
              this.bufBar.style.transform = "scaleX(".concat(J(t4), ")");
            } }, { key: "updateThumbnail", value: function(t4) {
              this.rect.update(), this.thumbnail.update(this.getCurrentTime(t4), t4 - this.rect.x, this.rect.width);
            } }, { key: "getCurrentTime", value: function(t4) {
              return J((t4 - this.rect.x) / this.rect.width) * this.player.duration;
            } }]) && Go(e3.prototype, n2), o2 && Go(e3, o2), i2;
          }(Y);
          function er(t3, e3, n2) {
            var o2 = function(e4) {
              return t3.emit(n2, e4);
            };
            return t3.video.addEventListener(e3, o2), { dispose: function() {
              return t3.video.removeEventListener(e3, o2);
            } };
          }
          function nr(t3, e3, n2) {
            var o2 = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : t3.video, r2 = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : function(e4) {
              t3.emit(n2, e4);
            };
            return o2.addEventListener(e3, ot(r2)), { dispose: function() {
              return o2.removeEventListener(e3, r2);
            } };
          }
          function or(t3, e3) {
            var n2 = t3.style;
            e3.themeColor && n2.setProperty("--theme-color", e3.themeColor), e3.progressBg && n2.setProperty("--progress-bg", e3.progressBg), e3.posterBgColor && n2.setProperty("--poster-bg-color", e3.posterBgColor), e3.volumeProgressBg && n2.setProperty("--volume-progress-bg", e3.volumeProgressBg);
          }
          function rr(t3, e3) {
            if (e3 && (t3.innerHTML = "", e3.length)) {
              var n2 = document.createDocumentFragment();
              e3.forEach(function(t4) {
                var e4 = document.createElement("source");
                Object.keys(t4).forEach(function(n3) {
                  e4.setAttribute(n3, t4[n3]);
                }), n2.appendChild(e4);
              }), t3.appendChild(n2);
            }
          }
          var ir = "nplayer:volume";
          function ar(t3) {
            t3.registerContextMenuItem({ id: "loop", html: sn.t(an), show: function(t4, e3) {
              e3.checked = t4.loop;
            }, click: function(t4, e3) {
              e3.checked = t4.loop = !t4.loop;
            } }), t3.registerContextMenuItem({ id: "pip", html: sn.t(rn), init: function() {
              this.invisible = !("pictureInPictureEnabled" in document);
            }, click: function(t4) {
              t4.loaded && (document.pictureInPictureElement !== t4.video ? t4.video.requestPictureInPicture() : document.exitPictureInPicture());
            } }), t3.registerContextMenuItem({ id: "version", html: "NPlayer v".concat("1.0.10"), disabled: true }), t3.registerSettingItem({ id: "speed", html: sn.t(on), type: "select", value: 1, options: [{ value: 0.25, html: "0.25" }, { value: 0.5, html: "0.5" }, { value: 1, html: sn.t(nn) }, { value: 1.5, html: "1.5" }, { value: 2, html: "2" }], init: function(t4, e3) {
              if (e3.options) {
                for (var n2 = t4.playbackRate, o2 = 0, r2 = e3.options.length; o2 < r2; o2++)
                  if (e3.options[o2].value === n2)
                    return void (e3.value = n2);
              }
              t4.playbackRate = 1;
            }, change: function(t4, e3) {
              this.value = e3.playbackRate = t4;
            } }), t3.registerControlItem(new Hn()), t3.registerControlItem(new qn()), t3.registerControlItem(new io()), t3.registerControlItem(new xn()), t3.registerControlItem(new bo()), t3.registerControlItem(new xo()), t3.registerControlItem(new Bo()), t3.registerControlItem(new tr());
          }
          function lr(t3) {
            var e3 = function(e4) {
              return tt(t3, e4);
            };
            if (e3(er(t3, "durationchange", c.DURATION_CHANGE)), e3(er(t3, "ratechange", c.RATE_CHANGE)), e3(er(t3, "play", c.PLAY)), e3(er(t3, "pause", c.PAUSE)), e3(er(t3, "ended", c.ENDED)), e3(er(t3, "waiting", c.WAITING)), e3(er(t3, "stalled", c.STALLED)), e3(er(t3, "canplay", c.CANPLAY)), e3(er(t3, "loadedmetadata", c.LOADED_METADATA)), e3(er(t3, "error", c.ERROR)), e3(er(t3, "seeked", c.SEEKED)), e3(er(t3, "enterpictureinpicture", c.ENTER_PIP)), e3(er(t3, "leavepictureinpicture", c.EXIT_PIP)), e3(nr(t3, "timeupdate", c.TIME_UPDATE)), e3(nr(t3, "volumechange", c.VOLUME_CHANGE)), e3(nr(t3, "progress", c.PROGRESS)), e3(nr(t3, "resize", c.UPDATE_SIZE, window, function(e4) {
              return function(t4, e5) {
                t4 && t4.rect.changed && t4.emit(c.UPDATE_SIZE, e5);
              }(t3, e4);
            })), window.ResizeObserver) {
              var n2 = new ResizeObserver(ot(function() {
                return t3.emit(c.UPDATE_SIZE);
              }));
              n2.observe(t3.el), e3({ dispose: function() {
                return n2.disconnect();
              } });
            }
            t3.on(c.LOADED_METADATA, function() {
              t3.video.paused ? t3.emit(c.PAUSE) : t3.emit(c.PLAY);
            });
          }
          function cr(t3) {
            return (cr = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function ur(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function sr(t3, e3) {
            return (sr = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function fr(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = yr(t3);
              if (e3) {
                var r2 = yr(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return pr(this, n2);
            };
          }
          function pr(t3, e3) {
            return !e3 || cr(e3) !== "object" && typeof e3 != "function" ? hr(t3) : e3;
          }
          function hr(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function yr(t3) {
            return (yr = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          var dr = "checkbox-active", br = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && sr(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = fr(i2);
            function i2(t4, e4) {
              var n3;
              return function(t5, e5) {
                if (!(t5 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, i2), n3 = r2.call(this, t4, ".checkbox"), e4.html && (n3.el.innerHTML = e4.html), e4.change && nt(hr(n3), n3.el, "click", function() {
                O(n3.el, dr), e4.change(E(n3.el, dr));
              }), e4.checked && _(n3.el, dr), n3;
            }
            return e3 = i2, (n2 = [{ key: "update", value: function(t4) {
              O(this.el, dr, t4);
            } }]) && ur(e3.prototype, n2), o2 && ur(e3, o2), i2;
          }(Y);
          function vr(t3) {
            return (vr = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function mr(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function gr(t3, e3) {
            return (gr = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function _r(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = Or(t3);
              if (e3) {
                var r2 = Or(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return wr(this, n2);
            };
          }
          function wr(t3, e3) {
            return !e3 || vr(e3) !== "object" && typeof e3 != "function" ? Er(t3) : e3;
          }
          function Er(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function Or(t3) {
            return (Or = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          var kr = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && gr(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = _r(i2);
            function i2(t4, e4) {
              var n3, o3, a2, l2, c2;
              (function(t5, e5) {
                if (!(t5 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, i2), n3 = r2.call(this, t4, ".slider"), o3 = Er(n3), l2 = function(t5) {
                var e5 = t5.clientX - n3.rect.x;
                n3.update(e5 / n3.rect.width, e5);
              }, (a2 = "onDrag") in o3 ? Object.defineProperty(o3, a2, { value: l2, enumerable: true, configurable: true, writable: true }) : o3[a2] = l2, n3.opts = e4, n3.rect = new D(n3.el), n3.trackEl = n3.el.appendChild(d(".slider_track")).appendChild(d(".slider_track_inner")), e4.stops) && e4.stops.forEach(function(t5) {
                (c2 = d(".slider_stop", void 0, t5.html ? "<span>".concat(t5.html, "</span>") : "")).style.left = "".concat(100 * t5.value, "%"), n3.el.appendChild(c2);
              });
              return n3.dotEl = n3.el.appendChild(d(".slider_dot")), tt(Er(n3), new I(n3.el, function(t5) {
                n3.rect.update(), n3.onDrag(t5);
              }, n3.onDrag)), nt(Er(n3), n3.el, "touchstart", function(t5) {
                return t5.preventDefault();
              }), n3.step = e4.stops && e4.step, n3.update(e4.value || 0, void 0, false), n3;
            }
            return e3 = i2, (n2 = [{ key: "update", value: function(t4, e4) {
              var n3 = !(arguments.length > 2 && arguments[2] !== void 0) || arguments[2];
              if (this.step) {
                var o3, r3 = 1 / 0, i3 = 0;
                this.opts.stops.forEach(function(e5) {
                  (o3 = Math.abs(t4 - e5.value)) < r3 && (r3 = o3, i3 = e5.value);
                }), t4 = i3, e4 = null;
              }
              e4 = e4 != null ? e4 : t4 * this.rect.width, this.trackEl.style.transform = "scaleX(".concat(J(t4), ")"), this.dotEl.style.transform = "translateX(".concat(J(e4, 0, this.rect.width), "px)"), n3 && this.opts.change && this.opts.change(t4);
            } }]) && mr(e3.prototype, n2), o2 && mr(e3, o2), i2;
          }(Y);
          function Pr(t3) {
            return (Pr = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function xr(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function Tr(t3, e3) {
            return (Tr = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function Sr(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = Rr(t3);
              if (e3) {
                var r2 = Rr(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return jr(this, n2);
            };
          }
          function jr(t3, e3) {
            return !e3 || Pr(e3) !== "object" && typeof e3 != "function" ? Cr(t3) : e3;
          }
          function Cr(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function Rr(t3) {
            return (Rr = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          function Lr(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var Ir = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && Tr(t4, e4);
            }(i2, t3);
            var e3, n2, o2, r2 = Sr(i2);
            function i2(t4, e4) {
              var n3;
              return function(t5, e5) {
                if (!(t5 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, i2), Lr(Cr(n3 = r2.call(this, t4, ".poster")), "tryToPlayed", false), Lr(Cr(n3), "onTimeUpdate", function() {
                n3.player.playing && (n3.tryToPlayed = true, n3.hide(), n3.player.off(c.TIME_UPDATE, n3.onTimeUpdate));
              }), Lr(Cr(n3), "tryHide", function() {
                n3.tryToPlayed && n3.hide();
              }), n3.player = e4, n3.poster = e4.opts.poster, n3.playEl = e4.opts.posterPlayEl || pn.play(), _(n3.playEl, "poster_play"), n3.el.appendChild(n3.playEl), n3.hide(), e4.opts.posterEnable ? (n3.show(), n3.poster && n3.applyStyle({ backgroundImage: "url(".concat(n3.poster, ")") }), nt(Cr(n3), n3.el, "click", function() {
                if (n3.tryToPlayed)
                  return m(n3.playEl), e4.pause(), void (n3.tryToPlayed = false);
                n3.tryToPlayed = true, e4.loaded ? n3.hide() : (g(n3.playEl), e4.loading.show()), e4.play();
              }), tt(Cr(n3), e4.on(c.CANPLAY, n3.tryHide)), tt(Cr(n3), e4.on(c.LOADED_METADATA, n3.tryHide)), tt(Cr(n3), e4.on(c.UPDATE_OPTIONS, function() {
                e4.opts.poster && e4.opts.poster !== n3.poster && !e4.playing && (n3.poster = e4.opts.poster, n3.applyStyle({ backgroundImage: "url(".concat(n3.poster, ")") }), n3.tryToPlayed = false, n3.addTimeUpdateHandler(), n3.show());
              })), n3.addTimeUpdateHandler(), n3) : jr(n3);
            }
            return e3 = i2, (n2 = [{ key: "addTimeUpdateHandler", value: function() {
              this.player.off(c.TIME_UPDATE, this.onTimeUpdate), this.player.on(c.TIME_UPDATE, this.onTimeUpdate);
            } }, { key: "isActive", get: function() {
              return this.el.style.display !== "none";
            } }, { key: "show", value: function() {
              m(this.el), m(this.playEl);
            } }, { key: "hide", value: function() {
              g(this.el);
            } }]) && xr(e3.prototype, n2), o2 && xr(e3, o2), i2;
          }(Y);
          function Ar(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function Dr(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var Br = function() {
            function t3(e4) {
              var n3 = this;
              !function(t4, e5) {
                if (!(t4 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, t3), Dr(this, "startX", 0), Dr(this, "duration", 0), Dr(this, "durationStr", ""), Dr(this, "currentTime", 0), Dr(this, "seekTime", -1), Dr(this, "videoTouched", false), Dr(this, "dragged", false), Dr(this, "onTouchStart", function(t4) {
                t4.touches.length === 1 && (t4.preventDefault(), n3.videoTouched ? (clearTimeout(n3.showControlTimer), n3.videoTouched = false, n3.player.toggle()) : (n3.videoTouched = true, n3.showControlTimer = setTimeout(function() {
                  n3.videoTouched = false;
                  var t5 = n3.player.control;
                  t5.isActive ? t5.hide() : t5.showTransient();
                }, 200)));
              }), Dr(this, "onTouchMove", ot(function(t4) {
                if (t4.touches.length === 1)
                  if (t4.preventDefault(), n3.dragged) {
                    var e5 = t4.touches[0].clientX - n3.startX;
                    if (Math.abs(e5) < 15)
                      return;
                    n3.seekTime = 0 | J(n3.currentTime + e5 / 5, 0, n3.duration), n3.toastItem = n3.player.toast.show("".concat(it(n3.seekTime), " \xB7 ").concat(n3.durationStr), "center", 0);
                  } else
                    n3.duration = n3.player.duration, !n3.player.opts.live && n3.duration && (n3.durationStr = it(n3.duration), n3.currentTime = n3.player.currentTime, n3.startX = t4.touches[0].clientX, n3.seekTime = -1, n3.dragged = true);
              })), Dr(this, "onTouchEnd", function() {
                n3.dragged && (n3.dragged = false, n3.toastItem && setTimeout(function() {
                  return n3.player.toast.close(n3.toastItem);
                }, 200), n3.seekTime >= 0 && Math.abs(n3.seekTime - n3.player.currentTime) > 3 && (n3.player.currentTime = n3.seekTime));
              }), this.player = e4, e4.opts.isTouch && this.enable();
            }
            var e3, n2, o2;
            return e3 = t3, (n2 = [{ key: "enable", value: function() {
              this.player.video.addEventListener("touchstart", this.onTouchStart), this.player.video.addEventListener("touchmove", this.onTouchMove), this.player.video.addEventListener("touchend", this.onTouchEnd), this.player.video.addEventListener("touchcancel", this.onTouchEnd);
            } }, { key: "disable", value: function() {
              this.player.video.removeEventListener("touchstart", this.onTouchStart), this.player.video.removeEventListener("touchmove", this.onTouchMove), this.player.video.removeEventListener("touchend", this.onTouchEnd), this.player.video.removeEventListener("touchcancel", this.onTouchEnd);
            } }, { key: "dispose", value: function() {
              this.toastItem && this.player.toast.close(this.toastItem), this.disable();
            } }]) && Ar(e3.prototype, n2), o2 && Ar(e3, o2), t3;
          }();
          function Nr(t3) {
            return (Nr = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t4) {
              return typeof t4;
            } : function(t4) {
              return t4 && typeof Symbol == "function" && t4.constructor === Symbol && t4 !== Symbol.prototype ? "symbol" : typeof t4;
            })(t3);
          }
          function Mr(t3, e3) {
            var n2 = Object.keys(t3);
            if (Object.getOwnPropertySymbols) {
              var o2 = Object.getOwnPropertySymbols(t3);
              e3 && (o2 = o2.filter(function(e4) {
                return Object.getOwnPropertyDescriptor(t3, e4).enumerable;
              })), n2.push.apply(n2, o2);
            }
            return n2;
          }
          function Hr(t3) {
            for (var e3 = 1; e3 < arguments.length; e3++) {
              var n2 = arguments[e3] != null ? arguments[e3] : {};
              e3 % 2 ? Mr(Object(n2), true).forEach(function(e4) {
                Wr(t3, e4, n2[e4]);
              }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(t3, Object.getOwnPropertyDescriptors(n2)) : Mr(Object(n2)).forEach(function(e4) {
                Object.defineProperty(t3, e4, Object.getOwnPropertyDescriptor(n2, e4));
              });
            }
            return t3;
          }
          function Ur(t3, e3) {
            for (var n2 = 0; n2 < e3.length; n2++) {
              var o2 = e3[n2];
              o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(t3, o2.key, o2);
            }
          }
          function Fr(t3, e3) {
            return (Fr = Object.setPrototypeOf || function(t4, e4) {
              return t4.__proto__ = e4, t4;
            })(t3, e3);
          }
          function Vr(t3) {
            var e3 = function() {
              if (typeof Reflect == "undefined" || !Reflect.construct)
                return false;
              if (Reflect.construct.sham)
                return false;
              if (typeof Proxy == "function")
                return true;
              try {
                return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
                })), true;
              } catch (t4) {
                return false;
              }
            }();
            return function() {
              var n2, o2 = Xr(t3);
              if (e3) {
                var r2 = Xr(this).constructor;
                n2 = Reflect.construct(o2, arguments, r2);
              } else
                n2 = o2.apply(this, arguments);
              return zr(this, n2);
            };
          }
          function zr(t3, e3) {
            return !e3 || Nr(e3) !== "object" && typeof e3 != "function" ? Yr(t3) : e3;
          }
          function Yr(t3) {
            if (t3 === void 0)
              throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t3;
          }
          function Xr(t3) {
            return (Xr = Object.setPrototypeOf ? Object.getPrototypeOf : function(t4) {
              return t4.__proto__ || Object.getPrototypeOf(t4);
            })(t3);
          }
          function Wr(t3, e3, n2) {
            return e3 in t3 ? Object.defineProperty(t3, e3, { value: n2, enumerable: true, configurable: true, writable: true }) : t3[e3] = n2, t3;
          }
          var Gr = function(t3) {
            !function(t4, e4) {
              if (typeof e4 != "function" && e4 !== null)
                throw new TypeError("Super expression must either be null or a function");
              t4.prototype = Object.create(e4 && e4.prototype, { constructor: { value: t4, writable: true, configurable: true } }), e4 && Fr(t4, e4);
            }(a2, t3);
            var e3, n2, o2, i2 = Vr(a2);
            function a2(t4) {
              var e4, n3;
              return function(t5, e5) {
                if (!(t5 instanceof e5))
                  throw new TypeError("Cannot call a class as a function");
              }(this, a2), Wr(Yr(e4 = i2.call(this)), "mounted", false), Wr(Yr(e4), "prevVolume", 1), Wr(Yr(e4), "settingNamedMap", Object.create(null)), Wr(Yr(e4), "contextmenuNamedMap", Object.create(null)), Wr(Yr(e4), "controlNamedMap", Object.create(null)), Wr(Yr(e4), "plugins", new Set()), Wr(Yr(e4), "toggle", function() {
                e4.paused ? e4.play() : e4.pause();
              }), e4.opts = function(t5) {
                var e5 = r(r({}, { shortcut: true, seekStep: 10, volumeStep: 0.1, volumeBarLength: 100, settings: ["speed"], contextMenus: ["loop", "pip", "version"], contextMenuToggle: true, openEdgeInIE: true, posterEnable: true, videoProps: { preload: "auto", playsinline: "true" } }), {}, { isTouch: "ontouchstart" in window || navigator.maxTouchPoints > 0 || navigator.msMaxTouchPoints > 0 }, t5);
                e5.controls = e5.controls || [["play", e5.isTouch ? "" : "volume", "time", "spacer", "airplay", "settings", "web-fullscreen", "fullscreen"], [e5.live ? "" : "progress"]], e5.bpControls = e5.bpControls || { 650: [["play", e5.live ? "" : "progress", "time", "web-fullscreen", "fullscreen"], [], ["spacer", "airplay", "settings"]] }, e5.controls = e5.controls.filter(Boolean).map(function(t6) {
                  return t6.filter(Boolean);
                });
                var n4 = {};
                return Object.keys(e5.bpControls).filter(function(t6) {
                  return t6 && !isNaN(Number(t6)) && Array.isArray(e5.bpControls[t6]);
                }).forEach(function(t6) {
                  n4[t6] = e5.bpControls[t6].filter(Boolean).map(function(t7) {
                    return t7.filter(Boolean);
                  });
                }), e5.bpControls = n4, e5;
              }(t4), (n3 = Yr(e4)).opts.openEdgeInIE && p && (window.location.assign("microsoft-edge:".concat(document.URL)), n3.emit(c.OPEN_EDGE)), sn.setCurrentLang(e4.opts.i18n), e4.container = b(e4.opts.container), e4.el = d(".nplayer", { tabindex: "0" }, void 0, ""), e4.opts.video ? (e4.video = e4.opts.video, _(e4.video, "video")) : e4.video = d("video.video"), e4.opts.src && (e4.opts.videoProps.src = e4.opts.src), or(e4.el, e4.opts), function(t5, e5) {
                e5 && Object.keys(e5).forEach(function(n4) {
                  t5.setAttribute(n4, e5[n4]);
                });
              }(e4.video, e4.opts.videoProps), rr(e4.video, e4.opts.videoSources), function(t5) {
                try {
                  var e5 = parseFloat(localStorage.getItem(ir));
                  isNaN(e5) || (t5.volume = e5);
                } catch (t6) {
                }
              }(e4.video), lr(Yr(e4)), e4.el.appendChild(e4.video), ar(Yr(e4)), e4.rect = tt(Yr(e4), new D(e4.el, Yr(e4))), e4.fullscreen = tt(Yr(e4), new ge(Yr(e4))), e4.webFullscreen = tt(Yr(e4), new Ee(Yr(e4))), e4.shortcut = tt(Yr(e4), new jn(Yr(e4), e4.opts.shortcut)), e4.touch = tt(Yr(e4), new Br(Yr(e4))), e4.toast = tt(Yr(e4), new de(e4.el)), e4.loading = tt(Yr(e4), new qt(e4.el, Yr(e4))), e4.poster = tt(Yr(e4), new Ir(e4.el, Yr(e4))), e4.opts.plugins && e4.opts.plugins.forEach(function(t5) {
                return e4.use(t5);
              }), e4.contextmenu = tt(Yr(e4), new re(e4.el, Yr(e4), e4.opts.contextMenus.map(function(t5) {
                return N(t5) ? e4.contextmenuNamedMap[t5] : t5;
              }).filter(Boolean))), e4.__settingItems = e4.opts.settings.map(function(t5) {
                return N(t5) ? e4.settingNamedMap[t5] : t5;
              }).filter(Boolean), e4.control = tt(Yr(e4), new Ht(e4.el, Yr(e4))), tt(Yr(e4), e4.on(c.CANPLAY, function() {
                var t5 = e4.opts.autoSeekTime || 0.3;
                e4.currentTime < t5 && (e4.currentTime = t5, e4.currentTime < t5 && (e4.video.load(), e4.video.pause())), e4.opts.autoSeekTime = 0;
              })), e4.opts.isTouch || e4.enableClickPause(), e4.emit(c.AFTER_INIT), e4;
            }
            return e3 = a2, (n2 = [{ key: "currentTime", get: function() {
              return this.video.currentTime;
            }, set: function(t4) {
              this.duration && t4 - this.video.currentTime && (this.video.currentTime = J(t4, 0, this.duration));
            } }, { key: "loaded", get: function() {
              return this.video.readyState >= 3;
            } }, { key: "duration", get: function() {
              return this.video.duration || 0;
            } }, { key: "buffered", get: function() {
              return this.video.buffered;
            } }, { key: "volume", get: function() {
              return this.video.volume;
            }, set: function(t4) {
              this.video.volume = J(t4), this.muted && t4 > 0 && (this.muted = false), function(t5) {
                try {
                  localStorage.setItem(ir, String(t5));
                } catch (t6) {
                }
              }(this.video.volume);
            } }, { key: "muted", get: function() {
              return this.video.muted || this.volume === 0;
            }, set: function(t4) {
              this.video.muted = t4;
            } }, { key: "playbackRate", get: function() {
              return this.video.playbackRate;
            }, set: function(t4) {
              this.video.playbackRate = t4;
            } }, { key: "ended", get: function() {
              return this.video.ended;
            } }, { key: "paused", get: function() {
              return this.video.paused;
            } }, { key: "playing", get: function() {
              return !this.paused && !this.ended;
            } }, { key: "loop", get: function() {
              return this.video.loop;
            }, set: function(t4) {
              this.video.loop = t4;
            } }, { key: "enableClickPause", value: function() {
              this.video.addEventListener("click", this.toggle);
            } }, { key: "disableClickPause", value: function() {
              this.video.removeEventListener("click", this.toggle);
            } }, { key: "use", value: function(t4) {
              return !t4 || this.plugins.has(t4) || (t4.dispose && tt(this, t4), t4.apply(this)), this;
            } }, { key: "mount", value: function(t4) {
              this.mounted ? (t4 = b(t4)) && t4 !== this.container && (this.container && this.container.removeChild(this.el), this.container = t4, this.container.appendChild(this.el), this.emit(c.UPDATE_SIZE)) : (t4 && (this.container = b(t4) || this.container), this.container && (this.container.appendChild(this.el), this.emit(c.MOUNTED), this.mounted = true));
            } }, { key: "incVolume", value: function() {
              var t4 = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.opts.volumeStep;
              this.volume += t4;
            } }, { key: "decVolume", value: function() {
              var t4 = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.opts.volumeStep;
              this.volume -= t4;
            } }, { key: "forward", value: function() {
              var t4 = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.opts.seekStep;
              this.currentTime += t4;
            } }, { key: "rewind", value: function() {
              var t4 = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.opts.seekStep;
              this.currentTime -= t4;
            } }, { key: "play", value: function() {
              return this.video.play();
            } }, { key: "pause", value: function() {
              this.video.pause();
            } }, { key: "seek", value: function(t4) {
              this.video.currentTime = J(t4, 0, this.duration);
            } }, { key: "toggleVolume", value: function() {
              this.muted ? (this.volume = this.prevVolume || 1, this.muted = false) : (this.prevVolume = this.volume, this.volume = 0);
            } }, { key: "eachBuffer", value: function(t4) {
              for (var e4 = this.buffered.length - 1; e4 >= 0 && !t4(this.buffered.start(e4), this.buffered.end(e4)); e4--)
                ;
            } }, { key: "registerSettingItem", value: function(t4, e4) {
              this.settingNamedMap[e4 || t4.id] = t4;
            } }, { key: "registerContextMenuItem", value: function(t4, e4) {
              this.contextmenuNamedMap[e4 || t4.id] = t4;
            } }, { key: "registerControlItem", value: function(t4, e4) {
              this.controlNamedMap[e4 || t4.id] = t4;
            } }, { key: "getSettingItem", value: function(t4) {
              return this.settingNamedMap[t4];
            } }, { key: "getContextMenuItem", value: function(t4) {
              return this.contextmenuNamedMap[t4];
            } }, { key: "getControlItem", value: function(t4) {
              return this.controlNamedMap[t4];
            } }, { key: "updateOptions", value: function(t4) {
              t4.videoProps !== this.opts.videoProps && Q(this.video, this.opts.videoProps, t4.videoProps), t4.src && t4.src !== this.opts.src && (this.video.src = t4.src), t4.videoSources !== this.opts.videoSources && rr(this.video, t4.videoSources), this.opts = Hr(Hr({}, this.opts), t4), or(this.el, this.opts), this.opts.shortcut ? this.shortcut.enable() : this.shortcut.disable(), this.emit(c.UPDATE_OPTIONS, this.opts);
            } }, { key: "updateControlItems", value: function(t4, e4) {
              this.control.updateItems(t4, e4);
            } }, { key: "dispose", value: function() {
              if (this.el) {
                this.emit(c.BEFORE_DISPOSE), et(this);
                var t4 = this.opts.plugins;
                t4 && t4.forEach(function(t5) {
                  return t5.dispose && t5.dispose();
                }), this.removeAllListeners(), v(this.el), this.el = null, this.container = null;
              }
            } }]) && Ur(e3.prototype, n2), o2 && Ur(e3, o2), a2;
          }(V);
          Wr(Gr, "__utils", at), Wr(Gr, "EVENT", c), Wr(Gr, "I18n", sn), Wr(Gr, "Icon", pn), Wr(Gr, "components", t2), Wr(Gr, "Player", Gr), Gr.prototype.Player = Gr, Gr.prototype.EVENT = c;
        }(), o;
      }();
    });
  }
});

// node_modules/@nplayer/vue/dist/index.min.js
var require_index_min2 = __commonJS({
  "node_modules/@nplayer/vue/dist/index.min.js"(exports, module) {
    !function(e, t) {
      typeof exports == "object" && typeof module == "object" ? module.exports = t(require_vue(), require_index_min()) : typeof define == "function" && define.amd ? define("NPlayerVue", ["vue", "nplayer"], t) : typeof exports == "object" ? exports.NPlayerVue = t(require_vue(), require_index_min()) : e.NPlayerVue = t(e.Vue, e.NPlayer);
    }(exports, function(e, t) {
      return function() {
        "use strict";
        var n = { 169: function(e2) {
          e2.exports = t;
        }, 205: function(t2) {
          t2.exports = e;
        } }, r = {};
        function o(e2) {
          var t2 = r[e2];
          if (t2 !== void 0)
            return t2.exports;
          var i2 = r[e2] = { exports: {} };
          return n[e2](i2, i2.exports, o), i2.exports;
        }
        o.n = function(e2) {
          var t2 = e2 && e2.__esModule ? function() {
            return e2.default;
          } : function() {
            return e2;
          };
          return o.d(t2, { a: t2 }), t2;
        }, o.d = function(e2, t2) {
          for (var n2 in t2)
            o.o(t2, n2) && !o.o(e2, n2) && Object.defineProperty(e2, n2, { enumerable: true, get: t2[n2] });
        }, o.o = function(e2, t2) {
          return Object.prototype.hasOwnProperty.call(e2, t2);
        };
        var i = {};
        return function() {
          var e2 = o(205), t2 = o(169), n2 = o.n(t2);
          function r2(e3, t3, n3) {
            return t3 in e3 ? Object.defineProperty(e3, t3, { value: n3, enumerable: true, configurable: true, writable: true }) : e3[t3] = n3, e3;
          }
          var u = typeof e2.h == "function", f = { install: function(t3) {
            var o2, i2 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, f2 = (r2(o2 = { name: i2.name || "NPlayer", props: { options: Object, set: Function }, mounted: function() {
              this.$refs.el && typeof document != "undefined" && (this.player || (this.player = new (n2()).Player(this.options)), this.player.mount(this.$refs.el), this.set && this.set(this.player));
            } }, u ? "beforeUnmount" : "beforeDestroy", function() {
              this.player && this.player.dispose();
            }), r2(o2, "render", function(t4) {
              return (t4 = u ? e2.h : t4)("div", { style: { width: "100%", height: "100%" }, ref: "el" });
            }), o2);
            t3.component(f2.name, f2);
          } };
          i.default = f;
        }(), i = i.default;
      }();
    });
  }
});

// dep:@nplayer_vue
var nplayer_vue_default = require_index_min2();
export {
  nplayer_vue_default as default
};
//# sourceMappingURL=@nplayer_vue.js.map
