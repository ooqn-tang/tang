<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.ttcxy.tang.portal.mapper.dao.DtsCommentDao">
    <select id="select" resultType="net.ttcxy.tang.portal.entity.dto.DtsCommentDto">
        select *,(select count(comment_id) from dts_comment where parent_comment_id = t1.comment_id) comment_count
        from dts_comment t1
        inner join uts_author t2 on data_id = #{dataId} and t1.author_id = t2.author_id
    </select>
    <select id="selectLower" resultType="net.ttcxy.tang.portal.entity.dto.DtsCommentDto">
        select *
        from dts_comment t1
        inner join uts_author t2 on parent_comment_id = #{commentId} and t1.author_id = t2.author_id
    </select>
</mapper>