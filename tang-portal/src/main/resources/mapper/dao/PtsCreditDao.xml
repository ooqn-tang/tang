<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.ttcxy.tang.portal.mapper.dao.PtsCreditDao">

    <select id="selectCreditValue" resultType="java.lang.Integer">
        SELECT SUM(value) FROM `pts_credit` where author_id = #{currentId}
    </select>
</mapper>