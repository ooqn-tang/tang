<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ttcxy.mapper.dao.DtsCommentDao">
    <select id="select" resultType="cn.ttcxy.entity.dto.DtsCommentDto">
        select *,(select count(comment_id) from dts_comment where parent_comment_id = t1.comment_id) comment_count
        from dts_comment t1
        inner join uts_author t2 on data_id = #{dataId} and t1.author_id = t2.author_id
    </select>
    <select id="selectLower" resultType="cn.ttcxy.entity.dto.DtsCommentDto">
        select *
        from dts_comment t1
        inner join uts_author t2 on parent_comment_id = #{commentId} and t1.author_id = t2.author_id
    </select>
    <select id="selectAll" resultType="cn.ttcxy.entity.dto.DtsCommentDto">
        select *
        from dts_comment t1 , uts_author t2 , sts_code t3
        where t1.author_id = t2.author_id and t1.state = t3.code_id
        <if test="state != ''">
            and t1.state = #{state}
        </if>
        order by create_date desc
    </select>
</mapper>