<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ttcxy.mapper.dao.DtsMessageDao">
    <select id="selectAuthorMessage" resultType="cn.ttcxy.entity.dto.DtsMessageDto">
        select t1.*,(select nickname from uts_author where author_id = t1.author_id) nickname
        from dts_message t1 where recipient_author_id = #{authorId} order by create_date desc
    </select>
</mapper>